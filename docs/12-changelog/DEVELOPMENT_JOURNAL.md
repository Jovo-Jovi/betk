# DEVELOPMENT_JOURNAL.md
> Step 13. One entry per session/task. Append-only. Update in the same PR as the change.

## Template
```
Date:
Agent/Model Used:
Phase / Task:
Prompt Used:
Files Changed:
Issues Found:
Solutions:
Tests Added:
Docs Updated:
Next Task:
```

## Log
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T13 | CI pipeline + guards. **`.github/workflows/ci.yml`** — 7-job pipeline (install → lint → typecheck → vitest → guards → types-drift → build) plus an opt-in `rls-smoke` job. All main jobs `needs` their predecessor; any failure blocks the chain. **Vitest decision (intentional):** `vitest` job runs only unit tests via `pnpm test:unit` (`vitest run --exclude 'tests/integration/**'`). The RLS smoke test (`tests/integration/rls.smoke.test.ts`) creates/tears down staging auth users and requires Supabase secrets — running it silently in the default CI job would be an unintentional dependency on staging. It is instead gated behind the separate `rls-smoke` job, which runs only on `develop` push and `workflow_dispatch`, fails loudly if staging secrets are absent, and provides all Zod-loader placeholder env vars explicitly. **`types-drift` job:** fails loudly (not silently) if `SUPABASE_PROJECT_REF` or `SUPABASE_ACCESS_TOKEN` GitHub secrets are absent — the error message names the missing secrets and explains how to add them. When secrets are present: regenerates types with `pnpm exec supabase gen types typescript --project-id $SUPABASE_PROJECT_REF --schema betk,betk_analytics > src/lib/supabase/types.ts` then `git diff --exit-code src/lib/supabase/types.ts`. **`guards` job** (parallel to vitest): runs both custom scripts. **`scripts/check-service-import.mjs`** — walks `src/app`, `src/features`, `src/components`; fails if any `.ts/.tsx` file imports `lib/supabase/service` (service-role client bypasses RLS); exits 0 on current tree (verified locally). **`scripts/check-zod-coverage.mjs`** — walks `src/features/[feat]/actions` and `src/app/api`; for each file that imports a Supabase client, verifies it also imports from `@/validations/`, `zod`, or a `*Schema` symbol; exits 0 / 0 files checked (no action files yet — near-no-op as expected, confirmed locally). **`playwright.config.ts`** — wires Playwright for Phase 02+ E2E critical flows (auth OTP, checkout split payment, seller confirm, dispute resolve per CICD_PIPELINE.md); no tests yet; chromium + mobile-chrome projects; Arabic locale; trace on first retry; no webServer block in CI. **`vitest.config.ts` UNTOUCHED** from T08. **`package.json`** — added `test:unit` script. Both guard scripts verified on current tree: exit 0. `pnpm typecheck` clean (exit 0). Files changed: `.github/workflows/ci.yml` (new), `scripts/check-service-import.mjs` (new), `scripts/check-zod-coverage.mjs` (new), `playwright.config.ts` (new), `package.json` (test:unit added). Next: T14 Phase exit verification (Opus 4.8).
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T12 | Constants — routes. Created `src/constants/routes.ts` — 66 route entries across 5 named surfaces: **public** (home `/`, search `/search`, category `/category/[slug]`, listing `/listing/[id]`, store `/store/[slug]`) + **checkout** top-level builder `/checkout?inquiry=[inquiryId]` (top-level per task spec); **auth** (login, verify, register); **buyer** (account, addresses, following, wishlist, inbox, inboxThread, checkoutConfirmation, orders, orderDetail, orderReview, orderDisputeNew, disputeDetail, notifications — 13 entries); **seller** (onboarding, status, dashboard, store, storeDelivery, storeReturns, storePayments, listings, listingNew, listingEdit, listingBoost, inventory, boosts, inbox, inboxThread, orders, orderDetail, reviews, earnings, transactions, payouts, payoutsNew, level, analytics, disputeDetail — 25 entries); **admin** (dashboard, sellerApprovals, users, listings, moderationFlags, moderationLog, reviews, categories, orders, disputes, disputeDetail, payments, payouts, collections, collectionDetail, boosts, notifications, settings — 19 entries). WhatsApp templates OD-5 FROZEN noted inline (no standalone route — sub-tab of `/admin/settings`). Static routes = bare string constants (no trailing slash), dynamic routes = `(param: string) => string` typed builders. Verified cross-reference against UI Spec §2 top-level list + every §3 page Route line — none missed. **enums.ts verified:** actual DB enum count is 34 (doc_type + doc_review_status take it past 32 stated in prior journal); enums.ts covers all 34 — no gap, no action needed. **statusColors.ts verified:** 8 `Record<EnumType, StatusColorPair>` maps, all exhaustive. `pnpm typecheck` clean (exit 0). Files changed: `src/constants/routes.ts` (new). Next: T13 CI types-drift check / Phase 01 DoD review.
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T11 | Service wrappers (typed stubs). **`src/configs/env.ts`** — made all non-Supabase server vars optional in the Zod schema (`GOOGLE_CLIENT_ID/SECRET`, `RESEND_*`, `SENTRY_DSN`, `SUPABASE_*_BUCKET`, `WHATSAPP_*`, `SMS_PROVIDER_KEY`); added optional `BOSTA_API_KEY`; client schema PostHog/Sentry vars also made optional. Rationale: `.env.local` has only the 3 Supabase vars in Phase 01 — required non-core vars caused `parseEnv` to throw on first request. Added `BOSTA_API_KEY` to `.env.example`. **`services/sentry.ts` (EXTEND)** — added `SentryFeature` type (15 feature areas per ARCHITECTURE §6); `setTag(key, value)` persistent scope tag; `setFeatureContext(feature, role?)` sets feature+role tags in one call; `captureMessage(message, extra?)` using `withScope+setExtras` pattern to avoid EventHint type conflict; `captureTaggedError(error, feature, opts?)` uses `withScope+setExtras+captureException` — no `{ extra }` passed to scope.captureException (EventHint-only). T09 exports `SENTRY_INIT_OPTIONS` + `captureError` preserved unchanged. **`services/posthog.ts` (EXTEND)** — added lazy posthog-node singleton (`getNodeClient()` via runtime `require("posthog-node")` inside function body — avoids bundling posthog-node in the client build since `PostHogProvider.tsx` imports this file and cannot have `server-only`); `captureServerEvent(userId, event, properties?)` and `identifyUser(userId)` — both guard on `typeof window !== "undefined"`; no-op+warn if key absent. T09 `POSTHOG_CONFIG` export preserved. **`services/resend.ts` (NEW)** — `server-only`; `EmailTemplateName` union (13 templates); per-template `EmailTemplateVars` interface; lazy `Resend` singleton from `serverEnv.RESEND_API_KEY`; `sendEmail<T>(to, template, vars)` generic over template type; no-op+warn without key; TODO(Phase 03+) for real template render + send. **`services/whatsapp.ts` (NEW)** — `server-only`; R-N02 enforcement: `fetchApprovedTemplate(name)` queries `betk.whatsapp_templates` via `createServiceClient().schema('betk').from(...)` (requires `.schema('betk')` since default client schema is `public`); rejects unapproved/inactive templates with `console.error`; no-op+warn without `WHATSAPP_API_TOKEN`/`WHATSAPP_PHONE_ID`; TODO(Phase 05) for real Cloud API call. **`services/sms.ts` (NEW)** — `server-only`; `sendSms(to, body)` for OTP + SLA alerts; no-op+warn without `SMS_PROVIDER_KEY`; TODO(Phase 04). **`services/courier.ts` (NEW)** — `server-only`; full Bosta API type surface: `BostaShipmentType` const map (SEND=10/CASH_COLLECTION=20/RETURN=30), `BostaContact`, `BostaAddress`, `BostaShipmentInput`, `BostaShipmentResult`, `BostaTrackingEvent`, `BostaTrackingResult`; `createShipment(input)` + `getTracking(trackingNumber)` return safe fallbacks without `BOSTA_API_KEY`; TODO(Phase 06). TypeScript fixes: (1) `captureTaggedError` used `scope.captureException(e, { extra })` — Scope.captureException only accepts EventHint, not CaptureContext → moved to `scope.setExtras()`; (2) whatsapp DB query used `from("whatsapp_templates")` against default public schema → fixed to `.schema("betk")`. `pnpm typecheck` clean (exit 0); lint clean (0 errors/warnings). Files changed: `src/configs/env.ts`, `.env.example`, `src/services/sentry.ts`, `src/services/posthog.ts`, `src/services/resend.ts`, `src/services/whatsapp.ts`, `src/services/sms.ts`, `src/services/courier.ts`. Next: T12 constants/routes.ts (typed route builders per UI Spec §2/§3).
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T10 | Auth middleware skeleton. Created `src/middleware.ts` (@supabase/ssr cookie-bound `createServerClient` per ARCHITECTURE §4) and `src/app/blocked/page.tsx`. **Did NOT touch Sentry** (instrumentation re-wire already committed). Pattern: mutable `NextResponse.next({request})` accumulates refreshed cookies via `setAll`; `getUser()` (not `getSession()`) revalidates the token before any gating. Route classification by URL prefix (route groups are URL-invisible): `/admin*`→admin, `/seller*`→seller, buyer prefixes (`/account /wishlist /orders /inbox /notifications /checkout /disputes`)→buyer, everything else (`/` `/search` `/category` `/listing` `/store` `/auth/*` `/blocked`)→public. Public routes refresh session + pass through with **no DB read** (keeps homepage lean). Protected routes: unauth → `redirect /auth/login?returnUrl=<path+search>` (cookies copied onto redirect). Then **single indexed lookup** `betk.users(role,status,deleted_at)` by PK (RLS `users_self` scopes to caller). **R-A05 block:** `status != 'active' OR deleted_at != NULL` → `/blocked`. Admin gate: role IN ('admin','superadmin') else → `/`. Seller gate: role='seller' else → `/`; **R-S04** non-active `seller_profiles.status` (second focused lookup, skipped on `/seller/status` to avoid loop) → `/seller/status`. Missing profile row / read error → login (no access leak). **OD-4 verified-phone transaction gate left as a documented TODO** (enforced in checkout/become-seller/payout Server Actions + RLS WITH CHECK in Phase 04/07/13) — NOT implemented here. `config.matcher` excludes `_next/static`, `_next/image`, `favicon.ico`, and common static extensions. `app/blocked/page.tsx` = RSC reusing `EmptyState` placeholder (Arabic copy), marked TODO(Phase DS). No login/OTP pages built (Phase 02 boundary). `pnpm typecheck` clean (exit 0); lint clean. Files changed: `src/middleware.ts` (new), `src/app/blocked/page.tsx` (new). Issues: none new — depends on later phases adding permissive INSERT policies (T08 finding) but unrelated to gating. Next: T11 service wrappers (extend sentry/posthog; build resend/whatsapp/sms/courier fresh).
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T09 post-session fix (2/2) | **Sentry instrumentation files moved to src/.** Next.js src-dir projects only auto-load instrumentation files from `src/` — root-level files are silently ignored, meaning `register()` never fired and server/edge capture was dead. `git add` + `git mv` for all four: `instrumentation.ts → src/instrumentation.ts`, `instrumentation-client.ts → src/instrumentation-client.ts`, `sentry.server.config.ts → src/sentry.server.config.ts`, `sentry.edge.config.ts → src/sentry.edge.config.ts`. Relative imports in `src/instrumentation.ts` (`./sentry.server.config`, `./sentry.edge.config`) resolve correctly from the new co-located position. No other file imported these by old root paths. Verified: added temporary `console.log` to `register()`, ran `pnpm dev`, confirmed `[BETK] instrumentation.ts register() fired — runtime: nodejs` in server output before first request. Log removed. `pnpm typecheck` clean (exit 0). Note: `⚠ require-in-the-middle critical dependency` warnings are OpenTelemetry/Sentry SDK internals — harmless, not our code.
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T09 post-session fix (1/2) | **T09 finding — Sentry browser-only:** initial `SentryProvider.tsx` called `Sentry.init()` at module scope in a "use client" component — server / Edge / Server Action errors were not captured. Fixed per ARCHITECTURE §6 by wiring @sentry/nextjs instrumentation hooks. **New files (root):** `instrumentation-client.ts` (Next.js 15.5 native client instrumentation; calls `Sentry.init(SENTRY_INIT_OPTIONS)` once, guarded by DSN presence), `sentry.server.config.ts` (Node.js runtime init using `SENTRY_DSN ?? NEXT_PUBLIC_SENTRY_DSN`), `sentry.edge.config.ts` (Edge runtime init, same DSN fallback), `instrumentation.ts` (`register()` dynamic-imports server/edge configs by runtime; exports `onRequestError = Sentry.captureRequestError` to auto-capture unhandled Server Component / Route Handler / Server Action errors). **Updated:** `src/app/_providers/SentryProvider.tsx` — removed `Sentry.init()` call + `@sentry/nextjs` import; left as a "use client" passthrough with a TODO(Phase 02) for `Sentry.setUser()`. `src/services/sentry.ts` — updated JSDoc to document delegation to instrumentation files and T11 extend-not-recreate contract. `pnpm typecheck` clean. **Carry-forward to T11:** `services/sentry.ts` + `services/posthog.ts` must be EXTENDED (captureMessage, tags, posthog-node); `resend`, `whatsapp`, `sms`, `courier` built fresh.
- 2026-06-23 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T09 | Root layout, providers, RTL shell. Extended `src/app/layout.tsx` (T03 fonts/tokens untouched; added SentryProvider + PostHogProvider + Toaster as "use client" leaf components; layout remains RSC). Created `src/app/_providers/SentryProvider.tsx` ("use client"; calls `Sentry.init(SENTRY_INIT_OPTIONS)` at module scope guarded by `typeof window !== "undefined" && dsn`; child-less passthrough component). Created `src/app/_providers/PostHogProvider.tsx` ("use client"; calls `posthog.init` at module scope with autocapture off + `person_profiles: identified_only` + no pageview; wraps in PHProvider only when key is set — no-op fallback when key absent). Created `src/app/(public)/layout.tsx` (PublicShell placeholder: `<header data-slot="topbar" />` + `<main data-slot="content" />`; TODO Phase 02/03 nav). Created `src/app/loading.tsx` (EmptyState with loading message). Created `src/app/error.tsx` ("use client"; `useEffect` calls `captureError(error, {digest})` on mount; renders ErrorRetryCard with `reset` callback). Created `src/app/not-found.tsx` (EmptyState + `<Link href="/">` back to home). Created `src/components/shared/EmptyState.tsx` + `src/components/shared/ErrorRetryCard.tsx` — minimal unstyled placeholders, `data-slot` attributes only, marked TODO(Phase DS). Created `src/components/ui/sonner.tsx` (RTL-aware Toaster wrapping sonner with `dir="rtl"` + Tailwind token classNames). Created `src/services/sentry.ts` (SENTRY_INIT_OPTIONS satisfies `Parameters<typeof Sentry.init>[0]` + `captureError` helper). Created `src/services/posthog.ts` (POSTHOG_CONFIG const + no-PII policy comment). Added `sonner@2.0.7` dependency. Both providers guard on env var presence → app boots without PostHog/Sentry keys in `.env.local`. `pnpm typecheck` clean; `pnpm lint` clean (0 warnings). Files changed: `src/app/layout.tsx`, `src/app/_providers/SentryProvider.tsx`, `src/app/_providers/PostHogProvider.tsx`, `src/app/(public)/layout.tsx`, `src/app/loading.tsx`, `src/app/error.tsx`, `src/app/not-found.tsx`, `src/components/ui/sonner.tsx`, `src/components/shared/EmptyState.tsx`, `src/components/shared/ErrorRetryCard.tsx`, `src/services/sentry.ts`, `src/services/posthog.ts`, `package.json`/`pnpm-lock.yaml`. Next: T10 auth middleware skeleton (Opus 4.8 security review).
- 2026-06-22 | Opus 4.8 (Cursor Agent) | Phase 01 / T08 | RLS verification harness against **staging** (never production). Files changed: `tests/integration/rls.smoke.test.ts` (new), `vitest.config.ts` (new — aliases `@`→`src`, `server-only`→no-op stub; node env; serial; setupFiles), `tests/setup/env.ts` (new — loads `.env.local` into process.env + backfills Zod-loader placeholders for unused vars so `configs/env.ts` parses in tests), `tests/setup/server-only.stub.ts` (new). Approach: keys via `configs/env.ts` (clientEnv URL/anon) + service client via `lib/supabase/service.ts` (serverEnv service key). OTP-only app, but actors minted through GoTrue admin (`auth.admin.createUser({email,email_confirm,password})`) + matching `betk.users` row (same id): Buyer A/B (phone set), Google G (auth_provider='google', phone NULL), Admin (admin/active), Seller (owns the one active store). Per-actor RLS context = fresh anon `createClient` + `signInWithPassword` (drives `auth.uid()`); one pure anon client. Deterministic + idempotent: per-run unique-string suffix on slug/betk_ref/phone + email-prefix `sweepLeftovers()` before seed; `purgeByUserIds()` in afterAll deletes all betk fixtures (FK-safe, self-derives store/order ids) then every created auth.users row — verified 0 residue via MCP. Hard `STAGING_GUARD` (project-ref allow-list `sojmjvohiziapiwkzsjg`, override via `RLS_ALLOW_PROJECT_REF`) throws before seeding if the target isn't allow-listed. **Result: 6 tests / 5 PASS / 0 FAIL.** A1 default-deny (anon reads active listing; draft + soft-deleted hidden) — PASS, phase NOT blocked. A2 buyer isolation (Buyer B sees 0 rows of A's order, error null — no leak; owner reads own). A3 `is_admin()` true/false for admin/buyer, `my_store_id()` returns seeded store for owner / null for buyer. A4a Google G (phone NULL) REJECTED on orders INSERT (`new row violates row-level security policy`). A4c phone-verified Seller PASSES the phone-gated `payouts` INSERT (positive proof). Issues/findings: (1) **orders & seller_profiles have the RESTRICTIVE phone gate but NO permissive INSERT policy** → authenticated inserts default-denied regardless of phone, so the positive gate path can't be verified on orders (A4b recorded as FINDING, proven on payouts instead) — ownership INSERT policies expected in later checkout/become-seller phases; (2) **append-only `order_status_history`/`moderation_logs` (`DO INSTEAD NOTHING` rules) are undeletable via the Data API** → assertion 5 is opt-in (`RLS_TEST_APPEND_ONLY=1`) and otherwise a FINDING (enforcement structurally guaranteed by rules + no non-admin UPDATE/DELETE policy); (3) ~21 RLS-enabled-no-policy tables (default-deny, parent-scoped) carried from T05; (4) `decrement_stock_on_confirm` trigger still missing from source (carried from T05). Tests added: the harness itself (6 vitest tests). `pnpm typecheck` clean. Next: T09 root layout/providers/RTL shell (Sonnet).
- 2026-06-22 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T07 | Type generation + JSONB interfaces + enum constants. `types.ts` was already current from T05 (no re-gen needed). Created `src/constants/enums.ts` — **34** named type aliases re-exported from `Database["betk"]["Enums"]`, covering all enums in the schema incl. the OD-4 `auth_provider` delta; grouped by domain (identity/auth, seller/store, catalog, orders, payments, shipping, messaging, reviews/disputes, boosts, notifications, admin/moderation, documents). (Note: originally logged as "32" — corrected to 34 after T12 live-DB verification: `select typname … where typtype='e'` returned 34 rows including `doc_type` and `doc_review_status`.) Created `src/types/jsonb.ts` — four hand-written interfaces over `Json`: `StorePaymentMethods` (instapay_handle, vodafone_cash, orange_cash, cod_enabled), `StoreDeliveryOptions` (modes, min/max_delivery_days, delivery_fee_egp, free_delivery_threshold_egp, pickup_governorate, ships_nationwide), `NotificationPrefs` (push/sms/whatsapp/email booleans matching DB default `{"push":true,"sms":true,"whatsapp":true,"email":false}`), `NotificationData` (entity_id, entity_type, action_url, reference, amount_egp + open index). Added `getTyped<T>(json: Json): T` typed assertion helper with JSDoc usage example and type-safety caveat. Updated `src/constants/statusColors.ts`: tightened all 8 `Record<string, …>` declarations to their concrete enum types (`Record<OrderStatus, StatusColorPair>` etc.) — all keys already matched, now enforced at compile time. `pnpm typecheck` clean (0 errors). Files changed: `src/constants/enums.ts` (new), `src/types/jsonb.ts` (new), `src/constants/statusColors.ts` (tightened key types). Next: T08 RLS verification harness (Opus 4.8).
- 2026-06-22 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T06 | Categories taxonomy seed. Verified boost_packages (3) and admin_settings (11) already present in `20260622083013_boosts_admin_analytics.sql` — no duplicates added. Created new CLI-managed migration `supabase/migrations/20260622091700_categories_seed.sql` (first CLI-pattern migration, establishing going-forward convention). SQL uses two idempotent blocks: (1) INSERT of 8 top-level categories (`parent_id NULL`) covering Egypt's informal creative economy — Clothing & Fashion, Jewelry & Accessories, Arts & Crafts, Home & Decor, Beauty & Care, Food & Beverages, Books & Education, Creative Services; (2) INSERT of 31 subcategories (3–4 per top level) with `parent_id` resolved via slug JOIN — fully idempotent ON CONFLICT (slug) DO NOTHING. Applied via `supabase db push` (native binary). Verified on staging: `betk.categories` = **39 rows** (8 top-level + 31 subcategories); all 8 top-level slugs and Arabic names confirmed. `migration list` = **14 migrations**, `20260622091700` present in both Local and Remote, no orphans. Infrastructure finding: `npx supabase` JS wrapper (v2.106.0) hangs silently on Windows — root cause is the wrapper trying to resolve the native binary path before output. Workaround: use `node_modules/.pnpm/@supabase+cli-windows-x64@2.106.0/.../supabase.exe` directly. Files changed: `supabase/migrations/20260622091700_categories_seed.sql`. Next: T07 JSONB interfaces + enum constants, then T08 (Opus) RLS verification harness.
- 2026-06-22 | Opus 4.8 (Cursor Agent) | Phase 01 / T05 | Database migrations (full schema + FREEZE DELTAS). Authored 13 FK-safe migration files under `supabase/migrations/` (`0001_extensions_schemas_enums` → `0013_grants`) by splitting the now-complete authoritative `docs/03-database/BETK_DATABASE_SCHEMA.sql` (1256 lines) per the PHASE_01_FOUNDATION grouping, preserving source statement order. Applied all 13 to **staging** via Supabase MCP `apply_migration`. Verified: **43 tables** (betk 41 + betk_analytics 2); **34 RLS policies** (31 permissive + 3 RESTRICTIVE OD-4 phone-gate on orders/seller_profiles/payouts INSERT); **6 pg_cron jobs**; **4 triggers**; seeds present (3 boost_packages, 11 admin_settings). Freeze deltas confirmed on `betk.users`: `phone_number` nullable VARCHAR(15) UNIQUE, `auth_provider` NOT NULL DEFAULT 'phone', `deleted_at`/`anonymized_at` nullable timestamptz. Ran `pnpm db:types` → `src/lib/supabase/types.ts` regenerated (43 tables + enums incl. auth_provider + betk_analytics); `pnpm typecheck` clean. Issues / findings: (1) source defines **4** triggers — the "stock-decrement on confirm" trigger named in BETK_ERD §7 / DoD is **absent from the source file** and was NOT invented (reproduced FROM FILE ONLY) — flag for DB owner; (2) source has **no GRANT statements** — `0013_grants.sql` contains standard Supabase role-grant plumbing (step 057) and is the one non-verbatim file; betk/betk_analytics still need adding to `config.toml [api].schemas` for Data API exposure; (3) security advisors (expected, all match verbatim source): ~21 tables RLS-enabled-no-policy (default-deny, parent-scoped — remaining policies in T08/later phases), `function_search_path_mutable` on the 6 functions, `extension_in_public` for pg_trgm/unaccent — logged as hardening for the security review gate. Files changed: 13 × `supabase/migrations/*.sql`, `src/lib/supabase/types.ts`. Next: T06 seed data (categories taxonomy) + T07 JSONB interfaces/enum constants, then T08 RLS verification harness (Opus).
- 2026-06-17 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T04 | Supabase clients + local dev. Files changed: `src/configs/env.ts` (Zod loader — `server-only` guard; `clientEnv` validates NEXT_PUBLIC_* + PostHog/Sentry client vars; `serverEnv` validates service key + all server secrets; throws with named-field errors on missing vars), `src/lib/supabase/client.ts` (createBrowserClient, anon key, RLS applies), `src/lib/supabase/server.ts` (async createServerClient, cookie-bound via `next/headers`, getAll/setAll with silent no-op for read-only RSC renders), `src/lib/supabase/service.ts` (createServiceClient, server-only + SERVICE_ROLE top comment, autoRefreshToken+persistSession false), `src/lib/supabase/types.ts` (placeholder `Database = {}` until first gen types run), `.env.example` (17 var names, no values). `supabase init` run successfully (supabase/ dir created, config.toml). `.env.local` confirmed git-ignored. `pnpm typecheck` clean. Next: T05 database migrations (43 tables + OD-2/OD-4 deltas) + `supabase gen types`.
- 2026-06-17 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T03 | Design tokens, RTL shell, shadcn. Files changed: `src/app/globals.css` (full BETK HSL token set — 15 vars light + dark `.dark` class; `--radius: 0.625rem`; body `leading-relaxed`; RTL logical-utility convention documented), `tailwind.config.ts` (all tokens via `hsl(var(--token))`; borderRadius lg/md/sm/full; fontSize display/h1/h2/h3; fontFamily display/body/mono/sans; boxShadow card/card-hover/dialog), `src/app/layout.tsx` (Cairo + IBM Plex Sans Arabic + IBM Plex Mono via `next/font/google`; vars injected on `<html dir="rtl" lang="ar">`), `components.json` (style=default, rtl=true, baseColor=neutral, cssVariables=true, aliases wired), `constants/statusColors.ts` (StatusBadge map — 8 domains × all C3 §2 enum values → `{bg,fg}` Tailwind class pairs + `statusColorMap` unified lookup). Issue: shadcn@4.11.0 CLI is fully interactive (no stdin pipe support in PowerShell); components.json created manually from schema. Decision: tailwindcss-rtl plugin NOT needed — Tailwind 3.3+ has native logical-property utilities. pnpm typecheck clean. Next: T04 Supabase clients + local dev.
- 2026-06-16 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T02 | Directory skeleton. Created full `src/` tree per BETK_CODEBASE_ARCHITECTURE.md §1: `app/(public|auth|buyer|seller/seller|admin/admin|api)`; 15 feature folders (auth discovery buyer-account messaging checkout orders reviews disputes notifications seller-onboarding store-management listings boosts seller-analytics admin) each with components/ hooks/ actions/ queries/ types/ index.ts (with FR/UI-Spec/table traceability comment headers); components/ui + components/shared; lib/supabase + lib/utils.ts (cn helper); services hooks types validations constants configs; tests/{unit,integration,e2e}. READMEs added to all top-level src/ folders. pnpm typecheck clean. Next: T03 design tokens.
- 2026-06-16 | Sonnet 4.6 (Cursor Agent) | Phase 01 / T01 | Repo & tooling init. Created package.json (Next.js 15.5.19, React 19, TS strict, all prod+dev deps per PHASE_01_FOUNDATION T01), tsconfig.json (strict+noUncheckedIndexedAccess+@/* paths), next.config.ts (typedRoutes, outputFileTracingRoot), .eslintrc.json, .prettierrc, postcss.config.js, tailwind.config.ts, src/app/layout.tsx (RTL skeleton), src/app/page.tsx, src/app/globals.css. pnpm install succeeded (696 packages). pnpm typecheck clean. pnpm dev boots blank app at localhost:3000 with dir=rtl lang=ar. Next: T02 directory skeleton.
- 2026-06-13 | Opus 4.8 (Claude.ai chat) | Pre-dev | Generated full Dev OS document set (MVP Scope, PRD, ERD wrapper + SQL contract, Architecture, Codebase, Configuration, 9 AI Skills, AI Team, Master Execution Prompt, Phases, all standards, Git, Cursor setup, .cursorrules, Launch Checklist). Next: sign off OD-1..OD-6, then Phase 01.
- 2026-06-13 | Opus 4.8 (Claude.ai chat) | Pre-dev | Applied MVP Freeze Sheet (OD-1…OD-6). OD-2: added users.deleted_at/anonymized_at (nullable). OD-4: Google OAuth IN — users.phone_number now nullable+UNIQUE, added auth_provider enum/column, verified-phone-before-transacting gate, R-A01 amended (ADR-003 superseded by ADR-008; ADR-009 logs the OD-2 columns). OD-5: WhatsApp templates relocated to Admin→Settings→Notifications; sessions UI confirmed OUT. OD-6: authoritative 43-table inventory + methodology added to ERD §1.1. Files touched: SQL contract, ERD, MVP Scope, PRD, Architecture, Security Guidelines, ADR, UI Spec, Phases, Configuration, Master Prompt, .cursorrules, security skill, SESSION_CONTEXT. Next: confirm OD-4 assumption, then Phase 01.
- 2026-06-13 | Opus 4.8 (Claude.ai chat) | Pre-dev | Integrated Claude Design: created BETK_DESIGN_BRIEF.md; added design-system ownership boundary to codebase architecture, master prompt, .cursorrules, skill-ui-engineer/reviewer; added Phase DS + phase-packs/PHASE_DS_DESIGN_SYSTEM.md; added Claude Design flow to BETK_GIT_WORKFLOW. Visual contract owned by Claude Design (components/ui + components/shared); Cursor composes + wires data only. Placement Option A early vs B late pending.
- 2026-06-13 | Opus 4.8 (Claude.ai chat) | Pre-dev | Auto-execution wiring: converted skills to Cursor Project Rules in .cursor/rules/*.mdc (core always-on; DB/actions/ui/tests auto-attach by globs; security/UI reviews agent-requested). Skills now load automatically from the files in play — human picks the model only; core rule flags model mismatch. Added HOW_RULES_AUTORUN.md; marked docs/.cursorrules legacy.
