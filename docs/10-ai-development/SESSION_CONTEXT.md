# SESSION_CONTEXT.md
> Running state Claude reads at the start of every Cursor session to restore context without relying on chat memory. Update at the end of each session. (Dev OS Step 7 / Memory Guard.)

## Standing facts (read this first — E-1, 2026-09-23)

A fresh window follows this block. Later sections are history. Where they disagree with this block, this block wins.

- **Counts are frozen.** Tables **51** (OD-20). Pages **79** (OD-21). Counting rule: `BETK_UI_SPEC.md` §0 (one route pattern per role, locales once; tabs, modals, drawers, steps, and query strings do not count). Live physical tables **43** (`betk` 41 + `betk_analytics` 2) — **re-measured at Stage C** (2026-09-22 21:56:32 UTC, `pg_class` `relkind = 'r'` in `betk` and `betk_analytics`, Postgres 17.6). RLS on, none forced. Still 43.
- **Numbers, re-read at Stage C before any mint. None taken.** REG-01..REG-92 contiguous after the REG-65 row (the only gap before that row). Highest row **REG-92**. Next free **REG-93**. OD-1..OD-21. Next free **OD-22**. ADR-001..ADR-025. Next free **ADR-026**. **E-1 re-read (2026-09-23):** header still REG-01..REG-92, no REG-93 row, no OD-22, no ADR-026. **None taken.** **P08-T00 re-read (2026-09-26), before the mint:** header still REG-01..REG-92, no REG-93 row. Took **REG-93..REG-99**. Next free **REG-100** / **OD-22** / **ADR-026**. **P08-T01 (2026-09-26):** none taken. Next free stays **REG-100** / **OD-22** / **ADR-026**.
- **Phases.** 01–06 signed on `main`. **07 retired**, never reused, tag `archive/phase-07-v1-single-seller`. **08–20 planned** in `BETK_PHASES.md`. **Stage C is APPROVED** (`docs/03-database/BETK_V2_SCHEMA_DELTA_PLAN.md` §10; approval commit `ac392d8`, approved content `3bec2ea`, merged to `main` as `f41e985`). **CF-10, CF-11, and CF-12** are resolved by Phase 08 T00 (the bullets below). **C-FIX (2026-09-23, scope owner)** recorded D1–D4 as DECIDED in that plan: the five non-cancelled zombies become `cancelled` in M5 with one history INSERT each; `refunded_subtotal` is the admin-supplied goods portion; `submit_seller_application` keeps its argument list through Phase 08; the nine empty `admin_settings` keys fail closed. The plan's header banner (lines 3–5, UNSIGNED) predates §10; §10 governs.
- **Fixed sequence after E-1.** E-1 → human sends the counsel brief to Egyptian counsel → D-1 (Claude Design handoff, CD-DELTA-6) → human pastes it into Claude Design → Phase 08 T00 (must resolve CF-10, CF-11, CF-12) → Phase 08 tasks strictly in order. Phase 09 is **BLOCKED** until Claude Design Wave 1 passes review AND REG-75 + REG-88 are pinned. The legal gate blocks **launch** of phases 09, 11, and 15, not the build. **D-1 handoff is written (2026-09-25):** `docs/00-design/CD_DELTA_6_HANDOFF.md`. Phase 09 stays blocked on Claude Design Wave 1 plus REG-75 and REG-88. **Phase 08 T00 is written (2026-09-26)** and merged as PR #68 (`40f5b9cd877a362aff8c541e333093e299126cf6`). **Phase 08 T01 (2026-09-26)** cut `feature/phase-08-schema` from that SHA and re-measured plan §0. No migration file. Next = human review of the T02 kit in the planning chat, then the human runs the rehearsal, then T02-VERIFY. See the P08-T02 bullet.
- **P08-T02 (2026-09-26).** Option B kit at `docs/03-database/rehearsal/` (`staging-text/M1.sql`–`M6.sql`, `run/00`–`08`, `README.md`). Nothing under `supabase/` or `src/`. **None taken.** Next free stays **REG-100** / **OD-22** / **ADR-026**. **D-A (verbatim):** D-A §4.8 money: the invariant is §4.8's own sentence "Sums are the live totals". The printed "1000.00" is an arithmetic erratum (3 × 100 + 4 × 200 = 1100.00; live 1100.00 at T01). Every money assertion, in the rehearsal and in T05, compares the sums captured immediately before M4 with the sums after. No printed constant. The signed plan file is not edited. **D-B (verbatim):** D-B Rehearsal binding: T03 and T05 apply the staging texts T02 writes, byte-for-byte, verified by sha256. **FLAG:** D-B names T03 and T05 only. T04 applies M4. `M4.sql` is in the kit. T04's prompt was not given the byte-for-byte line. SHA256 (`Get-FileHash -Algorithm SHA256`, hex lowercased): M1 `0593a9dedd107abd1f189823eac9153c9d2c792c2b1c88c40a3e7c600db4be2c`; M2 `287a824160d1da4c6ee942d38c7c1c08e1d9c382e0cc7b4f3a1e9765b4126995`; M3 `47feecf1aec24ef925f2da756bef7df0171a31bb29db32bbf88cecabc50e1037`; M4 `4b3d26eed2fe71789e977d250fe6afe12b0eddbaa2b0e2ef76a8b2887d764db8`; M5 `7d97368a7e2578c4af725df0bff5f0718e25a63595c144c659f8f8ff306d25c7`; M6 `455429f386c21f5054195c5011ffc4581f5215f8590b40ac011c78dd8b48ff32`. Staging re-SELECT (UTC) matches T01: the five targets are pending `81147596-94ee-4a25-b634-34c043409242`, `b327bfb8-f807-418e-9448-1fb645351f3b`, `41c5b2c2-e5e0-4a60-9d28-3dc467a23a2a` and confirmed `02482319-a2a7-4b54-aaf1-8c24b5a95150`, `c7ba4f04-eefd-489a-b8de-5daa917e998b`; keeps cancelled `e5d776fc-1402-484e-84c4-d2b441f5868f`, `da73deed-0670-4cc7-bccd-064b8d301b6f`. History md5s equal plan §3. Listings: `151532d9-f69d-464d-ad8d-741d647c51ae` stock 50, `30e7879e-a811-4986-813a-38393f7274be` NULL, `8edcd182-cc02-4430-ae9a-9049318ca58a` NULL. `list_migrations` still 31, last `20260723140552`. No `apply_migration`. No branch created or deleted.
- **CF-10 (done, P08-T00, 2026-09-26).** `SECURITY_GUIDELINES.md` admin-review parenthetical is now “v1 Phase 14 — retired numbering; v2 owner: Phase 09” (`BETK_PHASES.md` §4.b P49, Phase 09 task “P49 food and seller approval”). `BETK_MODIFICATION_SPEC_REVIEW.md` has a banner over an unchanged body (eight Phase 07+ mentions). `docs/README.md` counts are 79 pages (OD-21), 51 tables (OD-20), OD-1…OD-21. `BETK_DESIGN_BRIEF.md` was not edited (Claude Design-owned). `CD_DELTA_6_HANDOFF.md` §0 already overrides its counts. That brief is a note for the next sanctioned addendum.
- **CF-11 (pinned, P08-T00, 2026-09-26).** The rehearsal sequence is in `phase-packs/PHASE_08_SCHEMA.md` §4. The docs do not contradict it. Agent execution is **BLOCKED**: `execute_sql` and `apply_migration` take no branch ref, and `get_cost` / `confirm_cost` are absent in project-scoped mode. Recommendation: the human runs the branch (option B) and rehearses M4–M6 only. No restorable backup, no M1. T00 did not check the plan tier. **Human decision (2026-09-26), verbatim:** "§4.4 = option B: the human creates the rehearsal branch in the dashboard and runs the SQL; the agent prepares the scripts and checks the pasted results. 3f = rehearse M4–M6 only (plan §7.2). M7–M8 are not run on the rehearsal branch."
- **CF-12 (done, P08-T00, 2026-09-26).** Nine empty keys match plan M3 / §8.2.5. `price_band_min_egp` and `price_band_max_egp` are owned by REG-62. Minted **REG-93..REG-99** for the other seven. Next free **REG-100**. Staging may use a labelled placeholder written by a named later task; production needs the pinned value.
- **Migration ledger, re-measured Stage C.** Local files **31** (method: file list under `supabase/migrations` on `v2-stage-c-schema-delta`, cut from `origin/main` `c88465a`). Remote applied **31**, last version `20260723140552` name `order_payment_write_layer_reg49` (method: MCP `list_migrations` on namespace `project-0-BETK-supabase-betk`). Versions are **1:1**. Pre-existing name drift on the first 13 remote names is unchanged (Stage A-RESUME). `apply_migration` was not called. No file was added under `supabase/migrations/`. **P08-T00 re-measure (2026-09-26)** on `v2-p08-t00` at `a6c9045`: local files **31**, remote **31**, same last version, versions 1:1, the same 13-name prefix. `apply_migration` was not called. **P08-T01 re-measure (2026-09-26)** on `feature/phase-08-schema` at `40f5b9c`: local **31** (method: `(Get-ChildItem -File supabase/migrations).Count`), remote **31** (MCP `list_migrations`), pairwise versions match, last `20260723140552` / `order_payment_write_layer_reg49`, the same 13-name prefix. `apply_migration` was not called. No file was added under `supabase/migrations/`.
- **Security advisor, re-measured Stage C.** Method: MCP `get_advisors` `type=security` on `project-0-BETK-supabase-betk`. Payload `observed_at` **2026-09-22T21:56:46.983Z**. INFO `rls_enabled_no_policy` **8** (`dispute_evidence`, `dispute_messages`, `flagged_content`, `otp_tokens`, `restock_alerts`, `seller_strikes`, `sessions`, `whatsapp_templates`). WARN `function_search_path_mutable` **6**. WARN `extension_in_public` **2**. WARN `anon_security_definer_function_executable` **2**. WARN `authenticated_security_definer_function_executable` **2**. WARN `auth_leaked_password_protection` **1**. The historical 5-tuple **8/6/2/4/1** folded the two definer lints into one bucket of 4. The finding set matches that fold. It is not a new defect. Performance, same method, `type=performance`, `observed_at` **2026-09-22T21:56:46.316Z**: INFO unindexed FK **37**, WARN auth RLS initplan **43**, INFO unused index **30**, WARN multiple permissive policies **32**. The B7 timestamps (21:12:33 / 21:13:09) are the previous measurement. **P08-T00 re-measure (2026-09-26):** security `observed_at` **2026-09-26T10:21:09.168Z** (same six counts, same eight rls-no-policy tables, same six functions, same two extensions, same two definers). Performance `observed_at` **2026-09-26T10:21:10.030Z**: unindexed FK **37**, auth RLS initplan **43**, unused index **30**, multiple permissive policies **32**. Matches plan §0.9. Not a T01 flag. **P08-T01 re-measure (2026-09-26):** security `observed_at` **2026-09-26T14:10:04.675Z** (same six counts, same eight tables, same six functions, same two extensions, same two definers; leaked-password finding in that payload has no `observed_at`). Performance `observed_at` **2026-09-26T14:10:05.186Z**: unindexed FK **37**, auth RLS initplan **43**, unused index **30**, multiple permissive policies **32**. Matches plan §0.9.
- **P08-T01 §0 re-measure (2026-09-26).** Clock `2026-09-26 14:09:58.010354` UTC. Postgres **17.6**. Method: MCP `execute_sql` (`SELECT` only), `list_migrations`, and `get_advisors` on `project-0-BETK-supabase-betk`. No `apply_migration`. No `create_branch`. **§0.1** `pg_class` `relkind = 'r'`: **43** tables (`betk` 41, `betk_analytics` 2). RLS on every one. `relforcerowsecurity` false on every one. Views and materialized views **0**. Column counts match the §0.1 table (`orders` 19, `listings` 23). Absent, as §0.1 says: `listings` `weight_g`, `length_mm`, `width_mm`, `height_mm`, `specs`, `prep_days`, `stock_touched_at`; `inquiries` `quoted_price`, `quoted_prep_days`, `quote_expires_at`, `quoted_at`. `orders` ordinal names match §0.1. `betk_ref` is `varchar(25)` NOT NULL. `delivery_fee` is `numeric(10,2)` NOT NULL default `0`. `subtotal` and `total_amount` are `numeric(10,2)` NOT NULL. `status` default `'pending'`. Row counts (`count(*)`): orders **7**, history **7**, order_items **0**, payments **0**, order_messages **0**, shipments **0**, disputes **0**, reviews **0**, inquiries **3**, listings **3** (all `status = 'active'` and `deleted_at` null), users **5**, stores **3**, addresses **2**, seller_documents **0**, `storage.objects` **5** (`docs` 2, `media` 3). **STOP checks hold:** ledger versions 1:1, orders 7, payments 0, order_items 0, history md5 set equals plan §3. Method for the md5 set: the §3 expression, `ORDER BY created_at, id`. The seven values, in that order, are `357e3f6d12058113ae3d21b5b81929fa`, `7b4f9101c18e69a4be6bef0dfbcb4319`, `5b4eabf2b9789ca19fce5fcde28aabed`, `ca59784dbd81490475bd2b5754c3985a`, `37790ab559cdf19c28d27b294773f8a4`, `ceaf82c5ea6811d9b54dad054ce24d2f`, `ac58be52f37544d8460a4661ad996829`. **§4.9 statuses now:** pending `81147596-94ee-4a25-b634-34c043409242`, `b327bfb8-f807-418e-9448-1fb645351f3b`, `41c5b2c2-e5e0-4a60-9d28-3dc467a23a2a`; confirmed `02482319-a2a7-4b54-aaf1-8c24b5a95150`, `c7ba4f04-eefd-489a-b8de-5daa917e998b`; already cancelled `e5d776fc-1402-484e-84c4-d2b441f5868f`, `da73deed-0670-4cc7-bccd-064b8d301b6f`. All seven `delivery_fee` are 0. `betk_ref` lengths are 17 or 18. **FLAG for T05 (the §0 STOP list does not include it):** live sums are subtotal **1100.00**, delivery_fee **0.00**, total_amount **1100.00**, commission_amount **0.00** (three orders at 100: the three pending ids; four at 200: the two confirmed ids and the two cancelled ids). Plan §4.8 prints **1000.00** beside that same parenthetical. The live sum is 1100.00. Stock capture: `151532d9-f69d-464d-ad8d-741d647c51ae` = 50, `30e7879e-a811-4986-813a-38393f7274be` = NULL, `8edcd182-cc02-4430-ae9a-9049318ca58a` = NULL. Both addresses: governorate Cairo, city Nasr City, street length 11. **§0.2** the §0.2 `pg_enum` query: **34** enums, all in `betk`. `order_status` is `pending, confirmed, preparing, dispatched, delivered, cancelled, returned`. `doc_type` is `national_id_front, national_id_back`. No `ready`. No `escalation_reason`, `return_status`, or `agreement_document`. **§0.3** constraints **148** (`pg_constraint`). Indexes **102** (`pg_indexes`). Rules are the four named in §0.3, each `DO INSTEAD NOTHING`. Named orders-family constraints match §0.3, including the history FK and the four `orders` FKs with no `ON DELETE`, `order_items` `ON DELETE CASCADE`, and the other named FKs with no `ON DELETE`. No listing CHECK whose definition mentions weight. `idx_payments_status` is present. **§0.4 beside ERD §8.** `pg_policies` count **66**. Zero-policy tables are the eight in §0.4. `modlog_admin_insert` count **1**, WITH CHECK `is_admin() AND admin_id = auth.uid()`. `orders_phone_gate` is RESTRICTIVE. Named predicates match §0.4, including the four-key `settings_payment_config_read` array and store still inside `payments_access` and `shipments_access`. Policies whose qual or check calls bare `auth.uid()`: **43**. Policies that call `(select auth.uid())`: **0**. ERD §8 target names still absent from `pg_class`: `cart_items`, `master_orders`, `returns`, `return_evidence`, `agreement_acceptances`, `store_categories`, `courier_rates`, `store_pickup_addresses`, `seller_orders`. `sessions` and `otp_tokens` are inside the zero-policy eight, which is the §8 empty-cell state. The other six zero-policy tables are the ones §8 says Stage C adds; they are still zero, which is the §0 before-picture. **§0.5 and the four objects ADR-025 names.** User triggers match the §0.5 table, including the WHEN clauses on `trg_decrement_stock_on_confirm` (`AFTER UPDATE OF status` when status becomes `confirmed`) and `trg_set_inquiry_converted_order` (`AFTER INSERT` when `inquiry_id` is not null). `trg_enforce_order_transition` is BEFORE UPDATE on `orders`. **14** `betk` functions (`prokind = 'f'`). Posture matches the §0.5 function table, including `create_order_from_inquiry(uuid, uuid, delivery_preference, payment_method)` INVOKER, `search_path=betk, public`, anon EXECUTE false, authenticated EXECUTE true, and `submit_seller_application` still the 15-argument list. `btree_gist` count **0**. Extensions match §0.5 (`pg_trgm` and `unaccent` in `public`). The four ADR-025 objects are present: `create_order_from_inquiry`; `decrement_stock_on_confirm` / `trg_decrement_stock_on_confirm` (DEFINER, EXECUTE revoked from anon and authenticated); `enforce_order_transition` / `trg_enforce_order_transition` (same posture); `set_inquiry_converted_order` / `trg_set_inquiry_converted_order` (same posture). **§0.6** `role_table_grants` on `betk.orders`: anon SELECT, INSERT, UPDATE, DELETE; authenticated SELECT, INSERT, DELETE, and no table-level UPDATE; authenticated UPDATE columns `cancellation_reason` and `status`; service_role includes TRIGGER and TRUNCATE. `payments` authenticated UPDATE columns: `confirmed_at`, `confirmed_by`, `notes`, `proof_path`, `status`, `transfer_reference`. anon has table-level UPDATE on `payments`. `inquiry_messages` authenticated UPDATE is `is_read` only. authenticated and anon both have table-level SELECT on `orders`. **§0.7** `cron.job`: six active jobs, ids 1, 4, 6, 7, 8, 9. Ids 2, 3, and 5 are absent. Job 8 `daily-platform-snapshot` (`5 22 * * *`) is the command that reads `betk.orders`. Buckets: `docs` public false, `media` public true. Storage policies on `storage.objects` are the five named in §0.7. No docs UPDATE or DELETE policy. Both `docs` objects sit under a prefix equal to a `betk.users` id. One of three `media` objects does. `pg_default_acl` on `betk` for `postgres`, tables: `{anon=arwd/postgres,authenticated=arwd/postgres,service_role=arwdDxtm/postgres}`. **M3 keys.** The nine empty keys in pack §5 are absent from `betk.admin_settings` (`SELECT key`).
- **Tests and guards, not re-run at Stage C.** Last measured at **Phase 07 / T04 (2026-07-24)**, with the integration total restated at **RESIDUE-PURGE (2026-07-24)**: guard suite **4**; zod-coverage **37 files**; hardcoded-arabic **68 files**; i18n-parity **827/827**; unit **144/144**; staging integration **215 passed + 5 skipped**. Do not copy these forward as a new measurement.
- **Built `page.tsx` count, re-measured B7.** **26** files under `src/app` (method: `(Get-ChildItem -Recurse -File -Filter page.tsx src/app).Count`, REG-67, on this branch cut from `origin/main`). The 26/26 P-number map was **last measured at B4-FIX2** and was not re-walked here.
- **CF-8.** `project-0-BETK-supabase-betk` is the **only** authoritative Supabase MCP namespace for BETK. The **B2S** org must never answer for BETK work. A B2S `list_projects` result is not staging.
- **CF-9 — effort rule (supersedes the 2026-07-22 Opus/Sonnet sentence).** **Grok 4.7 (high)** is the top-tier executing model and satisfies every former Opus row. Tiers are **Max / High / Medium / Low**. Max = exit gates, RLS, read-first audits. High = migrations, write layers, security review, salvage, T00 packs. Medium = compose-only UI on a settled query layer. Low = mechanical housekeeping. Do not report a model mismatch because the window is Grok 4.7.

## Project state
> **SUPERSEDED baseline (B7, 2026-09-23).** The paragraph below is history. “43 tables · 59 pages” and “scope temporarily UNFROZEN” are **not binding**. Binding counts are **51 tables (OD-20)** and **79 pages (OD-21)**. “Next = B7” in that paragraph is finished by this task.
- **BETK is Arabic-first RTL marketplace (Egypt) — Next.js 15 + Supabase + Tailwind/shadcn + TS strict + Zod.** Phases 01–06 are COMPLETE, MERGED, and SIGNED OFF on `main`: Phase 01 — Foundation (signed off 2026-06-23) · Phase 02 — Auth (signed off 2026-06-30) · OD-7 Bilingual AR/EN + Theme (merged PR #34 @ `648ed4f`, 2026-07-02) · Phase 03 — Catalog & Discovery (merged PR #43 @ `13efd38`, 2026-07-19) · Phase 04 — Seller Onboarding & Store Management (merged PR #44 @ `a80a454`, 2026-07-21) · PERF-01/02 batch (merged PR #45 @ `d3dc150`, 2026-07-21) · Phase 05 — Listings & Inventory (merged PR #46 @ `acb3f1f`, 2026-07-22) · Phase 06 — Messaging & Inquiries (merged PR #47 @ `881832f`, 2026-07-22). **SUPERSEDED (B6, 2026-09-22) — the clause “NEXT: Phase 07 — Checkout & Orders” is retired and is not the next task.** Do not resume Phase 07. Number **07 is never reused** (tag `archive/phase-07-v1-single-seller`). Operative v2 phases are **08–20** in `docs/10-ai-development/BETK_PHASES.md`. **Next = B7.** Historical record continues: branch `feature/phase-07-orders`, pack `docs/10-ai-development/phase-packs/PHASE_07_ORDERS.md` — T00 (2026-07-22) closed out Phase 06 + reorganized this file; **T01 (DB & RLS foundation + contract pinning, Opus Max) is DONE (2026-07-23)** — REG-09 + REG-48 closed, REG-49 opened (owed by T02), the ORDER-SET CONTRACT block is pinned below, migration `20260723074953` landed (ledger 29). **CORRECTION-03 (2026-07-23, Opus High) then landed the OD-8 §9 schema delta** — migration `20260723110557` (ledger 29→30): the 3 additive custodial columns + 2 CHECKs + 6 provisional `admin_settings` payment-config rows, COLUMNS + SEED ONLY (no RLS policy — REG-49 still owed by T02), REG-62 minted (hard pre-launch gate), advisor 8/8 unchanged. **CORRECTION-02B (2026-07-23, Sonnet) then closed the OD-8 code half** — REG-55 (storefront `payment_methods` leak removed) + REG-61 (`hasPaymentMethod` gate reworded to settlement-handles-only) CLOSED, compose-only, i18n parity 730/730; REG-63 + REG-64 minted (record-only). **REG-57 CLOSED by Phase 07 / T01b (2026-07-23, Sonnet High)** — `PHASE_07_ORDERS.md` T02–T06 REGENERATED for the custodial model (payee=BETK, admin deposit-verification gate, commission snapshot, no COD auto-confirm, no pure-COD path); branch `feature/phase-07-orders` re-cut from the fresh post-#49 `main` tip @ `3f76ccb` (containment vs the merged branch verified empty first, local + remote). §0 NAMED DECISION verdict (verbatim): "Under custody, admin deposit confirmation is required for the order lifecycle to advance at all: the seller cannot accept until the deposit lands. Resolution = (c), narrowly. Phase 07 builds the deposit-verification SLICE of `/admin/payments`, not the page's full spec." Register: **REG-65 RESERVED** (seller-onboarding dead delivery-fee-field mint, post-Phase-07); **Phase-07 task mints start at REG-66.** **T02a (read-first audit, 2026-07-23) then T02b (write layer, Opus 4.8, 2026-07-23) are DONE** — migration `20260723140552_order_payment_write_layer_reg49` (ledger 30→31) landed REG-49 (the three-layer actor↔column write model, ADR-019) + the atomic `create_order_from_inquiry` SECURITY INVOKER rpc (ADR-018); **REG-49 CLOSED**, REG-67 minted (record-only, counts-drift Guard F candidate), REG-68 minted+CLOSED (`moderation_logs` admin INSERT, #14-class, F3 pinned), REG-69 minted STANDING (`admin_settings` literal allow-list); ADR-018 + ADR-019 Accepted. Checkout/orders Server Actions + queries + pure rules built (`src/features/checkout`, `src/features/orders`, `requireAdmin` gate); security advisor 8/6/2/4/1 UNCHANGED (+5 attributed perf findings). **T03 (2026-07-23, Sonnet Medium) then built `/checkout` + `/checkout/confirmation/[id]`** (compose-only, zero `components/ui`/`components/shared` diff) — REG-70 minted (record-only). **Phase 07 / T04 (2026-07-24, Sonnet High — flagged UP from the pack's Medium, see the T04 journal entry for why) then built buyer `/orders` + `/orders/[id]`**, folding in a T03-evidence top-up (STEP 1a REG-55 P0 custody non-regression DOM proof; STEP 1b OD-4 client-loop proof; STEP 1c hard-404-by-status-code proof, both `/checkout/confirmation/[id]` AND the new `/orders/[id]`; **STEP 1d BLOCKED — REG-71 minted**, deferred to a dedicated RESIDUE-PURGE window) and a read-only **STOREFRONT-DIAG** (outcome: **C — Claude Design**, REG-72 minted, CD-DELTA-5 handoff written, zero `components/ui`/`components/shared` diff preserved). D6 (buyer-cancel vs. an already-confirmed deposit) flagged UNANSWERED, current behaviour built to spec, **REG-73 minted**. i18n parity 730→**827/827**; physical route-file count 30→**32** (+2). **Phase 07 T01–T04 + RESIDUE-PURGE are committed and green on `feature/phase-07-orders` and UNMERGED** (feature-branch local migrations **31** files ending `20260723140552`; **`origin/main` local dir was 30 files** ending `20260723110557` until Stage A-RESUME 2026-09-19 forwarded the missing file onto `v2-stage-a-ledger-repair` — this branch **31 local / 31 remote 1:1**, last `20260723140552`; `apply_migration` never called; REG-65..74, ADR-018/019, guards 4/4 i18n 827/827, unit 144/144, integration 215+5 — those test counts are Phase-07/T04 figures, UNVERIFIED this session). **Stage A (2026-09-17) recorded the branch disposition** — memo `docs/10-ai-development/BETK_V2_STAGE_A_BRANCH_DISPOSITION.md`; recommend forward-port the T02b **file only** (do not merge `src/`; do not re-apply); tag `archive/phase-07-v1-single-seller`; **REG-77** minted. **Phase 07 is STOPPED at T04 BY DECISION, not failure.** Do not resume Phase 07 as written. The v1 freeze (43 tables · 59 pages · OD-1..OD-8 · AC-BUY-6) is **SUPERSEDED**. Scope is temporarily **UNFROZEN** pending the rewritten ERD (freezes tables) and UI_SPEC (freezes pages). Estimated new size **~50 tables · ~73 pages (ESTIMATES only — not frozen; B3 freezes tables, B4 freezes pages)**. **B1 (2026-09-19) DONE** — `docs/01-product/BETK_MVP_SCOPE.md` rewritten for v2 on branch `v2-b1-mvp-scope`; minted **OD-9..OD-19**. **B1-FIX (2026-09-19) DONE** — minted **REG-78..REG-80** (took next free REG-78 at mint time). **B2 (2026-09-19) DONE** — `docs/01-product/BETK_PRD.md` rewritten for v2 on branch `v2-b2-prd`. Capability-level R/FR/AC (not one-FR-per-page). REG-79 written **OPEN**. REG-78 behaviour-not-principal. REG-80 boost codes **PENDING** in place. Counts still UNFROZEN. Next: **B3 (ERD rewrite)**. **AUTHORITY** = `docs/01-product/BETK_MVP_SCOPE.md` §1–§11 + `docs/10-ai-development/BETK_V2_SCOPE_BASELINE.md` + `docs/10-ai-development/BETK_V2_ROLE_JOURNEYS.md` + the v2 PRD (B3 cites PRD §3 domain-by-domain; does not invent from journeys diagrams). This repo is **PUBLIC** (or about to be) and is the single source of truth for future chat sessions. The GitHub org was transferred to a new account — **CI-SECRET leg discharged 2026-09-17** (`SUPABASE_ACCESS_TOKEN` rotated; `SUPABASE_PROJECT_REF` unchanged; zero token values). Still owed: GitHub remotes, branch protection (see REG-77), Vercel linkage, REG-35 redirect allow-list. Staging project now under Supabase org **BETK**; project ref unchanged. Full per-task narrative history for every phase/task above → `docs/12-changelog/DEVELOPMENT_JOURNAL.md` (append-only log, one entry per task). Recurring patterns/precedents → `docs/PRECEDENTS.md`.

## Standing facts
> **Historical standing facts (B7).** The B7 block at the top is current. Counts, the ledger, the advisor tuple, and “Next = B7” in this section are not a fresh measurement. Test and guard figures here were last measured at Phase 07 / T04 and RESIDUE-PURGE (2026-07-24).
- **Stack:** Next.js 15 · Supabase · Tailwind/shadcn · TS strict · Zod · Resend · PostHog · Sentry · Vercel.
- **ADR-010 (auth model) = Model A (GoTrue-canonical).** GoTrue owns OTP/OAuth + sessions; `betk.users` = find-or-create mirror of `auth.users.id`. `betk.otp_tokens` → app-layer ≤5-attempt limiter (T02, never holds raw OTP); `betk.sessions` → UNUSED in MVP. AC-AUTH-2: GoTrue covers raw-OTP-never-persisted + expired/used-rejected + 60s expiry + session creation; ONLY "≤5 attempts/token" needs app-layer work (T02 verify action, backed by otp_tokens.attempt_count + chk_otp_attempts). Full record: docs/02-architecture/ADR.md.
- **Docs baseline:** full Dev OS set in `docs/`. **Tables FROZEN at 51 (OD-20, B3 2026-09-22). Pages FROZEN at 79 (OD-21, corrected in place B4-FIX2 2026-09-22).** B1 rewrote `BETK_MVP_SCOPE.md`; **B2 rewrote `BETK_PRD.md`**; **B3 rewrote `BETK_ERD.md`**. v1 UI Spec headline 59 is the **historical heading count** (60 headings minus the WhatsApp tab). Under the B4 counting rule that source measures **65** route patterns. v2 freeze is **79** (`BETK_UI_SPEC.md` §0–§3). Initially counted 77 from spec headings; corrected to 79 by reconciliation against built routes on main (B4-FIX). Live schema **today** = 43 tables (`betk` 41 + `betk_analytics` 2) — TRUE TODAY (B3 `pg_tables`). v2 **target** = **51** (OD-20 supersedes OD-6). ~73 was an **estimate** and is not OD-21. Gap list: `BETK_UI_SPEC.md` §8. Open REGs carried after B5: REG-79, REG-85, REG-88, REG-78’s mechanism (principle closed), REG-92 (ADR-020 grant guard). REG-89 closed (ADR-022). REG-91 closed (ADR-023). REG-90 closed (grant ADR-020). ADR range is ADR-001..ADR-025; next free **ADR-026**. Next free REG is **REG-93**. **v2 PRD code families (B2 mint):** new R-C/Q/E/F/U/G/K/V; extended R-A07, R-S10, R-L11–22, R-O11–29, R-N07–08, R-M07–08; new FR-CART/CHK/PAY/QTE/SLA/ESC/STK/RET/AGR/COU/VIS/CLO/COM/CAT + FR-AUTH-4 + FR-ADM-18–21. Next free after B2: see `BETK_PRD.md` §0.2. Boost families **RETAINED** (REG-80 closed 2026-09-22, scope owner — v1 text stands). B2-FIX re-read the register (next free was **REG-81**) and took **REG-81..REG-87**; next free was **REG-88**. **B3 re-read and took REG-88 and REG-89; next free REG-90.** F-MODE is resolved in `BETK_ERD.md` §3.5. **B6 (2026-09-22) DONE** — `BETK_PHASES.md` is the v2 build plan. Operative phases **08 schema → 09 signed-surface delta → 10 cart → 11 checkout → 12 deposit → 13 seller orders → 14 courier → 15 returns → 16 reviews and disputes → 17 notifications → 18 admin → 19 earnings → 20 boosts** (20 may start after 09). Phase **07 stays retired**. Open REGs are homed in that file §4.e. **Next = B7** (build-start entry, including the REG-65 row gap).
- **Guard suite = 4** (`check-service-import` · `check-zod-coverage` · `check-no-hardcoded-arabic` · `check-i18n-parity`, the last closed REG-27). Latest known-green counts (Phase 07 / T04, 2026-07-24): zod-coverage **37 files** · hardcoded-arabic **68 files** · i18n-parity **827/827** leaf keys, 0 orphans (T04 added the full `orders.*` namespace — list/detail/filter/timeline/cancel/empty-states, both locales) · unit **144/144** (`pnpm test:unit`, 10 files = 9 `tests/unit/*` + the `send-sms-hook` edge-fn suite; T04 added `checkoutForm.phoneGate.navigate.unit.test.ts` = 2 cases, the OD-4 CLIENT-half proof — STEP 1b) · full staging integration **215 passed + 5 skipped** (27 files — RESIDUE-PURGE (2026-07-24) re-enabled `orderPayment.noPureCod.dbCheck.test.ts` as a CANARY (`EXPECTED_ORPHANS=7`, REG-71/REG-74) so it now RUNS + PASSES (was `describe.skip`'d at T04, STEP 1d BLOCKED); the remaining 5 skips are the pre-existing T02b/T01/A5 opt-ins) · **route count — REG-67-widened measurement method (physical `page.tsx`/`route.ts` files under `src/app`): 32** (was 30 at T03, +2 = `/orders` + `/orders/[id]`); the historical "44 routes both locales" `next build` route-table figure remains UNRECONCILED (see REG-67) and is no longer the reporting convention.
- **Migration ledger (MEASURED 2026-09-19, Stage A-RESUME — 1:1 restored on this branch).** Local dir = **31** files, last `20260723140552_order_payment_write_layer_reg49` (METHOD: `(Get-ChildItem -File supabase/migrations).Count` on `v2-stage-a-ledger-repair`). Remote applied = **31**, last version `20260723140552` name `order_payment_write_layer_reg49` (METHOD: MCP `list_migrations` on sole namespace `project-0-BETK-supabase-betk`; `get_project_url` = `https://sojmjvohiziapiwkzsjg.supabase.co`). Version-set compare vs `origin/main`'s 30 files: **short by EXACTLY `20260723140552` and nothing else** (pairwise: versions 1–30 identical; row 31 remote-only before this copy). File restored by blob checkout from tag `archive/phase-07-v1-single-seller` (not retyped); git blob SHA **`06238da9332dafd822aa057f42ab7fdfa400c561`** identical on this branch and at the tag path. Filename `20260723140552_order_payment_write_layer_reg49.sql` = `{applied version}_{applied name}.sql` (REG-24). Live objects MATCHES-FILE (pg_policies / pg_proc / pg_trigger / column_privileges). **`apply_migration` was never called.** Table counts (record only, live `information_schema.tables` BASE TABLE): `betk` **41**, `betk_analytics` **2**. Security advisor 8/6/2/4/1 still **UNVERIFIED** (`get_advisors` not run). Historical Stage A measurement (2026-09-17, `origin/main` was 30 local / remote then-UNVERIFIED) and the do-not-re-apply recommendation: `docs/10-ai-development/BETK_V2_STAGE_A_BRANCH_DISPOSITION.md`.
- **Supabase Preview on `origin/main` `f13ac65` = failure (red), NOT a required check.** METHOD: `gh api repos/Jovo-Jovi/betk/commits/f13ac652ff2046fe02dc6665861db3fb8efc267d/check-runs` — check name `Supabase Preview`, app `supabase`, conclusion `failure`, summary `Remote migration versions not found in local migrations directory.` METHOD for required-ness: `gh api repos/Jovo-Jovi/betk/branches/main/protection` → `required_status_checks.contexts = ["RLS smoke (staging)"]` only; `gh api repos/Jovo-Jovi/betk/rulesets` → `[]`. A red Preview (or Types-drift/Guards) on a migration-free PR will merge if nobody looks — see **REG-77**. **Stage A-RESUME (2026-09-19) is the verification:** once `20260723140552_order_payment_write_layer_reg49.sql` merges to `main`, that Preview check is **EXPECTED to go green** (local dir will contain the missing remote version). Do not treat a still-red Preview on pre-merge `main` as a new defect.
- **SECURITY-SWEEP (2026-09-03) — human-supplied evidence, NOT re-run at Stage A.** Full-history scan reported: **186** reachable commits; **gitleaks 8.30.1** (`detect --source . --log-opts="--all"` and extra `--all -m --full-history`) **0 findings**; **trufflehog 3.97.2** (`git file://C:/Users/Marco/Desktop/BETK --only-verified`) **0 verified** (and unverified_secrets=0). Source: SECURITY-SWEEP session [SECURITY-SWEEP](7416078f-11fc-41c1-9927-4dd8d6fc77d2). Stage A did not re-install or re-run the scanners.
- **Supabase org / CI-secret facts (2026-09-17 — human-supplied; zero token values).** Staging Supabase project now sits under Supabase org **BETK**; **project ref UNCHANGED** (`sojmjvohiziapiwkzsjg` — the same staging project as every prior phase; public, committed in `tests/integration/rls.smoke.test.ts` STAGING_ALLOWLIST). Local CLI profile `betk` logged in 2026-09-17. GitHub repo secret `SUPABASE_ACCESS_TOKEN` rotated **2026-09-17T08:51:27Z**; `SUPABASE_PROJECT_REF` left unchanged. This discharges the **CI-SECRET** leg of the org-transfer re-verification item **ONLY**. Still owed: GitHub remotes, branch protection, Vercel linkage, REG-35 redirect allow-list. **Local login does not close REG-24** (login does not parse `config.toml` the way `db push`/typegen do; hook-secret abort unmeasured). MCP remains the confirmed apply path. Stage A MCP probe (`plugin-supabase-supabase` `list_projects` / `list_organizations`) resolved only org **B2S**; workspace `project-0-BETK-supabase` returned permission denied after `mcp_auth` — BETK staging was **not** reachable that Stage A session. **Stage A-RESUME (2026-09-19) reached staging** via sole namespace `project-0-BETK-supabase-betk`; `get_project_url` = `https://sojmjvohiziapiwkzsjg.supabase.co`; `list_migrations` = 31 (last `20260723140552`). Catalog had no `supabase` / no `plugin-supabase-supabase`. `apply_migration` not called; `execute_sql` was SELECT-only introspection.
- **REG-65 gap — closed as a numbering gap by B7 (2026-09-23).** The row now exists. Original meaning: seller-onboarding dead delivery-fee field (baseline §11, close by deletion). **B7 status: open. v2 owner: Phase 09** (OD-10; delivery-mode toggles and the dead fee field retire there). The number stays burnt. Next free stays REG-93. The earlier “do not resolve here; no row minted” sentence is superseded by this bullet and is not deleted: it was true until this task.
- **Storage posture:** `docs` bucket = PRIVATE, own-prefix INSERT, no UPDATE/DELETE (R-S08 retention — a new upload is a new path, the prior object persists). `media` bucket = PUBLIC (serves public URLs), own-prefix INSERT/UPDATE, `.list()` DENIED to non-owners (T01-FIX), NO DELETE policy anywhere (ADR-013/FLAG-1 — row-remove only; the storage object is retained by design).
- **REG-40 (standing rule, load-bearing):** on any ISR route under `[locale]` whose `generateStaticParams` returns `[]`, thread `locale` EXPLICITLY on every render path (`getTranslations({locale})`, `locale` as a prop into Suspense-wrapped children, `locale=` on server `<Link>`) — next-intl's `setRequestLocale` cache-store is not reliably resolved across independently-rendered scopes on the on-demand/non-default-locale path (symptom: 500 on `/en/…`, 200 on `/…`). Full rule: `docs/02-architecture/CACHING_STRATEGY.md §7`.
- **Design system (Claude Design):** the visual system lives in `components/ui` + `components/shared`, OWNED by Claude Design (brief: `docs/00-design/BETK_DESIGN_BRIEF.md`); Cursor composes + wires data, NEVER restyles. Current kit (as of CD-DELTA-4, 2026-07-21): shared = 37 files, `ui/*` = 14 files. Full land-by-land history (DS-LAND/DS-GATE/CD-DELTA-1..4) → journal.
- **SMS/OTP delivery:** production = TorvoSMS via the Supabase Send SMS Hook → Edge Function `send-sms-hook`; registered sender ID `3MS EGY`. **REG-12 + REG-13 are CLOSED (2026-07-22)** — handset delivery confirmed on a physical device AND operator-level sender-ID registration confirmed propagated; the hard pre-launch SMS gate is RETIRED (standing note: if the sender ID ever changes/expires, REG-13 reopens). Full provider/secrets history → journal (this session's archive-fold entry).
- **Staging config:** Supabase Auth provider settings (OTP/OAuth toggles, test phone numbers, OTP length/expiry) are a manual Dashboard action, human-confirmed applied; `supabase/config.toml [auth]` is the version-controlled source-of-truth for local dev; any drift between them is an ops issue, not a code issue.
- **Effort rule (pinned 2026-07-22, Phase 07 / T00):** **SUPERSEDED (B7, 2026-09-23, CF-9).** The operative rule is in the B7 standing-facts block: Grok 4.7 (high) satisfies every former Opus row; tiers are Max / High / Medium / Low. Historical text, not deleted: **Opus+Max** = exit gates, RLS/policy design, ADR decisions, read-first audits · **Opus+High** = write layers, migrations, security review · **Sonnet+Medium** = compose-only UI on a settled query layer · **Sonnet+High** = delicate docs/preservation work · **Sonnet+Low** = mechanical housekeeping.
- **Execution model:** skills auto-attach via `.cursor/rules/*.mdc` (core always-on + glob-scoped DB/actions/ui/tests + agent-requested security/UI reviews). You pick the model only. **CF-9:** the core rule does not flag a mismatch when the window is Grok 4.7. Ref: `HOW_RULES_AUTORUN.md`.
- **Communication posture (load-bearing, human posture 2026-07-23; v2 SUPERSEDED in place B1-FIX 2026-09-19):** **ALL buyer↔seller communication is IN-APP only** (the `inquiries` thread). There is **NO off-platform contact affordance, ever** — a doc/UI line naming an external channel (WhatsApp/FB/Instagram/etc.) is ONE of exactly three things: a **share** (channel-agnostic device share sheet on a public link — listing/storefront), a **BETK→user notification** (WhatsApp templates under Admin → Settings, OD-5; delivery, not conversation), or **BETK support** (BETK→user support channel, e.g. a support WhatsApp deep-link on the footer, REG-50) — **never counterparty contact.** **SUPERSEDED (v1, not deleted):** Identity/address DO cross at fulfilment, but only via the **courier/shipping label**: the BETK-generated label prints the buyer's name + phone + address for delivery, and per **REG-14 the `delivery` mode covers self-delivery — so a self-delivering seller IS the courier** and legitimately reads the label. **v2 operative (OD-10, OD-17, N28):** the seller sees **no buyer identity and no buyer location** — not name, not phone, not address, not city. Order ref + items + prep deadline only. The BETK-generated courier label remains the fulfilment handoff, visible to **admin and the courier**. Self-delivery is retired (OD-10). How RLS names the courier principal is **REG-78** (B3). This remains the standing basis for REG-45 (closed-as-not-a-defect) and `docs/PRECEDENTS.md`.

## Scope decisions (v2 — tables FROZEN at 51 under OD-20; pages FROZEN at 79 under OD-21; full register in `BETK_MVP_SCOPE.md` §4)
> **SUPERSEDED as a binding freeze (B7, 2026-09-23) — v1 Frozen decisions.** The OD-1..OD-8 sheet below is the v1 freeze. It is not the v2 inventory when read alone. **43 tables and 59 pages in this sheet are not the freeze.** Binding counts are **OD-20 = 51** and **OD-21 = 79**. Rows that say **HOLDS UNCHANGED** (OD-1, OD-2, OD-3, OD-4, OD-5, OD-7) stay the v2 decision for those ODs. OD-6 is superseded by OD-20. OD-8 holds with the B1 amendment.
v1 freeze sheet dispositions (B1, 2026-09-19; authority = `BETK_V2_SCOPE_BASELINE.md` §11):
- OD-1 low-stock DERIVED (no inventory_alerts table). Schema: NO. **HOLDS UNCHANGED.**
- OD-2 account = DEACTIVATE-only. Schema: YES — `users.deleted_at`, `users.anonymized_at` (nullable; no MVP behavior beyond deactivation). **HOLDS UNCHANGED.**
- OD-3 broadcast = no campaign entity (fan-out to notifications). Schema: NO. **HOLDS UNCHANGED.**
- OD-4 Google OAuth IN. Schema: YES — `users.phone_number` nullable+UNIQUE; `auth_provider`. Phone-OTP + Google OAuth; **verified phone required before transacting** (v1 named surfaces: checkout/become-seller/payout). **HOLDS UNCHANGED.** **FLAG / REG-79:** N21 pins **account** before add-to-cart, not verified-phone before add-to-cart. **B2 wrote R-A07 / FR-AUTH-4 / AC-AUTH-4 with the gate location OPEN** — did not pick add-to-cart vs checkout. See `BETK_MVP_SCOPE.md` §4.1 OD-4 FLAG and `BETK_PRD.md` §3.15.
- OD-5 sessions UI OUT; WhatsApp templates under Admin → Settings → Notifications tab. Schema: NO. **HOLDS UNCHANGED.**
- OD-6 table count = 43 (historical inventory). Schema: NO. **SUPERSEDED by OD-20.** **43 is TRUE TODAY** (`betk` 41 + `betk_analytics` 2, B3 `pg_tables` 2026-09-22). **Target frozen at 51.**
- OD-7 bilingual AR/EN (next-intl, AR default unprefixed, EN under /en) + light/dark theme (next-themes) — presentation-layer only. Schema: NO. **HOLDS UNCHANGED.** (PR #34)
- OD-8 **custodial payments & platform commission (SIGNED 2026-07-23; ADR-016).** Buyer pays BETK; admin verifies deposit; seller balance DERIVED; no gateway / no automated capture / no automated payouts; no pure-COD. **HOLDS WITH AMENDMENT** (B1): (1) per-seller-order commission snapshot on subtotal only; (2) InstaPay-ONLY buyer deposit rail; (3) ONE admin verification across all deposit rows under the master; (4) no seller acceptance (AC-SEL-14 retired); (5) courier-collected COD balance per shipment. Historical “table count 43 HOLDS / page count 59 HOLDS / order acceptance stays the seller's act” sentences are superseded. Landed columns remain; v2 schema inventory is B3. Full v1 record: `docs/10-ai-development/OD8_CUSTODIAL_PAYMENTS.md`.

v2 ODs minted B1 (took OD-9..OD-19 at mint time). **B3 re-read: next free was OD-20; took OD-20. Next free was OD-21. B4 re-read: next free was OD-21; took OD-21. Next free OD-22.**
- OD-9 cart → master → N seller orders → N shipments. Schema: YES.
- OD-10 courier-only delivery + origin×destination×weight rate matrix; combined buyer total; per-seller-order fee. Schema: YES. **`delivery_preference` resolved by B3** (`BETK_ERD.md` §3.5): enum kept, dead members retained, existing values not rewritten.
- OD-11 custom-item quoting = price discovery feeding a cart line (N23 band + 24h + prep time). Schema: YES.
- OD-12 returns with a dedicated evidence table (N25). Schema: YES.
- OD-13 agreements/T&C at signup + version-gate before order completion (N26, buyers included). Schema: YES.
- OD-14 escalation is the only seller exit (seller can never cancel). Schema: YES (columns on seller order, not a table).
- OD-15 stock: decrement at checkout, restore on cancel/expiry, NOT on return, ZERO on out-of-stock escalation. Schema: NO.
- OD-16 prep SLA: `confirmed_at + MAX(prep_days)`, 50%/80% ladder, breach auto-escalates. Schema: YES.
- OD-17 address visibility: admin + courier only; seller sees no buyer identity or location (N28). Schema: NO (read restriction). **REG-78 principle closed (ADR-024):** no courier RLS principal. Admin-initiated label is the admin’s own RLS read. Automated handoff, not picked, would be a service-role read. Neither branch uses a definer. Mechanism stays open under the courier gate.
- OD-18 derived closure + derived seller balance (no close action, no invented enum member). Schema: NO.
- OD-19 catalogue: products only; fixed price; shipping attributes mandatory; seller categories ≤3; eligibility band. Schema: YES. **Boosts (G6) are IN as retained v1 scope. REG-80 CLOSED** (2026-09-22, scope owner; not an OD).
- OD-20 **table count FROZEN at 51** (`betk` 49 + `betk_analytics` 2). Supersedes OD-6. This OD does not freeze pages. Schema: YES (the inventory). Taken at mint 2026-09-22 (next free was OD-20).
- OD-21 **page count FROZEN at 79** (route patterns; counting rule in `BETK_UI_SPEC.md` §0). Supersedes the v1 headline of 59. Initially counted 77 from spec headings; corrected to 79 by reconciliation against built routes on main (B4-FIX). Schema: NO. Taken at mint 2026-09-22 (next free was OD-21). Corrected in place. Gap list: `BETK_UI_SPEC.md` §8. P78 `/auth/phone`, P79 `/blocked`.

## v2 decisions (signed, do not re-open)
- **N21** No guest cart. An account is required before the first add-to-cart.
- **N22** ONE payment proof at MASTER-ORDER level. DIRECTION: each child seller order's deposit row SNAPSHOTS the proof reference at verification time. **B3 VALIDATED** this (`BETK_ERD.md` §3.2). **B5 ADR-021 accepted it:** the buyer writes `master_orders`; a DEFINER trigger copies onto each deposit row; ADR-019’s `authenticated` UPDATE grant on `payments` drops `proof_path` and `transfer_reference`.
- **N23** Custom-item quoting: the seller may quote up to 2x the original/requested listing price. Band = [listing price, 2x listing price]. Never below the listing price. Valid 24h. The quote also states that item's prep time.
- **N25** Returns get a DEDICATED returns-evidence table. Do NOT reuse `dispute_evidence` — different lifecycle and purpose, keep them cleanly separated.
- **N26** Terms acceptance at SIGNUP, and version-gated re-confirmation before any order can complete. `agreement_acceptances` therefore covers BUYERS as well as sellers.
- **N27** Staging MIGRATES, it does not reset. Preserve current state and migrate it, so the migration path itself is validated.
- **N28** Sellers see NO buyer location — not address, not city. Order ref + items + prep deadline only.

## Consolidated issue register (REG-01..REG-99; next free = REG-100) — canonical, adopted 2026-07-16 (R4)
> **B7 measurement (2026-09-23). Re-read before the REG-65 row. None taken past REG-92.** Rows present before that insert: **91**. Only gap: **REG-65**. Highest row: **REG-92**. No REG-93 row. After the insert, REG-01..REG-92 are contiguous. Next free stays **REG-93**. Next free OD stays **OD-22**. Next free ADR stays **ADR-026**. Each row ends with a **B7** status. A historical “Phase 08–14” inside a row is a **v1 heading (§H)** unless the B7 owner names a v2 phase. Map: 08 delivery → v2 14 courier (RLS still v2 08); 09 reviews → v2 16; 10 disputes → v2 16; 11 boosts → v2 20; 12 notifications → v2 17; 13 earnings → v2 19; 14 admin → v2 18. Phase 07 is retired and owns nothing.
> **P08-T00 re-read (2026-09-26), before any mint.** Header was REG-01..REG-92, next free **REG-93**, no REG-93 row. Took **REG-93..REG-99** (seven empty `admin_settings` keys with no owning REG). Next free **REG-100**. Next free OD stays **OD-22**. Next free ADR stays **ADR-026**.
> **Single namespace going forward.** Supersedes the scattered `open-issue #n` / T14-findings numbering (full per-task narrative history → `docs/12-changelog/DEVELOPMENT_JOURNAL.md`); "Old ref" cross-references the pre-register numbering for traceability. Refer to REG-nn for open work from here on. Adopted verbatim from `docs/10-ai-development/REPAIRS/BETK_REPAIR_PLAN.md`, statuses reconciled to what's actually run at this point (R1/R2/R3/R4 done; R5 done 2026-07-16 → REG-08 closed).

| REG | Item | Old ref | Owner / gate | Status |
|---|---|---|---|---|
| REG-01 | Migration ledger mismatch (local `20260701021800` vs remote `20260630232657`) | audit gap #8 | **R1** | ✅ Closed by R1 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-02 | `decrement_stock_on_confirm` owed to source + live | #4 | **R2** | ✅ Closed by R2 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-03 | pg_cron Cairo/UTC offset (3 daily jobs) | T14 finding | **R3** | ✅ Closed by R3 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-04 | ADR numbering collision (OD-7 "ADR-002" → **ADR-011**) | OD-7 carry | **R4** | ✅ Closed by R4 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-05 | MVP_SCOPE §6 vs §8 citation inconsistency | audit gap #12 | **R4** | ✅ Closed by R4 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-06 | Stale current-counts in docs (policies 34→39, indexes 34→41) | T14 corrections | **R4** | ✅ Closed by R4 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-07 | Empty `docs/05-features/`, `docs/11-decisions/` | audit gaps #9–10 | **R4** | ✅ Closed by R4 (2026-07-16) **B7 (2026-09-23):** closed. |
| REG-08 | rls-smoke opt-in on migration PRs | standing rec | **R5** | ✅ Closed by R5 (2026-07-16) — conditionally-required on migration-touching PRs **B7 (2026-09-23):** closed. |
| REG-09 | `orders` permissive ownership INSERT policy missing | #1 | Phase 07 checkout task | ✅ **Closed by Phase 07 / T01 (2026-07-23, Opus Max).** Migration `20260723074953_order_rls_and_conversion_link` added permissive `orders_insert` (`WITH CHECK buyer_id = auth.uid()`), ERD §3 row-54 verbatim; it COMBINES with the pre-existing RESTRICTIVE `orders_phone_gate` (OD-4 verified-phone gate bites here). Integration-proven BOTH halves (`order.rls.test.ts`): phone-verified buyer INSERTs own order (PASS), phone-NULL user DENIED (gate finally bites), cross-user `buyer_id` DENIED. Flipped the RLS-smoke **A4b FINDING→PASS** (+ retired the stale F2). Backfilled into `BETK_DATABASE_SCHEMA.sql`; advisor unchanged by this policy (references only `auth.uid()`). **B7 (2026-09-23):** closed. |
| REG-10 | `seller_profiles` permissive ownership INSERT policy missing | #1 | Phase 04 become-seller task | ✅ Closed by Phase 04 / T01 (2026-07-19, Opus 4.8) — migration `20260719133011_seller_ownership_insert_rls` added permissive `sp_insert` (`WITH CHECK (id = auth.uid())`), ERD §3-verbatim. Integration-proven both directions: phone-verified user INSERTs own row (sp_insert + RESTRICTIVE `seller_profiles_phone_gate` COMBINE), phone-NULL user DENIED (gate still bites), cross-user id DENIED. Backfilled into `BETK_DATABASE_SCHEMA.sql`. **B7 (2026-09-23):** closed. |
| REG-11 | Live Google OAuth consent E2E (real PKCE round-trip) | #13 | Pre-launch Playwright gate | Open (owned) **B7 (2026-09-23):** open. v2 owner: Pre-launch pin (Playwright OAuth). Blocks launch, not a build phase. |
| REG-12 | Handset SMS delivery + sender `3MS EGY` on device | SMS section | **HARD pre-launch gate** | ✅ **CLOSED (2026-07-22, human-supplied evidence).** Real OTP delivered to a physical handset; message rendered with sender "3MS EGY" (TorvoSMS log: 2 sends Jun 30, status `sent`, 58 chars/1 seg Arabic route; human confirmed on-device display). **The hard pre-launch SMS gate is retired.** **B7 (2026-09-23):** closed. |
| REG-13 | NTRA/operator sender-ID registration | memory item | External clock (was: start NOW, don't wait for pre-launch) | ✅ **CLOSED-WITH-EVIDENCE (2026-07-22, human-supplied evidence).** Sender ID "3MS EGY" active at the aggregator (created Jun 29 2026) AND operator-rendered verbatim on device — registration provably propagated. **Standing note: if the sender ID ever changes or shows an expiry, this REOPENS** — re-verify on any provider change. **B7 (2026-09-23):** closed. |
| REG-14 | `StoreDeliveryOptions.modes` mirrors store-side enum | #5 | Verify before Phase 04/07 consume | ✅ Closed-with-evidence by Phase 04 / T01 (2026-07-19, Opus 4.8) — **MATCH**. Live `betk.delivery_preference = {delivery, pickup, remote}` (schema source L54 agrees); `src/types/jsonb.ts` `StoreDeliveryOptions.modes = "delivery"|"pickup"|"remote"` — identical, no drift, no type change. The `self_deliver/bosta/pickup/remote` 4-value wording (Phase-01 T12 note → `BETK_UI_SPEC.md §3` L336/L384) is a **product/UI-doc description** (self-deliver + Bosta are both "delivery" fulfillment), NOT a DB enum — recorded as a doc-vs-schema divergence for the T08 docs sync; schema/type untouched. **Consumer correction:** T07's "4 mode toggles" line is corrected to **3 toggles** ({delivery, pickup, remote}) — live-verified. **T08 (2026-07-20): fully closed with BOTH evidence legs — type-level MATCH (T01) + runtime EXACT-shape JSONB round-trip (T07 `store.settings` integration: 3-mode set, no extra/missing keys). The doc-vs-schema divergence is now corrected in `BETK_UI_SPEC.md` §3 (Seller Onboarding step 4 + Delivery Settings) and the pack's T07 line; the "distinguishing courier" 4th-mode product option was declined-by-default (would need an OD amendment).** **B7 (2026-09-23):** closed. |
| REG-15 | `title_en` required-in-form decision (`title` required AR+EN at the Zod/listing-form layer; `title_en` stays nullable in DB) | OD-7 note | **Phase 05 listing form task** (corrected at Phase 04 / T08 — the create/edit-listing form is Phase 05, NOT Phase 04; the Phase-04 seller build has no listing-title field) | ✅ **FULLY CLOSED (schema half T02 + form half T04), T06-verified (2026-07-22, Opus).** **SCHEMA HALF (T02):** `createListingSchema`/`updateListingSchema` in `src/validations/listings.ts` require BOTH `titleAr` AND `titleEn` (`z.string().trim().min(1).max(80)` each); unit-proven (`listingRules.unit.test.ts`). **FORM HALF (T04):** `ListingForm.tsx` renders BOTH titles as REQUIRED inputs and blocks save when either is empty (client `validate()` L185-186). `listings.title_en` stays NULLABLE in DB — NO schema change; publish gate R-L03 keys on `title_ar` only. **CLEANUP NOTE (FLAG-2, T06 — no rework, recorded only):** the form's client `validate()` HAND-MIRRORS the T02 Zod shape, it does NOT derive it via `.pick(...)`/`.extend(...)` — there is no shared client-schema-from-server-schema helper in this repo to pick from (same manual-mirror precedent as `StoreProfileForm`/`OnboardingWizard`). The `src/validations/listings.ts` header comment claiming the form "mirrors this schema via `.pick(...)`" is therefore inaccurate — a doc-comment fix (and, optionally, a genuine shared client-schema helper) is a low-priority cleanup, not a gate blocker. **B7 (2026-09-23):** closed. |
| REG-16 | BL-01 locale-preserving Server-Action redirects | OD-7 carry | Optional cleanup, any Sonnet window | Open (optional) **B7 (2026-09-23):** open. v2 owner: Phase 09, optional, not an exit blocker. |
| REG-17 | BL-02 `errorAr` → `errorMessage` rename | OD-7 carry | Optional cleanup (pairs with REG-16) | Open (optional) **B7 (2026-09-23):** open. v2 owner: Phase 09, optional, with REG-16. |
| REG-18 | StatusBadge `flag`-domain labels (partial map) | OD-7 carry | **Claude Design decision** before any admin moderation badge renders | **DECIDED — keep-partial (applied at CD-DELTA-4 LAND 2026-07-21, brief §5.5; revisit at the admin phase when a moderation-flag badge first renders).** **B7 (2026-09-23):** open. v2 owner: Phase 18 (revisit when a moderation-flag badge first renders; keep-partial stays decided). |
| REG-19 | `betk.users` writes stay service-role (no self-UPDATE policy until grant re-scoped) | #10 | Standing pattern — revisit only deliberately | Standing **B7 (2026-09-23):** open. v2 owner: standing pin (service-role writes on betk.users). |
| REG-20 | `NODE_ENV`-conditional env requiredness | deploy TODO | Named pin: first production deploy. “Phase 14” in the old owner was a v1 heading (§H), not v2 Phase 14 (courier). | Open (owned) **B7 (2026-09-23):** open. v2 owner: named pin: first production deploy (not v2 Phase 14). |
| REG-21 | GitHub Actions version bumps (Node 20 deprecations) | CI housekeeping | Any convenient window | Open (housekeeping) **B7 (2026-09-23):** open. v2 owner: Phase 08, optional housekeeping, not an exit blocker. |
| REG-22 | Live OAuth + config.toml vs dashboard drift is ops-owned | staging-config note | Ops awareness item | Standing. **Staging-config note folded at Phase 05 / T06 (2026-07-22):** GoTrue `auth.admin.updateUserById(..., { phone })` 500s on this staging project (likely an auth-provider/SMS config gap) — surfaced by the `scripts/dev/set-test-role.ts` dev harness, which WARNS + CONTINUES on that specific failure. Impact is nil for the app: the `betk.users.phone_number` mirror write (the read-path the app + verified-phone gate actually consume) succeeds; only GoTrue's own `auth.users.phone` field is left unset in that dev flow. Ops-awareness only, no code/gate impact. **B7 (2026-09-23):** open. v2 owner: standing pin (ops: dashboard vs config.toml). |
| REG-23 | Design-repo remote + pinned SHA still unresolved for the brief's living-reference pointer (`<DESIGN-REPO-URL>`/`<SHA>` placeholder) | BRIEF-RESOLVE note (2026-07-16) | Design decision — asked, not invented | Open (owned) **B7 (2026-09-23):** open. v2 owner: Stage D pin (design-repo URL and SHA). D-1-FIX (2026-09-25): human decision h — CD-DELTA-6 handoff stays self-contained; placeholder is not a source. Status stays OPEN. |
| REG-24 | Direct `supabase` CLI binary aborts on `config.toml` hook-secret validation (missing `SEND_SMS_HOOK_SECRET`) — MCP `apply_migration`/`execute_sql` is the confirmed working path for DB writes/reads in this env | R2/R3 tooling note | Ops/tooling awareness | Standing **B7 (2026-09-23):** open. v2 owner: standing pin (MCP apply path; local CLI hook-secret abort). |
| REG-25 | `listings_public` RLS (`status='active'`) hid a genuinely `sold_out`-status listing that FR-PUB-4/R-N06 require to STAY publicly visible with a restock CTA (the live `decrement_stock_on_confirm` trigger flips the enum at `stock_qty=0`) | T05 FINDING 2 | REG-25-FIX (dedicated RLS review/policy-fix task) | ✅ Closed by REG-25-FIX (2026-07-19, Opus 4.8) — migration `20260718230302_listings_public_sold_out_visibility` amended `listings_public` + child `listing_images_public`/`listing_tags_public` to `status IN ('active','sold_out')` (verbatim-consistent, so a sold_out DETAIL page keeps its gallery+tags); browse grids stay `active`-only via the query layer (UI_SPEC L73/85/97/121 — detail-only, not invented); draft/paused/removed/soft-deleted still hidden; both-direction integration tests green; schema backfilled (incl. the never-backfilled T01-FIX child block) **B7 (2026-09-23):** closed. |
| REG-26 | `view_count` increment mechanism (FR-PUB-4) is UNPINNED — no trigger/function touches `listings.view_count` and anon has no UPDATE path, so the listing-detail page ships WITHOUT incrementing it (CONFIRMED T05, not improvised — no service-role reach-around) | T05 (view_count confirmation) | Owning-phase decision — a dedicated increment mechanism (RPC / seller-analytics path) | Open (owned). **RESTATED at Phase 05 / T06 (2026-07-22):** the Listings-Management "views" column renders the raw static `view_count` as-is (T03) — still no increment mechanism added this phase; stays OPEN for a later analytics/seller-facing phase to spec. **B7 (2026-09-23):** open. v2 owner: Phase 19. |
| REG-27 | i18n message-catalog parity guard script is MISSING — `messages/{ar,en}.json` key parity is verified ad-hoc per task (T03/T04 ran one-off recursive key-diffs) but there is no committed guard beside the other 3 (`check-service-import`/`check-zod-coverage`/`check-no-hardcoded-arabic`) | T03/T04 i18n parity checks | Tooling task — add a 4th guard script + wire into CI | ✅ Closed by PRE-GATE-FIX (2026-07-19, Opus 4.8) — new `scripts/check-i18n-parity.mjs` (pure-Node leaf-key deep-walk of ar.json vs en.json; reports totals + orphans each direction; exit 1 on any orphan OR leaf-count mismatch). Wired as **Guard D** in the CI `guards` job AND a new local `pnpm guards` aggregate (all 4). Current parity **305/305 leaf keys, 0 orphans**. **The guard suite is now 4** (`check-service-import`/`check-zod-coverage`/`check-no-hardcoded-arabic`/`check-i18n-parity`). **B7 (2026-09-23):** closed. |
| REG-28 | 404 chrome drift (NF-01) — the top-level `[...rest]` catch-all hard-404 renders Next's bare `__next_error__` shell (no `<html lang/dir>`, no chrome, generic title), contradicting BL-01-FIX's 2026-07-02 runtime-verified locale-aware/chrome-preserving 404 claim (likely an intervening-track side effect; `[...rest]` is a sibling of `(public)`, so unrelated to T04's own `loading.tsx` fix) | T04 SECOND FINDING | Dedicated 404-chrome pass | ✅ Closed by PRE-GATE-FIX (2026-07-19, Opus 4.8). **ROOT CAUSE:** the app's only `<html>`/`<body>` live in the dynamic `[locale]/layout.tsx` (there is no `app/layout.tsx`). On next@15.5 a `notFound()` that bubbles out of that dynamic-root segment renders on the special `/_not-found` route which sits ABOVE `[locale]`, so the layout's `<html lang/dir>` never re-applies → the bare `__next_error__` shell (vercel/next.js#64870, "still the case in 15.5"). This was a standing framework limitation, NOT a change introduced by a specific commit — BL-01-FIX's runtime claim predates the drift becoming visible. **FIX:** enabled `experimental.globalNotFound` + added `src/app/global-not-found.tsx` (full localized HTML document — reads negotiated locale from the `NEXT_LOCALE` cookie the middleware sets, `<html lang/dir>` + NextIntlClientProvider + kit `EmptyState`). Removed the `[locale]/[...rest]` catch-all so unknown paths are genuinely unmatched → routed to `global-not-found` (verified: `/xx`,`/does-not-exist`,`/en/does-not-exist`,`/xx/admin` → hard 404 + `noindex` + correct localized full-doc shell). `[locale]/not-found.tsx` kept (now bilingual via next-intl, was hardcoded-Arabic) for explicit `notFound()` from matched detail pages. **RESIDUAL (accepted):** explicit `notFound()` from a MATCHED route (listing/category/store missing record) still renders localized EmptyState CONTENT inside Next's default shell (no `lang/dir` wrapper) — the same #64870 limitation, unfixable without abandoning the unprefixed-default-locale architecture; status stays hard 404 + `noindex`. **B7 (2026-09-23):** closed. |
| REG-29 | `betk.store_follows` was RLS-ENABLED with ZERO policies (ERD §3 line 45 self-scope SPECCED but the `CREATE POLICY` statements were omitted from the Phase-01 SQL contract → default-deny for everyone incl. the owning buyer) — 2nd instance of the open-issue-#14 / T01-FIX class | Discovered by T06 (`toggleFollow` + storefront FollowButton state read) | Restored additively by T06 migration `20260718153021_store_follows_self_scope_rls.sql` (authorized flagged expansion of "no new policies", #14 precedent) | ✅ Closed by T06 (2026-07-18) — sf_select_self/sf_insert_self/sf_delete_self, no UPDATE policy per ERD; backfilled into `BETK_DATABASE_SCHEMA.sql`; T07 PR will now fire the R5 rls-smoke job (expected) **B7 (2026-09-23):** closed. |
| REG-30 | `experimental.globalNotFound` (next@15.5) is an EXPERIMENTAL Next flag — enabled in `next.config` by PRE-GATE-FIX to route unmatched paths to `src/app/global-not-found.tsx` (the localized full-doc 404 that fixed REG-28). Experimental API surface may change/be renamed/graduate/regress across Next minor+major upgrades; the fix also depends on the `NEXT_LOCALE` cookie contract + the no-`app/layout.tsx` dynamic-root-only `<html>` architecture (vercel/next.js#64870) | REG-28 fix side-effect (T07 mint 2026-07-19) | **Standing — re-verify on every Next upgrade** (confirm the flag still exists/behaves, unknown paths still hard-404 with localized full-doc shell + `noindex`; watch #64870 for a stable non-experimental path to retire the flag) | Open (owned) **B7 (2026-09-23):** open. v2 owner: named pin: re-verify on every Next upgrade. |
| REG-31 | `betk.stores` had NO INSERT policy (`stores_public` SELECT + `stores_manage` UPDATE only) despite ERD §3 speccing INSERT = own — 3rd instance of the open-issue-#14 / REG-29 class (ERD-specced policy omitted from the Phase-01 SQL contract; the Phase-03 T07 ERD audit listed `stores` as present-&-correct and missed the uncovered INSERT command) | Minted by Phase 04 / T01 (2026-07-19, Opus 4.8) | ✅ Closed by Phase 04 / T01 — same migration `20260719133011_seller_ownership_insert_rls` added permissive `stores_insert` (`WITH CHECK (seller_id = auth.uid())`), ERD-verbatim, no phone gate (ERD gates only orders/seller_profiles/payouts). Integration-proven: owner INSERTs own store, cross-user seller_id DENIED. #14 precedent = authorization. Backfilled into `BETK_DATABASE_SCHEMA.sql`. **B7 (2026-09-23):** closed. |
| REG-32 | `betk`-schema RPC (`Functions`) signatures are HAND-MAINTAINED in `src/lib/supabase/types.ts` — the CI types-drift regeneration (`supabase gen types --schema betk,betk_analytics`) does not reliably emit them; first instance `submit_seller_application` (T03), alongside pre-existing `is_admin`/`my_store_id`. The T03 action's typed `.schema("betk").rpc("submit_seller_application", …)` call REQUIRES the signature (typecheck depends on it — removing it breaks the build) | Minted by Phase 04 / T04 (2026-07-20, Sonnet) | ✅ **CLOSED by T08-FIX (2026-07-20, Opus 4.8, PR #44, CI run `29742475884`).** The AUTHORITATIVE CI `types-drift` job (real `--project-id` + `SUPABASE_ACCESS_TOKEN` secrets) PROVES `supabase gen types --schema betk,betk_analytics` emits `betk.Functions` (`is_admin`/`my_store_id`/`resubmit_seller_application`/`submit_seller_application`) NATIVELY — the failure was hand-edit-vs-generated (alphabetical Functions/Args ordering, no hand-comment, every Arg non-null — pg function-parameter metadata carries no nullability, unlike table columns). **FIX:** pulled the failed job's log (job `88352781673`) via the GitHub API and applied its printed diff to `types.ts` VERBATIM (confirmed byte-identical: local `git diff` reproduces the exact same blob hashes `aa01981..8dedd95` as the CI diff) — the hand-comment + hand-ordering removed, `types.ts` is now 100% generated, zero hand-maintenance. **Non-null Args broke 2 call sites that pass optional fields as `null`** (`submitSellerApplication.ts` prod action + the direct-rpc phone-gate test in `seller.submit.test.ts`) — fixed with ONE documented cast each at the `.rpc()` boundary (a locally-typed nullable `*RpcArgs`/`rpcArgs` object built first, cast to the generated `Args` type only at the call, comment cites the migration + REG-32 + "do not fix by editing types.ts"). `resubmitSellerApplication.ts`'s call site needed NO change (its 2 args are both required strings already). **Full RE-RUN green:** typecheck 0 errors · lint clean (pre-existing warnings only) · guards 4/4 · unit 82/82 · build 33 routes both locales · staging integration spot-check 14/14 (`seller.submit.test.ts` 7/7 incl. happy-path submit, `seller.resubmit.test.ts` 7/7 incl. rejected→resubmit) — zero residue (afterAll cleanup). **Local `pnpm db:types` still requires `SUPABASE_ACCESS_TOKEN` (absent locally) — regeneration is CI-authoritative going forward; for any future betk-schema rpc: add the migration, let CI's `types-drift` job produce the signature, apply its printed diff verbatim (never hand-add).** | Closed — see PR #44 body for the human-facing note **B7 (2026-09-23):** closed. |
| REG-33 | R-S03 slug change-once is enforced by the APP LAYER ONLY — live `betk.stores` has NO trigger/CHECK/constraint governing the change (only `chk_store_slug_fmt` / `uq_stores_slug` / `uq_stores_seller`, live-verified via pg_constraint + pg_trigger). `updateStoreProfile` (T06) is the sole guard: it rejects a changed slug when `slug_changed_at IS NOT NULL` and additionally scopes the slug UPDATE with `slug_changed_at IS NULL` (race-safe). A malicious direct client bypassing the action could, in principle, UPDATE its own store's slug twice (stores_manage RLS permits own-row UPDATE and does not inspect slug_changed_at) | Minted by Phase 04 / T06 (2026-07-20, Sonnet) | **Candidate hardening** — a `BEFORE UPDATE` trigger on `betk.stores` raising when `NEW.slug <> OLD.slug AND OLD.slug_changed_at IS NOT NULL` would make R-S03 DB-authoritative. No DB object added in T06 (task-scoped app-layer). **WHY it matters (T08): today `updateStoreProfile` is the ONLY writer of `stores.slug`, so the app guard is sufficient in practice — but future multi-writer paths break that assumption. Specifically a Phase-14 admin edit/backfill (or any second server-side writer that touches `stores`) would bypass an app-only guard and could change a slug twice, since `stores_manage` RLS permits own-row UPDATE and does not inspect `slug_changed_at`. The BEFORE UPDATE trigger makes R-S03 DB-authoritative regardless of writer.** Owner = the FIRST second-writer task (Phase-14 admin store edits) OR an opportunistic DB-hardening batch, whichever lands first. | Open (owned) **B7 (2026-09-23):** open. v2 owner: named pin: candidate slug trigger. Not in the Phase 08 migration. |
| REG-34 | `betk.listing_images` / `listing_tags` owner-write policies (INSERT/UPDATE/DELETE = own store) SPECCED in ERD §3 row 48 but omitted from the Phase-01 SQL contract (children carried ONLY their public SELECT policy → owner write default-denied) — 4th instance of the open-issue-#14 / REG-29 / REG-31 class | Pre-reserved by `PHASE_05_LISTINGS.md`; minted by Phase 05 / T01 (2026-07-21, Opus) | ✅ **Closed by Phase 05 / T01 (2026-07-21, Opus).** Additive migration `20260721111355_listing_children_owner_write_rls` added `listing_images_seller` + `listing_tags_seller`, each `FOR ALL USING (EXISTS(betk.listings l WHERE l.id=<child>.listing_id AND (l.store_id=betk.my_store_id() OR betk.is_admin())))` — mirroring the parent `listings_seller` scoped via the owning listing (the USING doubles as the INSERT/UPDATE WITH CHECK per the Postgres FOR ALL pattern). The pre-existing public SELECT policies (`listing_images_public`/`listing_tags_public`) are UNTOUCHED and OR-combine, reconstructing ERD row 48's "follows listing" SELECT (own-store any-status via the new policy OR public active/sold_out via the public policy) — which also unblocks T02's own-draft-children reads. References only the SECURITY DEFINER helpers `my_store_id()`/`is_admin()` (no bare `auth.*`) → advisor sweep byte-identical to baseline, 0 new findings. Ledger 25→26 (remote↔local 1:1); backfilled into `BETK_DATABASE_SCHEMA.sql`. `restock_alerts` verified RLS-enabled zero-policy + NOT touched (Phase-12/notifications-owned). Storage verified zero-change (media own-prefix reuse; no DELETE policy by design). Integration `tests/integration/listing.childrenRls.test.ts` 7/7 on staging (owner INSERT/UPDATE/DELETE + own-draft SELECT; cross-seller + anon writes denied; public SELECT unregressed; draft children hidden), zero residue. **B7 (2026-09-23):** closed. |
| REG-35 | Production Vercel domain + preview-domain pattern missing from the Supabase Auth redirect allow-list (the Site URL fix already human-applied covers the primary redirect; the allow-list is the broader set) | DIAG-PERF-01 D3/DIAG-14 (2026-07-20, Opus 4.8) | Pre-launch (dashboard-config, beside REG-11) | ✅ **CLOSED (2026-07-21, human-confirmed, HOUSEKEEP-P5).** Production redirect allow-list configured in the Supabase dashboard, including the preview wildcard `https://betk-*-jiovannys-projects-0219772b.vercel.app/auth/callback`. Dashboard-config only, no code/migration change. **B7 (2026-09-23):** closed. |
| REG-36 | `auth_rls_initplan` — ~24 `betk` policies + 5 `storage.objects` policies re-evaluate a bare `auth.<fn>()`/`auth.uid()` per row instead of `(select auth.<fn>())` (incl. the Phase-04 `sp_insert`/`stores_insert`) | DIAG-PERF-01 C2/DIAG-06 (2026-07-20, Opus 4.8) | Dedicated reviewed RLS/policy task (widespread — every betk write policy) | Open (owned) **B7 (2026-09-23):** open. v2 owner: named pin: dedicated initplan pass. Not folded into Phase 08. |
| REG-37 | CACHING_STRATEGY standing item: `rating_aggregates` inherits the homepage's 60s ISR TTL (no distinct 5-min TTL) + ~37 unindexed FKs on hot read paths (harmless on near-empty staging; covering indexes wanted before scale) | DIAG-PERF-01 A4+C3/DIAG-04+05 (2026-07-20, Opus 4.8) | Standing — CACHING_STRATEGY doc + an indexing pass before scale | **Caching-doc leg ✅ CLOSED-WITH-EVIDENCE by PERF-02 (2026-07-21, Opus, `feature/perf-01`)** — `docs/02-architecture/CACHING_STRATEGY.md` authored (per-route posture table for every public+authed+API route with WHY, anon-vs-cookie client rule, the `rating_aggregates`-inherits-60s decision, the store-ISR future candidate, the category load-more trade-off, the middleware guest fast-path contract, and §7 the next-intl on-demand-ISR locale rule = REG-40), cross-linked from `BETK_ARCHITECTURE.md §7`. `/category/[slug]` + `/listing/[id]` flipped to ISR (revalidate 60), verified via build route table + runtime cached-hit timing (EN category 567ms→35ms repeat) + hard-404-on-repeat both locales. **FK-index leg stays OPEN (owned)** — the ~37 covering indexes before scale are a DB task, untouched by PERF-02. **Noted at HOUSEKEEP-P5 (2026-07-21): pending, alongside CD-DELTA-4 (requested, in flight — see REG-38/REG-39).** **B7 (2026-09-23):** open. v2 owner: named pin: FK-index leg only, before scale. Caching leg is closed. |
| REG-38 | Locale-switch UX: `AppChrome`/`LanguageSwitcher`'s `router.replace(pathname,{locale})` is a full server round-trip (next-intl remounts the whole `[locale]` subtree) with no visible pending state, compounded by NO `(public)` route-level `loading.tsx` (forbidden by the hard-404 binding rule) — the locale toggle "feels dead" and in-page navigation "feels stuck" | DIAG-PERF-01 A1+A2/DIAG-01+02 (2026-07-20, Opus 4.8) | Two legs: (1) app-layer pending-state on the toggle itself; (2) `(public)` route-level skeleton-streaming strategy — Claude Design (UI Spec §6 / Brief §5.24) | **Leg (1) ✅ CLOSED by PERF-01 (2026-07-21, Sonnet, `feature/perf-01`)** — both locale toggles (`AppChrome` + `account/LanguageSwitcher`) wrap `router.replace` in `useTransition`; `LanguageSwitcher`'s plain `<select>` (app-layer, already unstyled) gets `disabled`+`aria-busy`+token `opacity-60`/`cursor-progress` directly; `AppChrome`'s wrapper div gets `aria-busy`+`opacity-60`+`pointer-events-none` while pending (its own click-handler also no-ops while pending) — **STOP-and-flag, CD-DELTA-4 candidate:** `AppTopbar`'s language `<button>` (frozen `components/shared`) has no `disabled`/`className` passthrough, so the button itself can't carry the pending affordance directly; code-commented in `AppChrome.tsx`. **Leg (2) stays Open (owned, Claude Design)** — no route-level `(public)` `loading.tsx`/skeleton strategy added (binding rule holds; still a holistic Design decision). PERF-01 additionally shipped a narrower, complementary mitigation on 2 specific `(public)` routes — component-level `<Suspense>` streaming (not a route `loading.tsx`) on `/category/[slug]` (`CategoryListingsSection`) and `/store/[slug]`'s Listings tab (`StoreListingsSection`), both `SkeletonGrid`-fallback, homepage-precedent — so header/chrome paints immediately while just the listings query streams in; `/search` and `/listing/[id]` are untouched (out of PERF-01's STEP 1 scope) and still have no per-route feedback, so leg (2)'s general strategy decision remains open. **Noted at HOUSEKEEP-P5 (2026-07-21): leg (2) is pending CD-DELTA-4 (Claude Design — requested, in flight).** ✅ **CLOSED by CD-DELTA-4 LAND (2026-07-21, Sonnet, `feature/phase-05-listings`)** — leg (1)'s AppTopbar API gap is filled: the shared `AppTopbar` gained the additive `langPending` prop (disabled + `aria-busy` + token `opacity-60`/`cursor-progress` ON THE BUTTON, zero visual change unset — the only sanctioned edit point, every other prop/JSX byte-preserved, diff-verified vs main); `AppChrome` now passes `langPending={isLocalePending}` and the PERF-01 wrapper workaround (the `pointer-events-none`/`opacity-60` chrome `<div>`) was REMOVED since the button fully expresses pending; `account/LanguageSwitcher` unchanged. Leg (2) landed as the token-only shared `RouteProgress` top bar (REG-38b ADOPTED — `fixed inset-x-0 top-0`, `z-60`, indeterminate `--primary` segment, renders null at rest) mounted in BOTH the public/buyer `AppChrome` and the seller `SellerChrome` shells (driven by a `useTransition` route-transition state), plus 3 additive route-transition skeletons (`CategoryPageSkeleton`/`ListingDetailSkeleton`/`StorefrontSkeleton`) on `CatalogSkeletons` (additive exports only, nothing renamed/removed). Brief §5.12/§5.13/§5.24 addenda applied. **B7 (2026-09-23):** closed. |
| REG-39 | 8 seeded category `icon_url` paths (`/icons/categories/{clothing,food,home,beauty,jewelry,arts,books,services}.svg`) all 404 in production (evidence: Vercel prod logs, 2026-07-20) — the icon assets were referenced in seed data but never produced/deployed to that path | Minted by PERF-01 (2026-07-21, Sonnet) | Real assets = **CD-DELTA-4** (Claude Design). PERF-01 ships a feature-layer stopgap only (drop the known-missing URLs at the mapping so the shared `CategoryGrid`'s Lucide default renders). | **Stopgap ✅ CLOSED by PERF-01 (2026-07-21, Sonnet, `feature/perf-01`)** — `HomeCategoryGrid.tsx` (feature layer, NOT `components/shared` — the `<img>` was already composed here) now drops any `iconUrl` under the known-missing `/icons/categories/` prefix at the mapping (`resolveIconUrl`), so `icon` resolves to `undefined` and the shared `CategoryGrid` renders its own Lucide `<Tag>` default; code-commented `REG-39, real assets via CD-DELTA-4`. Runtime-verified on the homepage: 0 actual `<img>` tags reference `/icons/categories/*` (zero browser-fetchable requests — the raw string still appears once, harmlessly, inside the RSC hydration payload's serialized props JSON, which is not a fetch target), 8/8 top-level categories render the `lucide-tag` fallback glyph instead. **Real-assets leg stays Open (owned, CD-DELTA-4).** **Noted at HOUSEKEEP-P5 (2026-07-21): CD-DELTA-4 has been requested and is in flight (not yet delivered).** ✅ **CLOSED by CD-DELTA-4 LAND (2026-07-21, Sonnet, `feature/phase-05-listings`)** — the 8 theme-neutral single-color (fixed brand teal `#1c8d7e`, sanctioned asset literal — CSS vars can't reach inside an `<img>`) 24×24 outline glyphs now ship at their exact seeded paths `public/icons/categories/{clothing,food,home,beauty,jewelry,arts,books,services}.svg` (byte-diff-verified vs the handoff templates). The `HomeCategoryGrid` `resolveIconUrl` stopgap + its REG-39 comment were REMOVED so the real `icon_url` paths render again. Runtime-verified on `next start`: the homepage HTML references all 8 `/icons/categories/*.svg`, each returns HTTP 200 (`image/svg+xml`), and there are ZERO `lucide-tag` fallbacks for the 8 top-level categories. Both legs now closed. **B7 (2026-09-23):** closed. |
| REG-40 | next-intl + on-demand ISR locale rule (STANDING, load-bearing): on an ISR route under `[locale]` whose `generateStaticParams` returns `[]` (nothing prebuilt → every path generated on demand at first hit / revalidation), next-intl's `setRequestLocale` `cache()` store is NOT reliably resolved across the independently-rendered layout / page / Suspense-child scopes for the NON-default locale — a store-based `getLocale()`/`getTranslations()` (or server `<Link>`, which calls `getLocale()` internally) then falls back to `headers()` → static generation aborts with `DYNAMIC_SERVER_USAGE` → **500 on `/en/…` while `/…` (AR) is 200** (the default locale is silently masked by next-intl's fallback). Build-time-prerendered routes (homepage, `/en/search` shell, authed shells) mask it — the failure only shows on the on-demand / revalidation path. | Discovered + fixed by PERF-02 (2026-07-21, Opus, `feature/perf-01`) during the STEP-4 runtime smoke | ✅ **Fix applied + STANDING RULE recorded** (`CACHING_STRATEGY.md §7`): thread `locale` EXPLICITLY on every ISR render path — `getTranslations({ locale, namespace })` everywhere (incl. the shared `(public)/layout.tsx`, which wraps all public pages), `locale` as a prop into Suspense-wrapped async children (`CategoryListingsSection`), and `locale={locale}` on server `<Link>` (`SubcategoryChips` + the category empty-state links). `setRequestLocale` is still called (primes the client provider) but the render path no longer depends on it. Any future ISR route under `[locale]` MUST follow this (verify: `next start`, hit `/en/<route>` first+repeat — a 500 on `/en` while `/` is 200 is this bug). | Standing (rule) — closed for category/listing; applies to all future ISR routes **B7 (2026-09-23):** open. v2 owner: standing pin (explicit locale on ISR). The category/listing bug is closed. |
| REG-41 | `inquiries` + `inquiry_messages` ERD §3-specced RLS policies absent (live-verified: `inquiries` carried ONLY `inq_buyer` SELECT; `inquiry_messages` RLS-enabled ZERO-policy → default-deny) — 5th instance of the open-issue-#14 / REG-29 / REG-31 / REG-34 class | Pre-reserved by `PHASE_06_MESSAGING.md`; minted+closed by Phase 06 / T01 (2026-07-22, Opus) | ✅ **Closed by Phase 06 / T01 (2026-07-22, Opus).** Additive migration `20260722115026_inquiry_messaging_rls`, ERD §3 rows 51-52 verbatim: **inquiries** — `inq_insert` (INSERT WITH CHECK `buyer_id = auth.uid()`, no phone gate — ERD §1.2 gates only orders/seller_profiles/payouts) + `inq_update` (UPDATE USING+CHECK `store_id = my_store_id() OR is_admin()` = the confirm→checkout transition surface); **SELECT was NOT re-added** — the pre-existing `inq_buyer` USING is already `((buyer_id=auth.uid()) OR (store_id=my_store_id()) OR is_admin())` = row 51 "buyer or store or admin" verbatim (live-quoted), so a 2nd SELECT policy would be a redundant multiple-permissive; `inq_buyer` LEFT UNTOUCHED. **inquiry_messages** — `inq_msg_select` + `inq_msg_insert` (thread parties = parent inquiry buyer OR store via EXISTS; INSERT pins `sender_id=auth.uid()`) + `inq_msg_update` (sender own rows only); NO DELETE (ERD "—"). Bare `auth.uid()` + SECURITY DEFINER helpers, default (public) roles — existing-style, REG-36 wrap NOT smuggled. Ledger 26→27 (remote↔local 1:1); backfilled into `BETK_DATABASE_SCHEMA.sql`; security advisor RESOLVES the `inquiry_messages` rls_enabled_no_policy INFO (14→13), 0 new findings. Integration `tests/integration/inquiry.rls.test.ts` **13/13** on staging (buyer create+read; owning seller reads same; unrelated seller/buyer + anon read → 0; buyer impersonation INSERT denied; both parties send + read; outsider send denied; sender-only UPDATE [other party's is_read flip → 0 rows]; no DELETE; seller status UPDATE ok, buyer + unrelated-seller status UPDATE → 0 rows), zero residue. `order_messages`/`notifications`/`restock_alerts` untouched. **B7 (2026-09-23):** closed. |
| REG-42 | Unread WRITE-path gap: the unread mechanism column is `inquiry_messages.is_read` (boolean NOT NULL default false — live-cited), but ERD §3 row 52 specs `inquiry_messages` UPDATE = **sender** only, so the RECIPIENT cannot flip `is_read` on the OTHER party's messages (exactly the rows a reader needs to mark read). The column EXISTS but the reader-driven mark-read WRITE is NOT RLS-achievable additively under ERD-verbatim (integration-proven: seller's UPDATE of the buyer's message → 0 rows). Broadening UPDATE beyond "sender" would violate ERD row 52. | Minted by Phase 06 / T01 (2026-07-22, Opus) — cite-or-flag discipline (a column exists so no STOP; the write-path tension is flagged, not improvised) | ✅ **CLOSED by Phase 06 / T02-FIX (2026-07-22, Opus): DECISION 3 REVISED = (b), grounded by an AUTHORIZED ERD §3 row-52 amendment.** The human authorized the amendment (BETK_ERD.md §3, 2026-07-22): the row's original "sender" UPDATE wording described **content-edit** rights; `is_read` is **definitionally receiver-driven** (a sender flipping read-state on their own message is a no-op). Migration `20260722124510_inquiry_read_receipt_rls` (additive + grant re-scope, ledger 27→28): **column safety = GRANT** — `REVOKE UPDATE ON betk.inquiry_messages FROM authenticated; GRANT UPDATE(is_read) … TO authenticated` (an authenticated caller may UPDATE ONLY is_read — a `body` edit is DENIED BY THE GRANT, error 42501, not zero rows; service_role/postgres/anon grants untouched — live-verified); **row safety = policy** — permissive `inq_msg_read_receipt` (`FOR UPDATE TO authenticated`, USING+CHECK = party to the parent inquiry AND `sender_id <> auth.uid()`), OR-combined with the untouched `inq_msg_update` (sender). `markInquiryRead(inquiryId)` action built (no service-role; idempotent; outsider→not_found); `unreadCount` wired into getOwnInquiries/getStoreInquiries + per-message `isRead`+thread `unreadCount` into getInquiryThread. **(a) DEFER superseded** (the only blocker was the missing cite — the amendment supplies it). **(c) DEFINER rpc STILL rejected** (advisor 0029, ADR-012's reject reason). ADR-015 records the decision. Advisor sweep = exact post-T01 baseline (13 rls-no-policy INFO), 0 new. Integration `inquiry.readReceipt.test.ts` 10/10 staging zero-residue; T01's 13 assertions unregressed (the one that encoded the now-closed gap revised to the amended behavior). **B7 (2026-09-23):** closed. |
| REG-43 | `inquiries.last_message_at` maintenance gap: column is `NOT NULL DEFAULT now()` with **no trigger** maintaining it (live-verified — all inquiries/inquiry_messages triggers are internal FK `RI_ConstraintTrigger`s), so the plan is an app-layer bump in T02. BUT ERD §3 row 51 specs `inquiries` UPDATE = **store/admin** only (`inq_update`), so a BUYER-sent message CANNOT bump `last_message_at` via RLS (buyers have no inquiries UPDATE); seller-sent bumps DO work (store/admin). | Minted by Phase 06 / T01 (2026-07-22, Opus) | ✅ **CLOSED-BY-DECISION — DECISION 4a DERIVE-AT-READ (Phase 06 / T02; re-affirmed at the T05 exit gate 2026-07-22, Opus; zero migration).** ⚠️ **STANDING WARNING (load-bearing for Phase 07/12):** `inquiries.last_message_at` is **UNMAINTAINED** — NO trigger, NO app write; it stays at its INSERT default (`= created_at`). Any future phase that needs "latest activity" MUST derive it from `max(inquiry_messages.sent_at)` (see `latestActivityAt` in `messagingRules.ts`); NEVER read `last_message_at` as truth. This warning is mirrored into the Phase-07 entry checklist + the ERD §3 order-set map row so Phase 07/12 see it. `last_message_at` is NEVER written (stays at its INSERT default = `created_at`, documented stale in the query + action headers); inbox ordering is DERIVED in the queries from `max(inquiry.created_at, max inquiry_messages.sent_at)` (`latestActivityAt`, `messagingRules.ts`), which re-sorts a thread on a BUYER's newest message for BOTH parties — proven by integration ("a BUYER's newest message re-sorts the thread to the top") + `last_message_at` asserted UNCHANGED after messages. WHY (b) a SECURITY DEFINER INSERT trigger was rejected: it is advisor-clean (trigger-only, EXECUTE revoked PUBLIC) but adds a migration + permanent DB object for a pure read-ordering concern that the query layer handles — ADR-013 ethos (do not add DB machinery for a need app-layer covers). Do NOT broaden the buyer's inquiries UPDATE (would violate ERD row 51). **B7 (2026-09-23):** closed. |
| REG-44 | Seller cannot read a BUYER's display name via RLS: ERD §3 row 39 specs `buyer_profiles` SELECT = "self or admin (**public name/gov for discovery**)" but the live `bp_self` policy is `id = auth.uid() OR is_admin()` (self/admin only — the public-name/gov branch the ERD text specs is ABSENT), so a seller reading their store inbox cannot surface the buyer's name. Same "specced-but-absent" #14 / REG-29/31/34/41 class, discovered at Phase 06 / T02 while building `getStoreInquiries`. | Minted by Phase 06 / T02 (2026-07-22, Opus) — cite-or-flag (flagged, NOT worked around with service-role) | ✅ **WON'T-FIX (2026-09-22, B3-FIX) — superseded.** N28 forbids a seller read of buyer name, phone, address, or city. REG-83 attaches a review to one seller order, so a public name or governorate on that review would identify the buyer of that order. The v1 row-39 public name/governorate SELECT branch is struck (`BETK_ERD.md` §H). Historical disposition, not operative: **REFRAMED 2026-07-23 (CORRECTION-02): REG-44 STANDS ALONE.** The prior "ONE POSTURE / bilateral pre-transaction identity opacity" coupling with REG-45 is **RETIRED** (REG-45 is now closed-as-not-a-defect — there is no off-platform contact affordance to serve, so there was never a symmetric "buyer can't read the seller's phone" defect; all buyer↔seller communication is in-app only). REG-44 resolves on the narrower, real ground that **the seller needs the buyer's NAME + delivery ADDRESS to FULFIL** — fulfilment (the courier/shipping label) necessarily exposes buyer name+phone+address to the seller / self-delivering seller at order time (Phase 07; REG-14 self-delivery). **No contact-exchange product decision is owed** (that was an artefact of the retired coupling); the only remaining question is the additive `buyer_profiles` name-read policy for the store inbox (ERD row 39's "name/gov for discovery" branch, still ABSENT). T02 surfaces `buyerId` only (no name); **T04 (2026-07-22, Sonnet) renders the neutral keyed label as planned** (`t("buyerLabel")` on `/seller/inbox` + `/seller/inbox/[id]`, no invented pseudonymous handle), runtime-proven in the DOM. NOT fixed here — migration for this is NOT authorized by the T02 decision set and it's a broader concern (also affects review author names etc.), so it belongs to a dedicated additive-policy task (add a scoped public name/gov SELECT branch to `bp_self` per ERD row 39, or a narrow buyer-name read for thread. **B7 (2026-09-23):** won't-fix. The historical sentence was already truncated before B7; the missing tail is not invented. |
| REG-45 | Buyer-side WhatsApp deep-link to the seller (UI_SPEC-pinned affordance on the inquiry thread) is RLS-unreachable: `stores`/`users` do not expose the seller's phone number to a buyer thread party (`users_self`-class SELECT policies are self/admin only; `stores_public` doesn't carry a phone column) — a buyer cannot resolve the seller's WhatsApp number without a service-role reach-around. | Minted by Phase 06 / T03 (2026-07-22, Sonnet) — discovered composing `/inbox/[id]`, flagged not improvised | ✅ **CLOSED AS NOT-A-DEFECT (2026-07-23, CORRECTION-02).** Misread: the UI_SPEC inbox "WhatsApp deep-link" line was read as buyer↔seller contact, but **NEITHER inbox screen's data requirements provision a counterparty phone column** — so RLS was never the obstacle, and there is nothing to serve because **all buyer↔seller communication is in-app only** (no off-platform contact affordance, ever — see the standing "Communication posture" fact + `docs/PRECEDENTS.md`). T03 (buyer) and T04 (seller) already OMITTED the button, so **ZERO code change is owed**; runtime-proven (no `whatsapp`/`wa.me` in either rendered DOM). The stale UI_SPEC lines are corrected in this cascade (buyer + seller inbox WhatsApp deep-links deleted; Listing-Detail share made channel-agnostic; a channel-agnostic storefront share ADDED as a human-authorized addition — REG-51). The prior "one posture / bilateral pre-transaction identity opacity" coupling with REG-44 is **RETIRED** — REG-44 now stands ALONE (seller needs the buyer's name+address to fulfil; see REG-44). **B7 (2026-09-23):** closed. |
| REG-46 | `notFound()` inside `/inbox/[id]` (and, latently, the pre-existing `(buyer)/account` `notFound()`) rendered the correct 404 CONTENT but shipped **HTTP status 200** — root cause: `src/app/[locale]/(buyer)/loading.tsx` (an OD-7-era shared Suspense boundary for the whole `(buyer)` route group) wraps every buyer route including `/inbox/[id]`; Next.js locks in the response status at the FIRST flush once any ancestor Suspense boundary can show a fallback, so by the time the awaited `getInquiryThread` resolves to `null` and throws `notFound()`, the 200 header is already committed — the exact BL-01-FIX / Phase-04-T04 / Phase-05-T04 class of bug, this time at the GROUP level rather than a single segment. | Discovered by Phase 06 / T03 runtime smoke (2026-07-22, Sonnet) — a forged-cookie HTTP check caught the status-code drift that a content-only assertion would have missed | ✅ **CLOSED (fixed) by Phase 06 / T03 (2026-07-22, Sonnet); whole-app re-sweep re-confirmed clean at the T05 exit gate (2026-07-22, Opus). Same precedent as BL-01-FIX/T04 ("fixed by DELETING the offending `loading.tsx`, never by adding one").** Deleted `src/app/[locale]/(buyer)/loading.tsx` — `(buyer)/layout.tsx` is purely structural (mounts `AppChrome`, no auth logic per its own header comment) so removing the shared fallback has no auth/UX-gate impact, only trades an instant Suspense skeleton for the browser's native pending state on buyer-route navigations (the same trade-off already accepted for every `(public)` detail page). Re-verified: `/inbox/[id]` now returns a genuine HTTP 404 for outsider/malformed/unknown ids (forged-cookie smoke, status code asserted, not content shape). **NOT independently re-verified for `/account`'s notFound() case** (same boundary, same fix, but out of T03's page scope) — flagged for whoever next touches `(buyer)/account`, `/wishlist`, `/orders`, `/notifications`, `/checkout`, or `/disputes` to confirm no regression expectation on the removed loading state. **B7 (2026-09-23):** closed. |
| REG-47 | Guard E — no `loading.tsx` at/above any route segment whose page can reach `notFound()` (the BL-01-FIX / T04-Phase-03 / REG-46 class: a group/segment Suspense fallback commits HTTP 200 at first flush BEFORE an awaited `null`→`notFound()` can set 404) | Minted by Phase 06 / T05 exit gate (2026-07-22, Opus) — **RECORD-ONLY** | **Open (owned) — housekeeping, NOT built here.** Three runtime-only catches (BL-01-FIX, T04-Phase-03, REG-46) is enough evidence the discipline needs a committed CI guard — same reasoning that promoted REG-27's ad-hoc i18n-parity checks to a guard. Scope when built: a `scripts/` check that fails when a `loading.tsx` sits at or above any segment whose `page.tsx` can (transitively) call `notFound()`. Schedule into any convenient housekeeping window; **do NOT build in this exit gate** (ZERO feature-code). **B7 (2026-09-23):** open. v2 owner: Phase 08 (Guard E). |
| REG-48 | Order-set children RLS absent (ERD §3 rows 53-59 specced, live-verified ZERO-policy: `order_items`, `order_status_history`, `order_messages`, `shipments`, `shipment_tracking_events`) — 6th instance of the open-issue-#14 / REG-29/31/34/41 class | Pre-reserved by `PHASE_07_ORDERS.md`; minted+closed by Phase 07 / T01 (2026-07-23, Opus Max) | ✅ **Closed by Phase 07 / T01 (2026-07-23, Opus Max).** Same migration `20260723074953`. Parent-scoped via the owning `orders` row (buyer own OR `my_store_id()` OR `is_admin()`): `order_items` (SELECT parties + INSERT buyer-of-parent = "system(checkout)"), `order_status_history` (SELECT parties + INSERT any-party; UPDATE/DELETE stay blocked by the live append-only RULES `no_update/no_delete_order_history` → no policy added), `order_messages` (SELECT parties + INSERT pins `sender_id=auth.uid()`; read-state NOT pinned — row 53 not REG-42-amended, `is_read` unused). **SHIPMENTS DECISION:** `shipments`/`shipment_tracking_events` **READ** policies landed now (FR-BUY-9 `/orders/[id]` tracking); store/courier **WRITE** deferred to Phase 08 (BETK_PHASES L52-54). Advisor `rls_enabled_no_policy` **13→8 INFO**, all WARN byte-identical. Integration `order.rls.test.ts` **15/15 + 1 opt-in skipped** on staging (REG-09 both halves, children both-direction reads, item INSERT scoping, order_messages pinned-sender + isolation, shipments READ + WRITE-denied, status-history outsider-INSERT denied, tension proof), zero residue. Backfilled into `BETK_DATABASE_SCHEMA.sql`. **B7 (2026-09-23):** closed. |
| REG-49 | `orders` UPDATE policy + `payments` INSERT/UPDATE policies ABSENT (T01 landed only the INSERT/READ foundation) — owed by T02's write layer | Minted by Phase 07 / T01 (2026-07-23, Opus Max) — cite-or-flag (surfaced during the read-first audit, not worked around) | ✅ **CLOSED by Phase 07 / T02b (2026-07-23, Opus 4.8).** ONE additive migration `20260723140552_order_payment_write_layer_reg49` (ledger 30→31) landed the **three-layer actor↔column write model** (ADR-019) + the checkout RPC (ADR-018). **Live `pg_policies` (pasted, MCP-verified):** `payments_insert` (INSERT `{public}` WITH CHECK `EXISTS(orders o WHERE o.id=payments.order_id AND o.buyer_id=auth.uid())` = buyer-of-parent); `payments_update` (UPDATE `{authenticated}` USING+CHECK `is_admin() OR buyer-of-parent`); `orders_update` (UPDATE `{authenticated}` USING+CHECK `buyer_id=auth.uid() OR store_id=my_store_id() OR is_admin()` — **`is_admin()` KEPT in the policy per SUB-DECISION A, ERD §3 row 54 verbatim**); `settings_payment_config_read` (SELECT `{authenticated}`, `key = ANY(ARRAY['betk_instapay_handle','betk_vodafone_cash','betk_orange_cash','delivery_fee_flat_egp'])` — literal allow-list, REG-69); `modlog_admin_insert` (INSERT `{public}` WITH CHECK `is_admin() AND admin_id=auth.uid()` — **F3 pinned**, REG-68). **Live column privileges (pasted):** `orders` authenticated UPDATE = exactly `{status, cancellation_reason}` (**F1 — `cancelled_by` dropped from GRANT, trigger-stamped; `confirmed_at` ungranted, trigger-stamped**); `payments` authenticated UPDATE = `{status, proof_path, transfer_reference, confirmed_at, confirmed_by, notes}`. **Triggers (SECURITY DEFINER, search_path-pinned, EXECUTE-revoked):** `enforce_order_transition` (BEFORE UPDATE — legality: pending→confirmed store-only w/ AC-SEL-14 confirmed-deposit gate, confirmed→preparing store-only, pending→cancelled buyer-only stamping `cancelled_by`; **F1** cancel-metadata guard OUTSIDE the status branch → `BETK_CANCEL_METADATA_FORBIDDEN`; **SUB-DECISION A** — no `OR is_admin()` in the actor checks, Phase 14 amends for admin-forced cancel), `enforce_payment_update` (BEFORE UPDATE — admin-only status change; **F2** transition pending→confirmed ONLY else `BETK_ILLEGAL_PAYMENT_TRANSITION`), `set_order_commission_snapshot` (BEFORE INSERT — `commission_rate`/`commission_amount = round(rate/100*subtotal,2)`; **F5** `BETK_COMMISSION_CONFIG_MISSING` if `commission_rate_pct` row absent, explicit '0' passes). **RPC `create_order_from_inquiry` (ADR-018):** SECURITY INVOKER, search_path pinned, EXECUTE revoked PUBLIC / granted authenticated; `delivery_fee` + amounts re-resolved server-side (NOT a param); **F4** betk_ref uniqueness retry in a `BEGIN…EXCEPTION WHEN unique_violation` block. **Advisor sweep: security 8/6/2/4/1 UNCHANGED** (0 new — the 3 DEFINER triggers are pinned+revoked → absent from 0011/0028/0029; the RPC is INVOKER). **Performance: +5, all attributed** — 4 `auth_rls_initplan` (`payments_insert`/`payments_update`/`orders_update`/`modlog_admin_insert`, the house bare-`auth.uid()` style, REG-36 class) + 1 `multiple_permissive_policies` on `admin_settings {authenticated, SELECT}` (the intended REG-69 buyer-read broadening), 0 unexplained. **anon retains table-wide UPDATE on orders/payments** (harmless — both new policies are TO authenticated + RLS on; ADR-015 precedent; recorded in the migration header so no future TO-public policy inherits it silently). Integration `orderPayment.writeLayer.test.ts` **26 passed + 3 opt-in skipped** on staging (three-layer authz, F1/F2 raises proven as ERRORS, allow-list both-directions, anon 0 rows, payment_config_missing), zero residue (default run); backfilled into `BETK_DATABASE_SCHEMA.sql`. **B7 (2026-09-23):** closed. |
| REG-50 | BETK **support** contact has no home. Footer "Contact us" should repoint to a **support WhatsApp deep-link** — a **BETK→user support channel**, NOT counterparty contact, so it is posture-compliant (brief §9(ii)'s own "repoint" option). Needs a support number + a landing home; **"Help center" STAYS unrouted**. A dedicated web-app support PAGE would take a **new OD, NOT authorized**. **B1 (2026-09-19): the parenthetical “would be OD-9” was a HYPOTHESIS, never a mint — OD-9 is now occupied by the multi-seller cart order shape.** A support page, if ever authorized, takes the next free OD **at that mint time**. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned) — needs a support number; no page added. Hypothetical “OD-9” is void. **B7 (2026-09-23):** open. v2 owner: named pin: no support page. A page would be a new OD. None is authorized. |
| REG-51 | **Channel-agnostic share** affordance (device share sheet on a **public link** — product listing + seller storefront; WhatsApp/FB/Instagram/link downstream, NOT enumerated). A share is not counterparty contact → posture-compliant. Storefront share ADDED here as a **human-authorized addition**; Listing-Detail share reworded channel-agnostic. | Minted by CORRECTION-02 (2026-07-23) — owner **Claude Design CD-DELTA-5** | Open (owned, CD-DELTA-5) — implement the share component. **B7 (2026-09-23):** open. v2 owner: Phase 09, blocked on ShareButton (Stage D). |
| REG-52 | Seller-onboarding category/subcategory picker is poorly organized (UX). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 / onboarding UX). **B7 (2026-09-23):** open. v2 owner: Phase 09 (compose Select). Does not block the phase exit. |
| REG-53 | Seller-onboarding delivery modes should **DERIVE from category** (a remote/digital service vs a physical product implies different valid modes) rather than offering all three unconditionally. No schema change (REG-14 `delivery_preference` modes unchanged). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned) — product/UX decision. **B7 (2026-09-23):** open. v2 owner: Phase 09 (closes by R-K01; do not derive a mode from category). |
| REG-54 | Docs say `seller_analytics_snapshots` / `platform_analytics_snapshots`; the LIVE tables are **`seller_snapshots` / `platform_snapshots`** (ERD §1 inventory + `betk_analytics` is authoritative). Fixed in `BETK_UI_SPEC.md` (Earnings) here; **doc sweep still owed** for `BETK_ERD.md §2` + `BETK_PRD.md` (FR-SEL/FR-ADM analytics rows) + any other doc. | Minted by CORRECTION-02 (2026-07-23) — docs-hygiene | Open (owned) — doc sweep only; live schema is correct, no code/DB change. **B7 (2026-09-23):** open. v2 owner: Phase 08 doc sweep when the rename lands. |
| REG-55 | Public storefront rendered the **seller's `payment_methods` handles to buyers** (`store/[slug]` About tab + checkout instructions) — an active leak under custody (a buyer could pay the seller directly, bypassing BETK + commission). Doc half corrected here (UI_SPEC storefront/checkout/confirmation now render BETK's `admin_settings` handles). | Minted by CORRECTION-02 (2026-07-23) — owner **CORRECTION-02B** (code) | ✅ **CLOSED by CORRECTION-02B (2026-07-23, Sonnet).** `store/[slug]` About tab's `StorePaymentSection` deleted (delivery + return policy stay); `getStoreBySlug` drops `payment_methods` from the select + mapped type (grep-proved zero other consumer — `/seller/store/payments` reads a different query, `getOwnStorePayments`). 6 orphaned i18n keys removed both locales (parity 736→730/730). Checkout/confirmation repoint to BETK's handles is Phase-07 T02's build (not yet built — no checkout UI exists to repoint), so only the storefront leak half was code-owed here; UI_SPEC already reflects the target state (CORRECTION-02). Runtime-proven on `next start`: zero payment text in the rendered DOM (script/style stripped first — hydration-payload precedent), both locales. **B7 (2026-09-23):** closed. |
| REG-56 | Terminal `order_status` for admin **order closure** (OD-8 §3 step 7) — **FLAGGED per STEP 0(a): the pinned enum `{pending,confirmed,preparing,dispatched,delivered,cancelled,returned}` has NO "closed"/"settled" member beyond `delivered`.** Per cite-or-flag, **no enum member is invented**. **Closure is expressed as a DERIVED condition — both payment rows `confirmed` AND `orders.status='delivered'`** (admin closure is the operational signal on `/admin/payments`; OD-8 §6 pairs it with `delivered_at + return_hold_hours` for the settlement/return-hold window). | Minted by CORRECTION-02 (2026-07-23) — cite-or-flag | Open (owned) — a first-class closure signal would be an OD amendment (new enum member = schema change), NOT invented here. **B1 / OD-18:** derived closure **extends to the master** (baseline §4.2 / §11 REG-56 Amend); still no invented enum member; still no close action. **B7 (2026-09-23):** open. v2 owner: Phase 19 (derived closure; no new enum member). |
| REG-57 | Phase-07 pack (T02–T06) must be **REGENERATED** for the custodial model (OD-8): no COD auto-confirm (§3.1, R-O04 retired), **no pure-COD path (§3.2 — DECIDED-retired, every order carries the 50/50 split)**, admin deposit-verification gate, commission snapshot, BETK's rails. The pack still describes the pre-custody model → this **BLOCKS Phase-07 pack regeneration / T02 start**. (NB: §3.2 pure-COD is DECIDED, not open — the OPEN item is the pack rebuild.) | Minted by CORRECTION-02 (2026-07-23) | Open (owned) — regenerate T02–T06 before building; T00/T01 stay DONE (artifacts survive, CORRECTION-01 §G3). **B7 (2026-09-23):** superseded. The instruction to regenerate the Phase 07 pack is void (BETK_PHASES.md §3, B6). The T01b regeneration sits on the archived branch and is not resumed. Custodial checkout, deposit, seller orders, and courier are v2 Phases 11–14. A retired phase owns nothing. |
| REG-58 | Dark-theme search-bar text/background contrast fails a11y (AA). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 a11y). **B7 (2026-09-23):** open. v2 owner: Stage D pin (existing SearchBar). Does not block Phase 09. |
| REG-59 | `/account` renders unstyled. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5). **B7 (2026-09-23):** open. v2 owner: Stage D pin (P09 shell). Page owner stays Phase 02. |
| REG-60 | Seller-console logo does not navigate home. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 / SellerChrome). **B7 (2026-09-23):** open. v2 owner: Stage D pin (seller chrome logo). Not REG-72. |
| REG-61 | R-S09 publish gate: `cod_enabled` no longer satisfies "seller can be paid" under OD-8 §7 — `hasPaymentMethod` in `src/features/listings/listingRules.ts` must become "**≥1 settlement handle set**" (`cod_enabled` alone stops satisfying it; a COD-only seller newly fails the gate). Doc half corrected here (UI_SPEC Payment Methods + publish validation + onboarding, "seller can be paid" wording). | Minted by CORRECTION-02 (2026-07-23) — owner **CORRECTION-02B** (code) | ✅ **CLOSED by CORRECTION-02B (2026-07-23, Sonnet).** `hasPaymentMethod` now checks ONLY `instapay_handle`/`vodafone_cash`/`orange_cash`; `cod_enabled` no longer satisfies it (unit-proven: `{cod_enabled:true}` alone → `false`, `evaluatePublishRequirements` → `["payment_method"]`). Publish-checklist i18n reworded both locales ("add a settlement method so BETK can pay you"). The ONE COD-only integration fixture found across the whole suite (`listing.writeLayer.test.ts`'s `sellerA`, used as the "publish CAN pass" control) was repaired to a real settlement handle — the fixture was fixed, not the rule; every other COD fixture already paired it with a real handle. Onboarding StepPayment copy (note + atLeastOneHint) reworded i18n-only to the settlement framing, zero wizard/Zod/JSONB shape change. **B7 (2026-09-23):** closed. |
| REG-62 | **HARD PRE-LAUNCH GATE** — all six OD-8 payment-config `admin_settings` keys ship PROVISIONAL/empty (seeded by CORRECTION-03 migration `20260723110557`): `commission_rate_pct='0'`, `delivery_fee_flat_egp='0'`, `return_hold_hours='48'` (engineering default, house-consistent with dispute_sla_hours/review_edit_window_hours — NOT spec-derived), `betk_instapay_handle`/`betk_vodafone_cash`/`betk_orange_cash`=`''`. **`0` and `''` are deliberate "not yet configured" sentinels, NOT business decisions.** Same class as the retired REG-12/13 SMS gate. | Minted by CORRECTION-03 (2026-07-23) — **HARD GATE** | **Open — HARD pre-launch gate (NOT housekeeping).** **v2 (B1 / baseline §12) NARROWS** this gate to `betk_instapay_handle` set · commission % set · price band set. Flat `delivery_fee_flat_egp` and VF/Orange BETK handles drop from this gate (courier matrix = Courier gate; buyer rail = InstaPay-only). `return_hold_hours` is **not named** in baseline §12 — FLAG for B3, not invented back in. Historical v1 launch block (commission, flat fee, return-hold, ≥1 of three handles) is superseded by the narrowed set. Sentinels `0`/`''` remain “not yet configured”. **B7 (2026-09-23):** open. v2 owner: launch gate (narrowed: price band, commission %, betk_instapay_handle). Not a build phase. |
| REG-63 | `stores.payment_methods.cod_enabled` is now DEAD (COD is universal per OD-8 §3.2 — every order carries the 50/50 split, COD is only the balance leg remitted to BETK — so the flag gates nothing and means nothing buyer-facing anymore). Every occurrence (JSONB shape, `storePaymentMethodsSchema` Zod, the onboarding wizard's `StepPayment` toggle, `PaymentsSettingsForm`'s toggle) is LEFT IN PLACE — removing it would be a JSONB/Zod/wizard shape change, not authorized. (CORRECTION-02B's STOP-AND-FLAG note asked for this to be minted as "REG-62" — that number was already taken by the OD-8 hard pre-launch gate above, so it is minted here instead.) | Minted by CORRECTION-02B (2026-07-23) — record-only | Open (owned, post-Phase-07 cleanup (Phase 07 is retired and owns nothing; B7 owner is the named pin: do not drop `cod_enabled`)) — a dedicated task should decide whether to remove the toggle (JSONB/Zod/wizard change) or repurpose its copy; no code change made here. **B7 (2026-09-23):** open. v2 owner: named pin: do not drop cod_enabled. Not a v2 phase task. |
| REG-64 | `/seller/store/payments` (`PaymentsSettingsForm.tsx`, `seller.store.payments.note` i18n key) still describes the handles as "shown to buyers at checkout" — the same stale buyer-facing framing CORRECTION-02B reworded on the onboarding `StepPayment` step, but this settings page/copy was NOT named in CORRECTION-02B's explicit scope (only the storefront render, publish gate, and onboarding step 3 were). Flagged in a code comment, i18n left untouched. | Minted by CORRECTION-02B (2026-07-23) — record-only | Open (owned) — reword `seller.store.payments.note` (+ any sibling copy) to the BETK→seller settlement framing, same treatment as `seller.onboarding.payment.note`. **B7 (2026-09-23):** open. v2 owner: Phase 09 (P29 settlement copy). |
| REG-65 | Seller-onboarding dead delivery-fee field. Onboarding stored a delivery fee in the store delivery JSON. Project-state prose called it the seller-onboarding dead delivery-fee-field. Baseline §11: “REG-65 dead delivery-fee fields · D9 · D10 — Close by deletion — the whole JSONB dies.” The number was pre-assigned with no row. B7 writes the row. The number stays burnt and is not reused. | Pre-assigned before B7; row written B7 (2026-09-23). Cite: this file’s project-state prose and `BETK_V2_SCOPE_BASELINE.md` §11. | Phase 09 | **B7 (2026-09-23):** open. v2 owner: Phase 09. Phase 09 retires the onboarding delivery-mode toggles and this dead delivery-fee field under OD-10 (`BETK_PHASES.md` Phase 09). Not closed in B7: the field is not deleted in this docs task. |
| REG-66 | Standing-facts "unit **120/120 · 8 files**" baseline (as recorded in the Phase-07/T01 and CORRECTION-03 journal entries) does not match any real `tests/unit/*` repo state — flagged for verification per the T01b unit-count-check carry-forward | Minted by Phase 07 / T01c (2026-07-23, Sonnet High) — pack identity + evidence remediation | ✅ **CLOSED-WITH-EVIDENCE (2026-07-23, Phase 07 / T01c).** `git log --all --diff-filter=D --name-only -- "tests/unit/*"` returns **EMPTY across the entire repo history** — no `tests/unit/*` file has ever been deleted; nothing was "silently dropped." Per-commit file-count audit of every commit that ever touched `tests/unit/` (`589fbb3`→`92869e4`→`7e4a9f1`→`2a8be4e`→`d5d67f6`→`fc9408d`→`8dde20a`→`6d922fb`[CORRECTION-02B]→`881832f`[Phase-06 merge]→`340cd86`/`99f7ac8`/`e0f6a2d`[CORRECTION-03]→HEAD) is **monotonic 1→2→3→4→5→6→7**, never reaching 8, never regressing — **the "8 files" figure was never true at any commit**, so "120/120 · 8 files" is a **copy-forward/transcription artifact**, not a measured baseline that later regressed. Live re-run at HEAD (`npx vitest run tests/unit`) confirms **111 passed (111), 7 files** — exactly matching the Standing-facts line already recorded below, which is therefore left UNCHANGED. No file re-added; no regression exists to fix. **B7 (2026-09-23):** closed. |
| REG-67 | **Counts-drift guard candidate (Guard F). WIDENED at Phase 07 / T04 (2026-07-24) to explicitly cover ROUTE COUNT, not just test counts.** Documented count baselines in `SESSION_CONTEXT.md` Standing facts (unit N/N · integration files/passed · **route count** · guard-suite subcounts · migration ledger · advisor 5-tuple) are hand-maintained and have drifted before (REG-66 "120/120·8 files" myth; the recurring i18n/zod subcounts). **The historical "44 routes both locales" `next build` route-table figure has now JOINED "120/120·8 files" as a recorded gate figure whose measurement method could not be reverse-engineered (T03 finding, re-confirmed at T04)** — `PHASE_07_ORDERS.md` T07 cites route count as exit-gate evidence, so this is load-bearing, not cosmetic. T03/T04 both reported the alternative, reproducible **physical `page.tsx`/`route.ts` file-count method** instead (28→30 at T03, 30→32 at T04) — this is the RECOMMENDED replacement convention pending Guard F. No committed check asserts either baseline still matches a live measurement. | Minted by Phase 07 / T02b (2026-07-23, Opus 4.8) — **RECORD-ONLY** (pairs with REG-47 Guard E); WIDENED by Phase 07 / T04 (2026-07-24) | Open (owned) — housekeeping, NOT built here. Candidate scope when built: a `scripts/` check (or a CI step) that re-measures unit/integration/route/guard counts + `list_migrations` + the advisor 5-tuple and fails if they diverge from the pinned Standing-facts baseline (or auto-updates it); ADOPT the physical file-count method for routes (reproducible), retire the unreconciled "44 routes both locales" figure. Schedule into a convenient housekeeping window alongside REG-47 (Guard E) and REG-74 (Guard G — staging-residue pre-flight, the RESIDUE-PURGE fix); **do NOT build during a feature task.** **B7 (2026-09-23):** open. v2 owner: Phase 08 (Guard F). |
| REG-68 | `betk.moderation_logs` admin INSERT policy ABSENT (SELECT-only `modlog_admin`; RLS-enabled, no INSERT policy → default-deny for the admin audit-write path) — #14-class (ERD §3 row 71 admin-write specced, INSERT command omitted from the Phase-01 SQL contract) | Minted by Phase 07 / T02b (2026-07-23, Opus 4.8) — surfaced building the deposit-confirmation audit path | ✅ **CLOSED by Phase 07 / T02b (2026-07-23, Opus 4.8).** Same migration `20260723140552`. Added `modlog_admin_insert` (INSERT `{public}` WITH CHECK `betk.is_admin() AND admin_id = auth.uid()`). **F3 — PINNED:** ERD §3 row 71 + the live column list confirm `moderation_logs.admin_id` exists and the row supports it, so the actor is pinned `AND admin_id = auth.uid()` per the `order_messages`/`inq_msg_insert` pinned-actor precedent (attribution can't be forged). No app writer this phase — `confirmDepositPayment` does NOT write `moderation_logs` (the `moderation_target` enum carries no `payment`/`order` member — a semantic mismatch flagged, not forced); the policy is landed for the Phase-14 admin surface. Advisor: +1 `auth_rls_initplan` (bare `auth.uid()`, REG-36 class), 0 security. **B7 (2026-09-23):** closed. |
| REG-69 | **STANDING — `admin_settings` buyer-read allow-list is LITERAL, never widen.** `settings_payment_config_read` (SELECT `{authenticated}`) exposes EXACTLY the 4 non-secret payment-config keys `{betk_instapay_handle, betk_vodafone_cash, betk_orange_cash, delivery_fee_flat_egp}` via a literal `key = ANY(ARRAY[...])` allow-list. The buyer needs these to render checkout (BETK's receive-handles + the flat delivery fee); `commission_rate_pct` + `return_hold_hours` (BETK↔seller economics) MUST stay admin-only. | Minted by Phase 07 / T02b (2026-07-23, Opus 4.8) — STANDING guardrail | **Standing (load-bearing).** The `key IN (...)` clause **must never become a prefix/pattern/`LIKE`/`NOT IN`** (any of which could leak a future secret key), and **no secret may ever be stored under those four keys.** Any new buyer-readable config key is added by EXTENDING the literal array in a reviewed migration, never by broadening the predicate shape. Integration asserts both directions (a non-admin sees exactly the 4; `commission_rate_pct`/`return_hold_hours` → 0 rows; anon → 0 rows). Revisit only deliberately. **B7 (2026-09-23):** open. v2 owner: Phase 08 rewrites the allow-list to exactly betk_instapay_handle; the literal-list rule stays a standing pin. |
| REG-70 | `betk.moderation_target` has no `payment`/`order` member — live enum (MCP-verified, 2026-07-23): `{seller, buyer, listing, review, dispute, payout}`. `confirmDepositPayment` (FR-ADM-10, admin deposit-verification) therefore CANNOT write its `moderation_logs` audit row (no legal `target_type` value describes "a payment" or "an order") — the action skips the log, citing this exact reason inline. A new enum member is a schema change and is NOT invented here; **Phase 14 (Admin) owns any enum amendment**, should the audit trail be extended to cover payment/order moderation actions. | Minted by Phase 07 / T03 (2026-07-23, Sonnet Medium) — record-only, close-out | Open (owned by Phase 14) (v1 heading, §H — v1 admin). B7 owner is the named pin: no new `moderation_target` member. v2 Phases 12 and 18 obey the pin. **B7 (2026-09-23):** open. v2 owner: named pin: no new moderation_target member. Phases 12 and 18 obey it. |
| REG-71 | **NO-PURE-COD whole-table check (STEP 1d) was BLOCKED by pre-existing staging residue — ROOT CAUSE MEASURED (RESIDUE-PURGE, 2026-07-24, Opus Max).** The "49 orphans" was a *symptom*; the live footprint at purge time was **71 orphaned orders across 4 fixture batches**: B1 `P7T02B-8cf7a1cb-*` (23 @ 07-23 20:22, 2 w/history), the 3 `BETK-2026072*-A04E/E239/E926` (all 3 w/history), B2 `P7T02B-a97d046c-*` (23 @ 20:23, 2 w/history), B3a `BETK-T05TEST-*` (1 @ 07-24 07:00, from `discovery.listing.test.ts`), B3b `P7T02B-03b6882e-*` (21 @ 07:07 + 15 payments, from `orderPayment.writeLayer.test.ts`). **ROOT CAUSE = INTERRUPTED test runs, NOT a `purge()` code defect.** NATURAL-EXPERIMENT PROOF: a full `npx vitest run tests/integration` started `2026-07-24T07:10:06Z` re-ran those SAME two files and **completed `exit_code 0`** (26 passed/1 skipped, ended `07:15:11Z`) leaving **ZERO residue** (0 orders + 0 `@betk.test` auth users created after 07:08 — its own identical fixtures were cleaned perfectly by `afterAll(purge)`); the 07:00 & 07:07 runs left full footprints ONLY because they were killed before the hook (Vitest does not run `afterAll` on Ctrl-C/stop/timeout/crash). Corroboration: B3b carried the EXACT complete default-suite set (21 bare orders + 15 payments + 0 history + 6/6 auth + 6/6 betk users + all stores/profiles/listings/inquiries/address) ⇒ zero teardown had occurred. The 7 history-bearing orders are the sole UNDELETABLE class (append-only `no_delete_order_history` RULE + NO-ACTION `order_id` FK; empirically `sqlstate 23503`). | Minted by Phase 07 / T04 (2026-07-24, Sonnet High); root cause measured + purge executed by RESIDUE-PURGE (2026-07-24, Opus Max) | **FIX = Guard G (REG-74) staging-residue detector at integration-suite start — NOT a `purge()` rewrite (`purge()` is provably correct).** Purge executed this window: nulled dangling `converted_to_order_id`, deleted the 64 bare orders + full programmatic footprint (users/stores/listings/inquiries/addresses/payments/reviews/review_photos/rating_aggregates), LEFT the 7 history-bearing orders + their transitively-pinned cluster (IDs recorded in the journal). NO-PURE-COD ledger PARTIAL→**RE-ENABLED**: `orderPayment.noPureCod.dbCheck.test.ts` now runs as a CANARY (`EXPECTED_ORPHANS=7`, the 7 refs/IDs pinned, NO timestamp cutoff — fails on the 8th orphan). **Post-purge VERIFIED (read-only, 2026-07-24):** `betk.orders`=7 (all 7 payment-less ∧ history-bearing = the stuck class), `betk.payments`=0, `@betk.test` auth users=0, dangling `converted_to_order_id`=0, `reviews`/`review_photos`/`rating_aggregates`=0, real `jiovanny.adel@gmail.com` intact; net delete = **64 orders (71→7)** + full programmatic footprint. Guard G minted as **REG-74**. **B7 (2026-09-23):** closed. The purge and the root cause are in this row. The unbuilt detector is REG-74 (open, Phase 08). |
| REG-72 | **STOREFRONT-DIAG (Phase 07 / T04, read-only diagnosis) — classified C (Claude Design), not fixed here.** The seller/store name rendered on `SellerMiniCard` (listing detail, `/listing/[id]`), `ListingCard` (search results + storefront + category grids — the store name under the listing title), and `StoreCard` all render as **plain, non-interactive text** — none of the three exposes an `href`/`onClick`/`asChild` prop on the name, so there is no compose-layer way to make it navigate to `/store/[slug]` without editing `components/shared` (compose-only discipline forbids that here). `getListingById`/discovery queries already select the store's `slug` end-to-end — **this is NOT a missing-data (W) defect**, confirmed read-only before any fix was attempted. **Same class as the already-open REG-60** (seller-console logo doesn't navigate home) — all four are "an entity's visual identity block has no navigation affordance," one recurring CD-DELTA-5 gap. | Minted by Phase 07 / T04 (2026-07-24, Sonnet High) — STEP 2, STOREFRONT-DIAG | **Open (owned, CD-DELTA-5).** CD-DELTA-5 handoff (record-only, for Claude Design): add an optional `href` (or `onClick`/`asChild`) prop to the name/logo slot on `SellerMiniCard`, `ListingCard` (store-name span), `StoreCard`, and `ConsoleSidebar`'s logo (REG-60) — same shape across all four; once landed, Cursor wires `routes.store(slug)` at each of the 4 call sites in a single compose-only follow-up. **B7 (2026-09-23):** open. v2 owner: Phase 09, blocked on Navigable store identity. Phase 17 wires P12 after the component exists. |
| REG-73 | **D6 — buyer-cancel vs. an already-CONFIRMED deposit (product decision CLOSED; see status).** `CancelOrderButton` (`/orders/[id]`, R-O03) gates ONLY on `order.status === 'pending'` — DB-authoritative (`enforce_order_transition`) and UI pre-check agree. Neither layer checks the DEPOSIT payment's status. A buyer CAN therefore cancel a pending order whose deposit is ALREADY `payments.status='confirmed'` (BETK/admin verified the transfer, but the seller has not yet accepted — exactly the `pending` + deposit-confirmed window). The money is already custodied by BETK against no order, with **no MVP refund path** (`payments.status='refunded'` is Phase 10/14). | Minted by Phase 07 / T04 (2026-07-24, Sonnet High) — flagged, not fixed; built to CURRENT spec | ✅ **CLOSED** (B2-FIX, 2026-09-22). The deposit-confirmed case is pinned by `BETK_V2_ROLE_JOURNEYS.md` §5.2, quoted on **R-O03**: “Buyer may cancel only before uploading proof. After the deposit is confirmed the exit is return/refund/dispute, never cancellation.” Cancel window also `BETK_V2_SCOPE_BASELINE.md` §2.6 (“Only before uploading payment proof”). Points at R-O03. v1 code still cancels on `pending` only; the orders rebuild implements the closed decision and does not re-open it. **B7 (2026-09-23):** closed. |
| REG-74 | **Guard G — staging-residue pre-flight at integration-suite START (the RESIDUE-PURGE fix).** REG-71 proved that INTERRUPTED integration runs (Ctrl-C/stop/timeout/crash) skip `afterAll(purge)` and leak their full fixture footprint into shared staging, which then silently accumulates and blocks whole-table invariants (the 49→71-orphan block on STEP 1d). `purge()` is provably correct (natural-experiment: a completed run self-cleaned to ZERO residue), so the fix is DETECTION at the front door, not a `purge()` rewrite. Candidate scope when built: a `globalSetup`/suite-start check that counts pre-existing `@betk.test` auth users + payment-less NON-stuck `betk.orders` and FAILS FAST naming the leftover refs, so an operator re-runs the purge before a fresh suite instead of a downstream assertion discovering it later. Sibling of Guard E (REG-47, `loading.tsx`-404) and Guard F (REG-67, counts-drift) — three record-only CI guards owed; same baseline-hygiene family as REG-66/REG-67 (hand-maintained invariants that drift when unenforced). | Minted by RESIDUE-PURGE (2026-07-24, Opus Max) — **RECORD-ONLY** | Open (owned) — housekeeping, NOT built here. INTERIM DETECTOR: the re-enabled `orderPayment.noPureCod.dbCheck.test.ts` canary (`EXPECTED_ORPHANS=7`, no timestamp cutoff) already fails on the 8th orphan, but mid-suite; Guard G moves the catch to suite START. Build alongside REG-47 (Guard E) + REG-67 (Guard F) in a housekeeping window; **do NOT build during a feature task.** **B7 (2026-09-23):** open. v2 owner: Phase 08 (Guard G). |
| REG-75 | **T&C acceptance backfill (N26)** — every user created during phases 01–06 signed up with no acceptance record. Owner = the phase that builds acceptance. Decide backfill vs forced re-accept at first login. | N26 | The phase that builds `agreement_acceptances` | Open (owned) — **RECORD-ONLY now.** **B7 (2026-09-23):** open. v2 owner: pin before Phase 09 agreements work. |
| REG-76 | **v1→v2 order migration (N27)** — every existing `betk.orders` row must become a `seller_order` under a synthetic master order. Includes the 7 undeletable zombie orders pinned by the append-only `no_delete_order_history` RULE and their transitive cluster (4 users, 2 seller_profiles, 2 stores, 3 inquiries, 2 addresses). | N27 | Stage C schema delta plan | Open (owned) — **RECORD-ONLY now.** **B7 (2026-09-23):** open. v2 owner: Phase 08 (N27 forward migration). **C-FIX (2026-09-23):** D1 decided. The five non-cancelled rows migrate as `cancelled` in M5, one history INSERT each, original seven history md5s unchanged. Not applied. |
| REG-77 | **CI-enforcement gap — branch protection requires only `RLS smoke (staging)`.** METHOD (2026-09-17, Stage A): `gh api repos/Jovo-Jovi/betk/branches/main/protection` → `required_status_checks.contexts = ["RLS smoke (staging)"]` (app_id 15368); `gh api repos/Jovo-Jovi/betk/rulesets` → `[]`. The eight CI job names in `.github/workflows/ci.yml` are `Install` · `Lint` · `Typecheck` · `Vitest (unit)` · `Guards` · `Types drift` · `Build` · `RLS smoke (staging)`. A repo ruleset (or required-check list) requiring **all eight names** is owed **before the v2 build starts**. RLS smoke self-gates to a logged no-op on migration-free PRs, so a red Types-drift/Guards/Build check on a docs-only PR will **merge silently** if nobody looks. Stage A PR is the canary for the 2026-09-17 `SUPABASE_ACCESS_TOKEN` rotation: if Types-drift or Guards go red with an empty `src/` diff, that is the token or the token↔`SUPABASE_PROJECT_REF` pairing — surface it, do not patch around it. | Stage A (2026-09-17) | Human — GitHub ruleset / branch protection, before v2 build | ✅ CLOSED (2026-09-26, Phase 08 T00). Open (owned) — **RECORD-ONLY now.** **B7 (2026-09-23):** open. v2 owner: build-start gate. Human. Before Phase 08. **E-1 (2026-09-23):** `gh api repos/Jovo-Jovi/betk/branches/main/protection --jq .required_status_checks.contexts` → `["RLS smoke (staging)"]`. `gh api repos/Jovo-Jovi/betk/rulesets` → `[]`. Not all eight names were required then. Protection was not changed in E-1. Method: PATCH `repos/Jovo-Jovi/betk/branches/main/protection/required_status_checks` with eight checks, each `app_id` 15368. Re-measure the same day: contexts `["RLS smoke (staging)","Install","Lint","Typecheck","Vitest (unit)","Guards","Types drift","Build"]`; `rulesets` `[]`; `rules/branches/main` `[]`. Branch protection supplies all eight names. |
| REG-78 | **Courier principal for RLS.** N28 / OD-17 require addresses visible to **admin + courier only**. AC-COU-6 forbids a courier login. **B3-FIX resolution:** no `user_role` courier and no courier RLS policy. No `courier_label_payload` function. Admin-initiated label = the admin session reading under its own RLS. No SECURITY DEFINER. No service role. An automated handoff with no admin session, if later chosen, is a server-side service-role read and still needs no definer. Both branches are for B5. The handoff stays tied to the courier gate. AC-COU-6 holds. Shipment status writes are admin. | B1-FIX (2026-09-19); closed B3 2026-09-22 | B5 recorded ADR-024 and did not pick a branch | **Principle CLOSED. Mechanism OPEN** under the courier gate (ADR-024). Do not invent a courier user. **B7 (2026-09-23):** open. v2 owner: courier gate (mechanism). Blocks Phase 14 launch. Principle is closed (ADR-024). |
| REG-79 | **OD-4 verified-phone gate trigger is unpinned under N21.** The gate **surface** is `/auth/phone` (P78) and `/auth/verify` (P07). v1 OD-4 (HOLDS UNCHANGED) names checkout / become-seller / payout as holds. N21 pins **account** before first add-to-cart, not verified phone. **The trigger point stays OPEN.** B2 did not pick add-to-cart vs checkout. B4-FIX2 did not pick it either. Named HOLD surfaces remain checkout / become-seller / payout. | B1-FIX (2026-09-19); surface named B4-FIX2 | Product pin still owed | Open (owned) — **trigger not decided.** **B7 (2026-09-23):** open. v2 owner: pin before Phase 10. |
| REG-80 | **Boosts (G6) are IN for v2 MVP as retained v1 scope.** Decided 2026-09-22 by the scope owner. v1 text stands: R-B01–R-B05, R-L08, FR-SEL-11, FR-SEL-12, FR-ADM-17, and the boost clauses of FR-PUB-1, FR-PUB-2, FR-SEL-21, FR-ADM-15 (`BETK_PRD.md` §1, §3.19). Not rewritten, not expanded, no new boost R/FR/AC. Baseline silence is not an exclusion. Any boost schema question is B3’s and is flagged then (REG-78-class), not a new PRD requirement. History: B1 preserved G6 without an OD; B2 left the codes PENDING. | B1-FIX minted 2026-09-19; B2 left PENDING; B2-FIX closed | B4 maps the retained v1 capabilities | ✅ **CLOSED** (2026-09-22, scope owner) — product pin. **B7 (2026-09-23):** closed. |
| REG-81 | **Child seller-order identifier format (was F-REF).** The master owns the buyer-facing order number (R-O02 amended). The child seller-order display shape is unpinned. Journeys §5.3 `BETK-2026-000123` is illustrative, not a pin. B3 can key a seller order by UUID and must not invent a display format. A human decides the shape. | B2-FIX (2026-09-22) — mint at REG-81 (next free at re-read) | Product pin before any child ref is printed | Open (owned). **B7 (2026-09-23):** open. v2 owner: pin before Phase 11. |
| REG-82 | **Cart restore on payment-window expiry (was F-CART).** **CLOSED** 2026-09-22 by the scope owner. Fixed-price lines restore as-is. Custom-quote lines restore only if the quote is still inside its 24h validity; otherwise dropped with a request-a-new-quote prompt. Propagated to R-C07, AC-CART-7, MVP_SCOPE §3.5. Schema path: `order_items.inquiry_id` → `inquiries.quote_expires_at` (`BETK_ERD.md` §3.3). | B2-FIX minted; B3 closed as a product pin | Stage C implements the path | ✅ **CLOSED** (2026-09-22, scope owner) — product pin. **B7 (2026-09-23):** closed. |
| REG-83 | **Review unit under a multi-seller master (was F-REV).** **CLOSED** 2026-09-22 by the scope owner. A buyer review attaches to **each seller order**. No master-level review. R-O07 / R-R01 / R-R02 amended where they still said OPEN. `reviews.order_id` UNIQUE stays on the seller order. | B2-FIX minted; B3 closed as a product pin | B4 maps review pages against the seller order | ✅ **CLOSED** (2026-09-22, scope owner) — product pin. **B7 (2026-09-23):** closed. |
| REG-84 | **Master-level dispute (was F-DSP).** **CLOSED** 2026-09-22 by the scope owner. **No** master-level dispute. Disputes stay per seller order (R-O06). No `master_order_id` on `disputes`. | B2-FIX minted; B3 closed as a product pin | Stage C keeps `disputes.order_id` on the seller order | ✅ **CLOSED** (2026-09-22, scope owner) — product pin. **B7 (2026-09-23):** closed. |
| REG-85 | **Store return policy vs the platform Return & Refund Policy (was F-POL).** FR-SEL-6 HOLDS UNCHANGED (store return policy remains). How that store text relates to the platform Return & Refund Policy (OD-13) is unpinned. Do not invent override, replacement, or dual-display. **B4 left this OPEN** on the storefront and the seller policy page (`BETK_UI_SPEC.md` §4.f). | B2-FIX (2026-09-22) — took REG-85 | Product | Open (owned) — **do not invent.** **B7 (2026-09-23):** open. v2 owner: pin before Phase 15, and before any Phase 09 edit that relates store policy to the platform policy. |
| REG-86 | **`return_hold_hours` vs the narrowed REG-62 gate (was F-HOLD).** R-O29 stands: manual payout waits for the return-hold window after delivery, and the duration is admin-configurable (MVP_SCOPE §5 → baseline §9). MVP_SCOPE §9 does **not** name `return_hold_hours` in the narrowed REG-62 gate. Do not invent it back into REG-62. Gate membership is a product/launch decision, not an ERD column decision. | B2-FIX (2026-09-22) — took REG-86 | Product / launch gate | Open (owned) — **do not add it to REG-62 here.** **B7 (2026-09-23):** open. v2 owner: named pin beside REG-62. Do not add return_hold_hours to that gate. Phase 19 still honours R-O29. |
| REG-87 | **`BETK_MASTER_EXECUTION_PROMPT.md` still says 59 pages and one FR per page (was F-PROMPT).** That sentence is stale against the v2 PRD (capability FRs, not page FRs), against OD-20 (tables 51), and against **OD-21 (pages 79)**. B4 did not edit the prompt. Do not treat the prompt as the v2 page inventory. | B2-FIX (2026-09-22) — took REG-87 | Later docs sweep | Open (owned) — record-only. **B7 (2026-09-23):** closed. BETK_MASTER_EXECUTION_PROMPT.md now says 79 pages (OD-21) and the §0 counting rule, and capability FRs rather than one FR per page. The table freeze in that prompt is 51 (OD-20). |
| REG-88 | **Checkout version-gate document set is unpinned.** R-G02 blocks order completion until the buyer has accepted the “current required versions”. R-G05 names four documents. Which of the four are in the gate is not pinned. `agreement_acceptances` can store all four. Stage C must not hard-code the set. | B3 (2026-09-22) — took REG-88 (next free at re-read) | Product pin before the gate is coded | Open (owned) — **do not pick.** **B7 (2026-09-23):** open. v2 owner: pin before Phase 09 agreements work. Phase 11 reads the pin and does not choose the set. |
| REG-89 | **CLOSED (ADR-022).** One transfer covers the whole master. Invariants accepted: sum of child deposit rows = the single transfer amount, AND each child deposit + balance = that child total. Master deposit = `round(master_total / 2, 2)`. Children are floored to the piastre; leftover piastres go by largest remainder, tiebreak seller-order id ascending. | B3 minted 2026-09-22; B3-FIX recorded the candidate and did not close it | Stage C implements ADR-022 inside `checkout_from_cart` | ✅ **CLOSED** (2026-09-22, B5, ADR-022) — three examples held both invariants. **B7 (2026-09-23):** closed. |
| REG-90 | **Seller order money visibility. CLOSED** 2026-09-22 (scope owner). The seller sees subtotal, commission, and net only. Never the delivery fee. Never the order total. Reason: the fee is origin × destination × weight, so a seller-visible fee lets the seller infer the buyer’s destination zone, which N28 forbids. Commission is on subtotal only, so the net needs neither. Propagated: `BETK_ERD.md` §3.10 (seller SELECT of `delivery_fee` and `total_amount` = NO) and `BETK_PRD.md` R-V02, R-O28, R-K04, AC-VIS-1, AC-CLO-3, FR-SEL-14/15/17/18. **B4-FIX:** `refunded_subtotal` (renamed from `refunded_amount`) is the goods portion, fee-free by definition. `revenue_egp` is subtotal-based; its writer stays unpinned (REG-26 class). The payout INSERT cap is the derived available net. Grant is ADR-020: revoke table SELECT from `authenticated` and `anon`, then grant every other column to `authenticated`. `BETK_ERD.md` §3.10 and §6.4. Guard is REG-92. | B4 (2026-09-22) — took REG-90 (next free at re-read) | Stage C applies ADR-020; REG-92 guards it | ✅ **CLOSED** (2026-09-22, scope owner; grant ADR-020) — product pin. Implementation decided. **B7 (2026-09-23):** closed. |
| REG-91 | **CLOSED (ADR-023).** Buyer cart and checkout need one combined delivery total. `stores.governorate` is public via `stores_public`. Checkout is SECURITY INVOKER under the buyer’s RLS and cannot read `store_pickup_addresses`, so the rate origin is `stores.governorate`. The pickup street never participates. Pickup governorate is trigger-constrained equal to `stores.governorate`. Stored result is `master_orders.combined_delivery_total`. Not a new table and not a new column. | B4 (2026-09-22) — took REG-91; B4-FIX recorded the candidate and did not close it | Stage C: origin is `stores.governorate`; trigger equates pickup governorate | ✅ **CLOSED** (2026-09-22, B5, ADR-023) — edge closed without a new column. **B7 (2026-09-23):** closed. |
| REG-92 | **Build guard for ADR-020.** (i) A `select *` or `RETURNING *` on `seller_orders` raises `42501` instead of omitting `delivery_fee` and `total_amount`, so every query lists columns. (ii) Any column added to `seller_orders` later is invisible to `authenticated` until that migration grants it. The two hidden columns stay ungranted. One guard covers both. | B5 (2026-09-22) — took REG-92 (next free at re-read) | Stage C, with the grant migration | Open (owned) — docs recorded the guard; the check is not written here. **B7 (2026-09-23):** open. v2 owner: Phase 08 (lint or runtime test, not a typecheck). |
| REG-93 | `payment_window_minutes` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 11. | Open (owned) — **P08-T00 (2026-09-26).** No earlier row names this key. |
| REG-94 | `return_window_hours` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 15. | Open (owned) — **P08-T00 (2026-09-26).** REG-86 names `return_hold_hours`, not this key. |
| REG-95 | `food_requirements` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 09. | Open (owned) — **P08-T00 (2026-09-26).** No earlier row names this key. |
| REG-96 | `agreement_buyer_terms_version` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 09. | Open (owned) — **P08-T00 (2026-09-26).** REG-75 is backfill vs re-accept. REG-88 is which documents are in the checkout gate. Neither pins this version string. |
| REG-97 | `agreement_seller_agreement_version` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 09. | Open (owned) — **P08-T00 (2026-09-26).** Same gap as REG-96. |
| REG-98 | `agreement_return_policy_version` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 11. | Open (owned) — **P08-T00 (2026-09-26).** REG-88 pins set membership, not this version string. |
| REG-99 | `agreement_privacy_version` value empty after M3; consumer fails closed (plan §8.2.5); staging may use a labelled placeholder written by a named task; production needs the pinned value. | CF-12, plan §8.2.5 | Human (product pin). Before Phase 11. | Open (owned) — **P08-T00 (2026-09-26).** Same gap as REG-98. |
> **CD-DELTA-2 shared-kit defects are NOT REG-numbered** (reconciled 2026-07-19): the three Claude-Design kit gaps — `ImageGallery` missing its own `"use client"`, `WishlistButton` heart-click bubbling into card navigation, `ListingCard` lacking `wishlistAddLabel`/`wishlistRemoveLabel` passthrough — are tracked + ✅ CLOSED as **CD-DELTA-2** (landed T06, 2026-07-18; full detail → journal). They had briefly collided with REG-26/27/28; those numbers are now restored to their canonical items above (view_count / i18n-parity guard / 404-chrome).

## B2 flags — disposition (B2-FIX, 2026-09-22)
Register re-read at B2-FIX mint: header was REG-01..REG-80, next free **REG-81**, no `REG-81` row. Took **REG-81..REG-87**. Next free was **REG-88**. **B3 re-read (2026-09-22):** no REG-88 row; took **REG-88** (checkout document-gate set) and **REG-89** (deposit half-piastre rounding). Next free **REG-90**. **B3-FIX re-read (2026-09-22):** header still REG-01..REG-89, next free **REG-90**, no REG-90 row. **None taken.** **B4 re-read (2026-09-22):** header REG-01..REG-89, next free **REG-90**, no REG-90 row. Took **REG-90** (closed pin) and **REG-91** (open projection). Next free **REG-92**. OD re-read: next free **OD-21**; took **OD-21**. Next free **OD-22**. **B4-FIX re-read (2026-09-22):** header REG-01..REG-91, next free **REG-92**, no REG-92 row. **None taken.** OD-21 not amended. **B4-FIX2 re-read (2026-09-22):** header still REG-01..REG-91, next free **REG-92**, no REG-92 row. **None taken.** OD-21 corrected in place from 77 to **79**. No new OD. **B5 re-read (2026-09-22):** header REG-01..REG-91, next free **REG-92**, no REG-92 row; ADR file ended at ADR-019. **Took REG-92 and ADR-020..ADR-025.** Next free **REG-93**, **ADR-026**. No new OD.

| Flag | Disposition | Number |
|---|---|---|
| F-MODE | (b) was an in-task note. **Resolved by B3:** enum and column kept; `pickup`/`remote` dead; existing values not rewritten (`BETK_ERD.md` §3.5). | not minted |
| F-REF | (a) human must decide the child display shape; B3 must not invent the journeys example | **REG-81** |
| F-CART | (a) authority contradiction; blocks B3 stock lifecycle | **REG-82** |
| F-REV | (a) blocks B4 page inventory | **REG-83** |
| F-DSP | (a) product must say whether a master-level dispute is wanted | **REG-84** |
| F-POL | (a) product / B4 must pin store policy vs platform Return Policy | **REG-85** |
| F-HOLD | (a) launch-gate membership; not an ERD decision. Do not add `return_hold_hours` to REG-62 | **REG-86** |
| F-PROMPT | (a) later docs sweep; B3 does not edit the master prompt | **REG-87** |
| F-N22 | not in the B2-FIX mint list. **B3 validated** the snapshot direction (`BETK_ERD.md` §3.2). Not overridden. | not minted |

## B3 entry checklist (B2-FIX, 2026-09-22) — DONE (B3, 2026-09-22)
Resolved in `BETK_ERD.md` §3. Do not reopen.
- **F-MODE — resolved.** Enum `delivery_preference` and `seller_orders.delivery_method` kept. `pickup` and `remote` are dead. Existing values are not rewritten. New rows write `delivery`. Fee ignores the column (`BETK_ERD.md` §3.5). Behaviour is already pinned: `BETK_V2_SCOPE_BASELINE.md` §2.5 — “Courier only. No pickup, no remote, no self-delivery. `delivery_preference` becomes single-valued and therefore carries no information.” Record the column/enum fate. Do not reopen courier-only.
- **REG-78 — revised by B3-FIX, recorded by ADR-024.** No courier login. No courier RLS principal. Both handoff branches are recorded. B5 did not pick. Mechanism stays under the courier gate.
- **F-N22 — validated, then accepted by ADR-021.** Canonical proof on the master; the verification trigger copies it onto each deposit row. ADR-019’s `authenticated` UPDATE grant drops `proof_path` and `transfer_reference`. Seller balance facts that are not proof live on `seller_orders` (`balance_confirmed_at`, `refunded_subtotal`, `payout_eligible_at`).

## Stage C inputs (B3-FIX, 2026-09-22)

Not a migration. The contract is `BETK_ERD.md` §4.1 and §13.

- Listing weight and dimensions: backfill active listings, or add the publish CHECK as `NOT VALID`.
- `enforce_order_transition` reads `OLD.buyer_id`. The column is kept, so the rework is not sequenced in front of a drop. If a drop is reopened, rework the function first.
- Dependency list, measured 2026-09-22 (`pg_depend`, policies, `pg_proc`, `pg_trigger`, indexes, rules, views). Views, rules, and other schemas: none. B3-FIX keeps all three columns. Copy them onto the synthetic master. Do not drop them from the child. Relax `betk_ref` to nullable.
  - `orders.buyer_id`: constraint `orders_buyer_id_fkey`; index `idx_orders_buyer`; policies `order_items_access`, `order_items_insert`, `order_messages_access`, `order_messages_insert`, `order_status_history_access`, `order_status_history_insert`, `orders_access`, `orders_insert`, `orders_update` (USING and WITH CHECK), `payments_access`, `payments_insert`, `payments_update` (USING and WITH CHECK), `shipment_tracking_events_access`, `shipments_access`; functions `enforce_order_transition` (`OLD.buyer_id`, trigger `trg_enforce_order_transition`), `enforce_payment_update` (trigger `trg_enforce_payment_update`), `create_order_from_inquiry`.
  - `orders.delivery_address_id`: constraint `orders_delivery_address_id_fkey`; function `create_order_from_inquiry`.
  - `orders.betk_ref`: constraint `uq_orders_betk_ref` (unique index of the same name); function `create_order_from_inquiry`.
- **B4 applied:** reviews render no buyer name and no buyer location (REG-44, `BETK_UI_SPEC.md` §4.b).
- **`storage.objects`:** the N28 proof covered the `betk` schema only. The `docs` bucket (payment-proof screenshots, seller documents) needs the same seller-NO verdict. Admin signed-URL read stays. Not a new `betk` table.

## Stage C inputs (B5, 2026-09-22)

Not a migration. SQL is Stage C. ADR text is `docs/02-architecture/ADR.md`.

- **ADR-020 / REG-92.** On `seller_orders`: `REVOKE SELECT` from `authenticated` and `anon`, then `GRANT SELECT` of every other column present in that migration to `authenticated`. Leave `delivery_fee` and `total_amount` ungranted. Every query lists columns. `RETURNING` lists columns. A later `ADD COLUMN` grants the new column in the same migration, except those two. P55 derives child total and fee from `payments.amount` and `subtotal`. `revenue_egp`’s writer stays unpinned; the §6.4 definition still binds it.
- **ADR-021.** Buyer UPDATE on `master_orders` is `proof_path` and `transfer_reference` only (three-layer). Drop those two from the `authenticated` UPDATE grant on `payments`. The verification DEFINER trigger copies them onto each deposit row. `anon`’s table-level UPDATE on `payments` stays as the ADR-019 hazard.
- **ADR-022.** Inside `checkout_from_cart`: `master_deposit = round(master_total / 2, 2)`; floor each child half to the piastre; hand leftover piastres out by largest remainder, tiebreak seller-order id ascending; balance = child total − child deposit.
- **ADR-023.** Rate origin is `stores.governorate`. Do not read `store_pickup_addresses` from the INVOKER checkout. Trigger: pickup `governorate` = `stores.governorate`. No new column.
- **ADR-024.** Do not add a courier role, a courier policy, or a definer label function. Do not pick the handoff branch. Admin-initiated = admin RLS read. Automated, only if the courier gate later says so = service-role read.
- **ADR-025.** Replace `create_order_from_inquiry` with INVOKER `checkout_from_cart` in one migration (N27 copies rows; it does not call the old rpc; the three columns `buyer_id`, `delivery_address_id`, `betk_ref` stay). Detach `trg_decrement_stock_on_confirm` before any backfill UPDATE that sets `confirmed`. Decrement only inside checkout; restore on cancel/expiry; not on return; zero on out-of-stock escalation. Rework `enforce_order_transition` in place; do not drop `buyer_id` first. Drop `trg_set_inquiry_converted_order`. Keep `converted_to_order_id` and its NO ACTION FK. No new writer. The cart link is `cart_items.inquiry_id`.

## Phase 07 entry checklist (carry-forwards into Checkout & Orders — schedule into owning tasks, don't lose them)
> **SUPERSEDED as an entry checklist (B7, 2026-09-23).** Phase 07 is retired and owns nothing. “Phase 08” below, where it means delivery and shipment writes, is a **v1 heading (§H)**. v2 shipment INSERT is Phase 11. Tracking writes are Phase 14. RLS for those tables is Phase 08. AC-SEL-14, `create_order_from_inquiry`, and `decrement_stock_on_confirm` in the bullets below are retired mechanics (ADR-025, OD-15). The bullets stay as the record of what landed.
> Pinned 2026-07-22 (Phase 06 / T05 exit gate, Opus). **Phase 07 = Checkout & Orders** (`BETK_PHASES.md`: AC-BUY-6 — an order can be created ONLY from a seller-confirmed inquiry; atomic order + payment record(s); order lifecycle + shipments). These are first-class tasks of the owning Phase-07 build, not surprises. Consumes the Phase 06 CONFIRM→CHECKOUT CONTRACT block below.
- ✅ **REG-09 — `orders` permissive ownership INSERT policy — DONE (Phase 07 / T01, 2026-07-23).** `orders_insert` (`WITH CHECK buyer_id = auth.uid()`) landed, combining with the RESTRICTIVE `orders_phone_gate`; migration `20260723074953`. The **verified-phone transaction gate bites HERE** — T02's `requireVerifiedPhone` (OD-4, ERD §1.2) still gates checkout order-create at the app layer on top of the RLS gate.
- ✅ **Order-set RLS rows (ERD §3 map rows 1–5) — DONE (Phase 07 / T01, REG-48).** `order_items`/`order_status_history`/`order_messages` READ+INSERT + `shipments`/`shipment_tracking_events` READ landed; shipment WRITE deferred to Phase 08 (v1 heading, §H; v2 shipment INSERT is Phase 11, tracking writes are Phase 14, RLS for both tables is Phase 08). **`order_messages` is the ORDER-thread (Phase 07) — NOT the inquiry thread (`inquiry_messages`, Phase 06); do not conflate them.** ✅ **REG-49 CLOSED by T02b (2026-07-23):** `orders` UPDATE + `payments` INSERT/UPDATE landed (migration `20260723140552`, ADR-019 three-layer model — see the ORDER-SET CONTRACT block + the REG-49 register row).
- **SUPERSEDED by ADR-025 (B5, not deleted).** v1 text: Checkout CONSUMES the confirm contract + OWNS the `converted_to_order_id` write. Gate order-create on `inquiries.status = 'confirmed'` (the T02-pinned member; `confirmInquiry` is the only writer of it). On successful order creation, checkout writes `inquiries.converted_to_order_id = <new order id>` (FK `fk_inquiries_order` → `betk.orders`) — this column is **NULL through all of Phase 06 by design**; Phase 07 is its sole writer. `inq_update` (store/admin) is the policy surface, so confirm/convert are seller/admin writes.
- ✅ **AC-BUY-6 — v1 rpc BUILT, then superseded (ADR-018 → ADR-025).** v1 text: atomic order + payment record(s) — DECIDED + BUILT (ADR-018 Accepted, T02b). Checkout is the atomic `create_order_from_inquiry` **SECURITY INVOKER** rpc (search_path pinned, EXECUTE revoked PUBLIC / granted authenticated) — one transaction creates the order + order_items + the two payment rows + commission snapshot + betk_ref, all under the caller's own RLS (the phone gate + `orders_insert` bite naturally; NOT `SECURITY DEFINER`, so no advisor 0029). `delivery_fee` + subtotal/total re-resolved server-side (never client params); betk_ref uniqueness retried in a `BEGIN…EXCEPTION WHEN unique_violation` block (F4).
- **REG-44 — ✅ WON'T-FIX (2026-09-22, B3-FIX), superseded.** v1 text (not deleted): fulfilment NECESSARILY exposes the buyer's name + delivery address to the seller at order time, NOT by broadening the `buyer_profiles` SELECT policy pre-transaction. **v2 operative:** the seller sees no buyer identity and no buyer location (N28). REG-83 attaches the review to the seller order, so a named review would identify that buyer. Reviews render no buyer name or location (B4 input). The row-39 public name/governorate branch is struck. **REG-45 is CLOSED-as-not-a-defect** (no off-platform counterparty-contact affordance exists — see the Communication posture standing fact).
- **⚠️ STANDING (REG-43): `inquiries.last_message_at` is UNMAINTAINED.** No trigger, no app write; it sits at its INSERT default (`= created_at`). If any Phase 07 (retired) / Phase 12 (v1 heading, §H; v2 notifications are Phase 17) surface needs an inquiry's latest activity, DERIVE it from `max(inquiry_messages.sent_at)` (`latestActivityAt`, `messagingRules.ts`) — NEVER read `last_message_at` as truth.

## Phase 06 / T01 — pinned CONFIRM→CHECKOUT CONTRACT + messaging schema facts (T02–T05 + Phase 07 cite THIS)
> Pinned 2026-07-22 (Phase 06 / T01, Opus). Every fact below is cited from `BETK_DATABASE_SCHEMA.sql` + LIVE staging (`pg_enum`/`information_schema`/`pg_policies`/`pg_trigger`/`pg_proc`), never assumed. Do not re-derive — cite this block. **v2 (ADR-025, R-O11):** checkout does not gate on `inquiries.status = 'confirmed'` and does not write `converted_to_order_id`. The sentences below that say otherwise are the v1 measurement, kept in place.
- **CONFIRM member (the confirm→checkout contract).** `inquiry_status` enum = `{open, replied, confirmed, declined, expired}` (schema `BETK_DATABASE_SCHEMA.sql` L52; live `pg_enum` zero-drift). The member that marks a seller-confirmed inquiry = **`'confirmed'`**. `inquiries.status` is `NOT NULL DEFAULT 'open'`. **T02 `confirmInquiry` transitions `status → 'confirmed'`** (seller-only, via `inq_update` = `store_id=my_store_id() OR is_admin()`; buyers cannot UPDATE — integration-proven). **Phase 07 checkout gates order-create on `status='confirmed'`** (AC-BUY-6). This IS the checkout-enablement write.
- **`converted_to_order_id` = Phase-07-write, NEVER Phase 06.** `inquiries.converted_to_order_id` UUID nullable (schema L372; live: nullable YES, no default), FK `fk_inquiries_order` → `betk.orders(id)` (schema L431-433; live-confirmed). Phase 06 NEVER writes it; **Phase-07 checkout owns the write** (set when the confirmed inquiry becomes an order). It stays NULL through all of Phase 06.
- **`last_message_at` ownership = app-layer (no trigger), with a buyer-bump caveat (REG-43).** `inquiries.last_message_at` `TIMESTAMPTZ NOT NULL DEFAULT now()` (schema L375); **NO trigger maintains it** (live: the only inquiries/inquiry_messages triggers are internal FK `RI_ConstraintTrigger`s). Plan = app-layer bump in T02. **CAVEAT:** ERD §3 row 51 restricts inquiries UPDATE to store/admin (`inq_update`), so a BUYER-sent message cannot bump it via RLS — see **REG-43** for the T02 decision (seller-only bump + derive buyer inbox sort from `max(inquiry_messages.sent_at)` / DEFINER trigger / accept). `createInquiry` sets it at INSERT (buyer INSERT allowed).
- **UNREAD mechanism = `inquiry_messages.is_read`, RECEIVER-WRITABLE (REG-42 CLOSED by T02-FIX).** `inquiry_messages.is_read` `BOOLEAN NOT NULL DEFAULT false` (schema L385) is the read-state column. Under the AUTHORIZED ERD §3 row-52 amendment (2026-07-22) + migration `20260722124510`, the **RECEIVER** may flip `is_read` on the OTHER party's messages: **column safety = GRANT** (`authenticated` UPDATE re-scoped to the `is_read` column only — a `body` edit is denied by the grant, error 42501), **row safety = policy** (`inq_msg_read_receipt`, party AND `sender_id <> auth.uid()`; the sender policy `inq_msg_update` is untouched but now column-confined). `markInquiryRead(inquiryId)` (no service-role, idempotent, outsider→not_found) is the write; the queries surface `unreadCount` (inbox) + per-message `isRead`/thread `unreadCount` (thread). T03/T04 render the unread indicator + mark-read-on-view. See REG-42 (CLOSED) + ADR-015.
- **`avg_response_hours` = UNMAINTAINED (re-cited, REG-26-class).** `seller_profiles.avg_response_hours` `NUMERIC(5,2)` (schema L189) has **NO trigger/function** touching it (live `pg_proc` scan of betk*/public normal functions for `%avg_response_hours%` = empty; re-confirms T06-Phase-05). T02 DECISION 2: app-layer recompute on seller reply OR formal defer (mint REG-4x, render static/NULL) — cite formula or state it as engineering choice.
- **RLS surface minted (REG-41, migration `20260722115026`).** inquiries: `inq_buyer` (SELECT, buyer OR store OR admin — pre-existing, untouched), `inq_insert` (buyer INSERT), `inq_update` (store/admin UPDATE). inquiry_messages: `inq_msg_select` + `inq_msg_insert` (thread parties; INSERT pins sender), `inq_msg_update` (sender own). No DELETE either table. No phone gate on any inquiry/message path (`requireActiveUser` is T02's gate, not `requireVerifiedPhone`).

## Phase 07 / T01 — pinned ORDER-SET CONTRACT (T02–T06 + Phase 08 cite THIS) (v1 heading, §H — that Phase 08 was delivery, not v2 Phase 08 schema)
> Pinned 2026-07-23 (Phase 07 / T01, Opus Max). Every fact cited from `BETK_DATABASE_SCHEMA.sql` + LIVE staging (`pg_enum`/`information_schema`/`pg_trigger`/`pg_policies`) — zero-drift verified. Do not re-derive — cite this block. Migration `20260723074953_order_rls_and_conversion_link` (ledger 28→29).
- **`order_status` enum + lifecycle.** `order_status = {pending, confirmed, preparing, dispatched, delivered, cancelled, returned}` (schema L56-59; live zero-drift). **NO `'paid'` member** (payment state lives on `payments.status`, NOT order status — Phase-05 T06 precedent). `orders.status NOT NULL DEFAULT 'pending'` (L416). Terminal-meaning: **cancelled** = order killed (with `orders.cancelled_by cancelled_by_type` + `cancellation_reason` L417-418), **dispatched** = handed to courier / out for delivery, **delivered** = received (`orders.delivered_at` L422). **PHASE 07 owns** (cite BETK_PHASES L46-48): `pending→confirmed` (AC-SEL-14 seller-confirm, FIRES the stock trigger, gated on the admin-confirmed deposit), `confirmed→preparing` (seller order mgmt), `→cancelled` **only from `pending`** (R-O03). **PHASE 08 owns** (v1 heading, §H; v2 owner of dispatched/delivered/returned is Phase 14) (delivery leg, follows the shipment-create ownership; BETK_PHASES L52-54): `→dispatched`, `dispatched→delivered` (sets `delivered_at`, opens the review window), `→returned`. Status transitions are written to `order_status_history` on EVERY transition (AC "status history written").
- **`payments` — EXACT two-row split (C2 §3.8 split-origin; payee/verification authority = OD-8 + ADR-016, supersedes ADR-002; NOT invented).** **CUSTODIAL (OD-8): payee = BETK, not the seller — the buyer pays BETK, which settles to the seller net of a flat platform commission (snapshot + derived-balance rules below).** Columns (schema L550-563): `id, order_id, payment_type, amount NUMERIC(10,2) CHECK(amount>0), method, status DEFAULT 'pending', confirmed_by, confirmed_at, transfer_reference, notes, created_at` + **OD-8 additive `payments.proof_path` VARCHAR NULL (buyer's transfer screenshot; ✅ LANDED by CORRECTION-03, migration `20260723110557`)**; **`uq_payment_type_per_order UNIQUE(order_id, payment_type)`** — this constraint is what caps an order at ≤1 deposit + ≤1 balance row. Enums: `payment_type={deposit, balance}` (L62), `payment_method={instapay, vodafone_cash, orange_cash, cod}` (L63), `payment_status={pending, confirmed, failed, refunded}` (L64). **SHAPE:** exactly TWO rows per order — **row 1** `payment_type='deposit'`, `amount = 50% of total_amount`, `method ∈ {instapay,vodafone_cash,orange_cash}` **paid to BETK's rails (from `admin_settings`, NOT the store's handles)**; **row 2** `payment_type='balance'`, `amount = the remaining 50%`, `method='cod'` **(courier collects and remits to BETK)**. The **50/50 split IS pinned** (C2 §3.8 "50% deposit upfront + 50% COD" + ADR-016); the per-piastre ROUNDING of an odd `total_amount` is NOT spec-pinned → T02 engineering choice (`deposit=round(total/2,2)`, `balance=total−deposit`, so the two always sum to `total_amount`). Status-per-row: both created `'pending'`; deposit→`confirmed` at the **ADMIN** deposit-confirmation gate (OD-8 — verification moved seller→admin, checked against the buyer's uploaded `proof_path`; awaiting-review convention = `proof_path NOT NULL AND status='pending'`); COD balance→`confirmed` on delivery (admin-confirmed after courier remittance). **Commission snapshot (OD-8 §3):** at checkout T02 snapshots `orders.commission_rate` (from `admin_settings.commission_rate_pct`) + `orders.commission_amount = round(commission_rate/100 * subtotal, 2)` onto the order (OD-8 additive columns ✅ LANDED by CORRECTION-03; commission is on `subtotal`, NOT `total_amount`). **Derived-balance rule (OD-8 §6 — NO wallet/ledger table):** the seller's BETK-owed balance is COMPUTED at read time from `orders`+`payments`, never persisted — **seller net = `subtotal − commission_amount`** per order; **approved** = past the return-hold window (`delivered_at + admin_settings.return_hold_hours`, both payment rows `confirmed`), **pending** = confirmed-but-still-in-hold. **NO DB CHECK enforces `deposit+balance = total_amount`** — app-layer invariant (T02). ✅ **`payments` INSERT/UPDATE + `orders` UPDATE policies LANDED by T02b (REG-49 CLOSED, migration `20260723140552`) — the three-layer actor↔column model (ADR-019).**
- **R-O02 BETK-ref.** `orders.betk_ref VARCHAR(25) NOT NULL`, UNIQUE `uq_orders_betk_ref` (schema L407/L423), **NO DB default → APP-GENERATED at checkout (T02)**. Format **`BETK-YYYYMMDD-XXXX`** (UI_SPEC L237, PRD FR-BUY-6, DESIGN_BRIEF L34 — an LTR island in the RTL UI). T02 generates + guarantees uniqueness (retry on the unique violation).
- **`delivery_method` — NOT a separate buyer enum (REG-14 sibling).** `orders.delivery_method` is typed **`delivery_preference`** (schema L412) — the SAME enum as the store-side `StoreDeliveryOptions.modes`. Members `{delivery, pickup, remote}` (L54). **Do NOT create/conflate a distinct buyer-side `delivery_method` type** — checkout writes one of the store's own advertised `delivery_preference` modes (REG-14: `StoreDeliveryOptions.modes` = identical `{delivery,pickup,remote}`, live-verified match).
- **Money columns = NUMERIC(10,2), NO-FLOAT.** `orders.{delivery_fee,subtotal,total_amount}` (L413-415), `order_items.{unit_price,subtotal}` (L442-443), `payments.amount` (L554) — all `NUMERIC(10,2)`. NEVER coerce to JS `number`/`::float` for arithmetic; compute in integer piastres or NUMERIC. Live CHECKs: `chk_order_total (total_amount = subtotal + delivery_fee)` (L424), `chk_order_item_subtotal (subtotal = quantity * unit_price)` (L444), `payments.amount > 0` (L554).
- **THE TRIGGER (trap retired) — `decrement_stock_on_confirm`.** Live def (schema L505-509): `AFTER UPDATE OF status ON betk.orders FOR EACH ROW WHEN (OLD.status IS DISTINCT FROM NEW.status AND NEW.status = 'confirmed')`. It fires ONLY on a transition INTO `'confirmed'` via **UPDATE** — never on INSERT. **Under OD-8 §3.1, every order INSERTs `status='pending'` and reaches `'confirmed'` only via the seller's UPDATE (AC-SEL-14, gated on the admin-confirmed deposit) — so the trigger fires NATURALLY, no special-casing needed.** **HISTORICAL/RETIRED:** the pre-custodial INSERT-pending-then-UPDATE-in-one-transaction workaround (built for the since-retired COD auto-confirm path, R-O04) is OBSOLETE and must not be carried forward — see OD-8 §3.1/§3.2 and `PHASE_07_ORDERS.md` §2.
- **THE TENSION — `converted_to_order_id` writer = PINNED option (a), LANDED in T01.** Checkout is buyer-driven (buyer INSERTs the order) but `inquiries` UPDATE RLS = store/admin only (`inq_update`) → the buyer cannot write `inquiries.converted_to_order_id`. **RESOLUTION (landed):** hardened SECURITY DEFINER `AFTER INSERT ON betk.orders WHEN (NEW.inquiry_id IS NOT NULL)` trigger `trg_set_inquiry_converted_order` → `betk.set_inquiry_converted_order()` copies `NEW.id` onto the source inquiry, once (idempotent via `converted_to_order_id IS NULL` guard — first order wins). Hardened identically to `decrement_stock_on_confirm`: `SET search_path=betk,public` + `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` (definer *trigger*, never API-exposed → NO 0028/0029 advisor; distinct from ADR-012's rejected definer *RPC*). **T02 does NOT write `converted_to_order_id` from the app — the trigger owns it.** Only this derived linkage write is definer; the order INSERT itself stays fully RLS-gated (`orders_insert` + `orders_phone_gate`).
- **RLS surface minted (REG-09 + REG-48, migration `20260723074953`).** `orders`: `orders_insert` (permissive INSERT WITH CHECK `buyer_id=auth.uid()`) — combines with the pre-existing RESTRICTIVE `orders_phone_gate` (verified-phone bites HERE, OD-4). `order_items`/`order_status_history`/`order_messages`: parent-scoped `*_access` (SELECT = buyer own OR store OR admin via the parent order) + `*_insert` (order_items INSERT = buyer-of-parent; status_history INSERT = any party; order_messages INSERT pins `sender_id=auth.uid()` AND party). `order_status_history` UPDATE/DELETE stay blocked by the live append-only RULES `no_update_order_history`/`no_delete_order_history` (DO INSTEAD NOTHING — fire regardless of RLS; a party's UPDATE/DELETE is a silent no-op, NOT an error), so no UPDATE/DELETE policy exists. `order_messages` read-state NOT pinned (row 53 was NOT REG-42-amended; `is_read` unused this phase). `shipments`/`shipment_tracking_events`: **READ-only** `*_access` landed now (parties via parent order / parent shipment→order) — WRITE (store/courier INSERT/UPDATE) DEFERRED to Phase 08 (v1 heading, §H; v2 tracking writes are Phase 14, shipment INSERT is Phase 11, RLS is Phase 08).
- **✅ REG-49 CLOSED by T02b (migration `20260723140552`, ADR-019): `orders` UPDATE + `payments` INSERT/UPDATE landed as the three-layer actor↔column model.** Each write path = **column GRANT** (which columns) + **row policy** (which rows) + **OLD-aware BEFORE trigger** (transition legality). `orders` authenticated UPDATE grant = `{status, cancellation_reason}` (F1 — `cancelled_by`/`confirmed_at` ungranted, trigger-stamped); `orders_update` policy USING+CHECK `buyer OR store OR is_admin()` (SUB-DECISION A keeps `is_admin()` in the POLICY); `enforce_order_transition` gates legality (pending→confirmed store-only w/ AC-SEL-14 confirmed-deposit gate; confirmed→preparing store-only; pending→cancelled buyer-only + stamps `cancelled_by`; F1 cancel-metadata guard). `payments` authenticated UPDATE grant = `{status, proof_path, transfer_reference, confirmed_at, confirmed_by, notes}`; `payments_update` policy `is_admin() OR buyer-of-parent`; `enforce_payment_update` = admin-only status change + F2 pending→confirmed ONLY. `payments_insert` = buyer-of-parent. Commission snapshot via `set_order_commission_snapshot` BEFORE INSERT (F5 raises if `commission_rate_pct` absent). Buyer checkout reads BETK handles + delivery fee via `settings_payment_config_read` (REG-69 literal allow-list). Advisor 8/6/2/4/1 UNCHANGED (+5 attributed perf). See the REG-49 register row for the full pasted `pg_policies` + column-privilege evidence.
- **✅ CORRECTION-03 (2026-07-23, migration `20260723110557`, ledger 29→30) — OD-8 §9 columns + seed LANDED. T02 CITES THESE, not OD-8's spec.** Additive columns (all NULLABLE, app-enforced, live-verified): `payments.proof_path VARCHAR NULL` (OD-8 §5 buyer transfer screenshot in the private `docs` bucket; awaiting-admin-review = `proof_path IS NOT NULL AND status='pending'`), `orders.commission_rate NUMERIC(5,2) NULL` (rate % snapshot, `CHECK BETWEEN 0 AND 100`), `orders.commission_amount NUMERIC(10,2) NULL` (computed snapshot, `CHECK >= 0`). CHECKs live-proven to bite (`-1` amount rejected, `101` rate rejected, valid + boundary 0/100 + NULL accepted). **6 provisional `admin_settings` payment-config rows seeded (admin_settings 11→17):** `commission_rate_pct='0'`, `return_hold_hours='48'`, `delivery_fee_flat_egp='0'`, `betk_instapay_handle=''`, `betk_vodafone_cash=''`, `betk_orange_cash=''`. **⚠️ REG-62 HARD GATE: every value is PROVISIONAL — `0`/`''` are "not yet configured" sentinels, NOT business decisions. The three handle values are DELIBERATELY EMPTY and MUST be set (≥1) before any checkout smoke test** (checkout cannot render payment instructions while empty; BETK earns nothing at `commission_rate_pct=0`). Commission snapshot at checkout: `commission_rate = commission_rate_pct`, `commission_amount = round(commission_rate/100 * subtotal, 2)` (base = `subtotal`, NOT `total_amount`). No RLS policy landed (REG-49 owed by T02).
- **SHIPMENTS decision recorded for the Phase-08 checklist (v1 heading, §H).** T01 landed the buyer/seller READ policies (`shipments_access`, `shipment_tracking_events_access`); Phase 08 owns the store/courier WRITE policies (INSERT/UPDATE) alongside shipment CREATE (v1 heading, §H; those BETK_PHASES lines are the historical plan). v2: RLS Phase 08, shipment INSERT Phase 11, tracking writes Phase 14. The `/orders/[id]` tracking section (FR-BUY-9) renders empty until v2 Phase 14 writes tracking. The old “until Phase 08 populates” sentence meant the v1 delivery heading, not the v2 schema phase.

## ERD §3 specced-but-absent-policy tables — owning-phase map (PINNED, 13 tables)
> **SUPERSEDED as an owner map (B7, 2026-09-23).** Owning-phase cells below that say Phase 07, Phase 08, “Disputes phase”, “Notifications phase”, or “Admin phase” are **v1 headings (§H)** unless a later B7 note says otherwise. v2 (`BETK_PHASES.md` §4.c): shipment and tracking RLS land in Phase 08; shipment rows are first written in Phase 11; tracking events are first written in Phase 14. `dispute_evidence` / `dispute_messages` RLS = Phase 08, first write = Phase 16. `restock_alerts` RLS = Phase 08, first write = Phase 17. `seller_strikes` and `flagged_content` RLS = Phase 08, first write = Phase 18. `whatsapp_templates` RLS = Phase 08, first write = Phase 14. `sessions` stays zero client policies, confirmed in Phase 08.
> Pinned 2026-07-19 (Phase 03 T07 gate) from the REG-25-FIX live `pg_policies` vs `BETK_ERD.md §3` audit. These tables are RLS-**enabled** but ship with **ZERO policies today** (the Phase-01 T08-F1 "policies arrive in the owning phase" class → default-deny for all until then). This is **not a defect** — each row's policies are owned by the phase that first needs the table's writes. Future phase-entry checklists MUST inherit their row: adding the specced policy is a first-class task of that phase, not a surprise. No policy here is Phase 03's to add (Phase 03 is read-only public + the two already-live self-scope write policies).

| # | Table(s) | Owning phase / decision | Note |
|---|---|---|---|
| 1–3 | `order_items`, `order_status_history`, `order_messages` | **Phase 07 — checkout/orders** (order set) | ✅ **CLOSED by Phase 07 / T01 (2026-07-23, REG-48)** — parent-scoped READ + INSERT (order_items INSERT=buyer-of-parent; status_history INSERT=any-party, UPDATE/DELETE blocked by append-only RULES; order_messages INSERT pins sender). Migration `20260723074953`. |
| 4–5 | `shipments`, `shipment_tracking_events` | **Phase 07 — orders/delivery** | ✅ **READ CLOSED by Phase 07 / T01 (2026-07-23, REG-48)** — buyer/seller/admin SELECT via parent order (FR-BUY-9 tracking). ⚠️ **Store/courier WRITE (INSERT/UPDATE) DEFERRED to Phase 08** (v1 heading, §H; shipment CREATE is Phase 08 per the historical BETK_PHASES lines) — v2 owners are RLS Phase 08, shipment INSERT Phase 11, tracking writes Phase 14. |
| 6–7 | `dispute_evidence`, `dispute_messages` | **Disputes phase** | `disputes` itself already has its SELECT policy; the evidence/message children are owed by the disputes build. |
| 8 | `inquiry_messages` | **Messaging phase** | ✅ **CLOSED by Phase 06 / T01 (2026-07-22, REG-41)** — `inq_msg_select`/`inq_msg_insert` (thread parties) + `inq_msg_update` (sender), no DELETE; migration `20260722115026`. (`inquiries` also gained `inq_insert`/`inq_update` this migration.) ⚠️ **REG-43 STANDING:** `inquiries.last_message_at` is UNMAINTAINED (no trigger, no app write) — any Phase 07 (retired) / Phase 12 (v1 heading, §H; v2 notifications are Phase 17) "latest activity" read MUST derive from `max(inquiry_messages.sent_at)`, never `last_message_at`. |
| 9 | `restock_alerts` | **Notifications phase** | Backs R-N06 sold_out restock CTA (the CTA renders in Phase 03; the subscribe/write path + policy are notifications' to build). |
| 10 | `seller_strikes` | **Admin phase** | Admin-write / seller-read own. |
| 11 | `flagged_content` | **Admin phase** | Admin moderation surface. |
| 12 | `whatsapp_templates` | **Admin phase** | Merged tab under Admin → Settings → Notifications (OD-5); not a standalone route. |
| 13 | `sessions` | **Intentionally UNUSED (ADR-010)** | No policy owed — Supabase Auth owns session state; `betk.sessions` is a legacy/unused artifact per ADR-010, not a live surface. |

## Schema deltas to apply in Phase 01 migration (vs C3 baseline)
- migration 003: `CREATE TYPE auth_provider AS ENUM ('phone','google');`
- migration 004 (users): `phone_number` nullable; add `auth_provider NOT NULL DEFAULT 'phone'`, `deleted_at TIMESTAMPTZ NULL`, `anonymized_at TIMESTAMPTZ NULL`.
- Transaction gate: Server Action + RLS WITH CHECK requiring `phone_number IS NOT NULL` on orders/seller_profiles/payouts inserts.
- R-A05 login block also checks `deleted_at IS NULL`.

## Precedents
- Recurring architectural/procedural patterns (RLS classes, migration procedure, compose-only discipline, etc.) are catalogued in `docs/PRECEDENTS.md` — consult it before re-deriving a pattern that's already been established.

## Recent work (last 3 tasks — full detail in the journal)
- **Phase 08 T01 — read-first re-measure (2026-09-26, Cursor Grok 4.7, thinking Max).** DOCS ONLY on `feature/phase-08-schema` cut from `origin/main` `40f5b9cd877a362aff8c541e333093e299126cf6` (PR #68). **None taken.** Next free stays **REG-100** / **OD-22** / **ADR-026**. Plan §0 matches live staging. STOP checks hold: ledger 31=31, orders 7, payments 0, order_items 0, history md5 set equals plan §3. **FLAG for T05:** live money sum is **1100.00**; plan §4.8 prints 1000.00 beside three-at-100 and four-at-200. **Human decision (2026-09-26), verbatim:** "§4.4 = option B: the human creates the rehearsal branch in the dashboard and runs the SQL; the agent prepares the scripts and checks the pasted results. 3f = rehearse M4–M6 only (plan §7.2). M7–M8 are not run on the rehearsal branch." No migration file. **Next (as of T01):** T02. T02 then wrote `docs/03-database/rehearsal/` (not `n27-shape.sql`). The operative next is the standing-facts P08-T02 bullet: human review, then the human runs the rehearsal, then T02-VERIFY.
- **Phase 08 T00 — entry gate, CF-10/11/12, and the Phase 08 pack (2026-09-26, Cursor Grok 4.7 high).** DOCS ONLY on `v2-p08-t00` from `origin/main` `a6c90450ef01ed5714a443ca007beed71f43664f` (PR #67 merge). **Gate:** REG-77 closed the same day. Branch protection contexts are the eight CI names; rulesets `[]`; branch rules `[]`. Ledger versions 31=31, last `20260723140552`; the first 13 remote names still carry the `000N_` prefix (standing fact, not a new gap). Advisors re-measured `2026-09-26T10:21:09.168Z` (security) and `2026-09-26T10:21:10.030Z` (performance): same counts as plan §0.9. **CF-10:** guidelines annotated to v2 Phase 09; review banner (8 mentions, body unchanged); README counts corrected. Design brief untouched. **CF-11:** mechanism pinned in the pack §4; agent execution BLOCKED (no branch-ref argument; cost tools absent in project scope); recommendation is a human-operated branch, M4–M6 only. **CF-12:** REG-62 owns both price-band keys. Took **REG-93..REG-99**. Next free **REG-100** / **OD-22** / **ADR-026**. **Next = human merges the T00 PR; human answers 3f and approves rehearsal cost when that task asks; then T01.**
- **D-1-FIX — correct decision f and record CD-DELTA-6 human decisions (2026-09-25, Cursor Grok 4.7, thinking low).** DOCS ONLY on `v2-d1-cd-delta-6` on top of `3606dbf` (PR #67, unmerged). **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. **Decisions:** a (1) optional `storeHref`; b (1) copy-URL fallback, channels unenumerated; c (3) `audience: "admin"` guard and Phase 14 CI import ban; d shadcn CLI `table`, controlled sort and pagination, one table with inline-axis scroll; e (1) no new dependency; f png, jpeg, webp; g REG-58 and REG-60 with Wave 1, REG-52 and REG-59 recorded not closed; h (2) handoff self-contained, REG-23 stays OPEN; i (1) optional logo callback; j (1) no DataTable allow-list. **G7 correction:** the §5f recommendation was jpeg from the staging `docs` sample; the human decision is png, jpeg, webp from the live upload MIME maps. The staging sample stays, labelled not the contract. **Carry-forwards:** P03 → Phase 09 T00, P30 adoption owner, P58 → Phase 12 T00. **Next = human pastes `CD_DELTA_6_HANDOFF.md` into Claude Design (Wave 1 first), then Phase 08 T00.** Zero `src/`.
- **D-1 — CD-DELTA-6 Claude Design handoff (2026-09-25, Cursor Grok 4.7 high — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-d1-cd-delta-6` from `origin/main` `07fbe95` (PR #66 merge). **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. File: `docs/00-design/CD_DELTA_6_HANDOFF.md` (contracts, no design, token names only). Coverage: 15 items. Status: MISSING 7 (ShareButton, ProofViewer, CartLine, CheckoutSellerSections, RateMatrixEditor, CourierLabelSheet, ChartSeries), PARTIAL 3 (Navigable store identity, DataTable, SellerOrderMoney), PIN 4 (REG-52, REG-58, REG-59, REG-60), N/A — PIN 1 (REG-23). Waves: 1 = ShareButton, Navigable store identity, DataTable, ProofViewer; 2 = CartLine, CheckoutSellerSections; 3 = SellerOrderMoney, RateMatrixEditor, CourierLabelSheet, ChartSeries. Pins have no wave. FLAG, not reconciled: Navigable store identity P03 (UI spec §8 and Phase 09 exit include it; the Phase 09 Stage D line omits it); DataTable P30 (UI spec §8; no Stage D line; page owner is signed Phase 05); ProofViewer P58 (UI spec §8; page map Phase 12; the Phase 12 block line does not name it). Decisions: a storeHref vs wrapper; b share fallback; c label mount guard vs Phase 14 CI; d DataTable CLI base, controlled sort, inline scroll; e no chart dependency unless the human sanctions one; f ProofViewer jpeg from the measured docs bucket; g REG-58 and REG-60 ride with Wave 1, REG-52 and REG-59 stay page composition; h brief stays self-contained; i sidebar logo callback; j generic table defines no forbidden column. Data props 57 (LIVE 19, TARGET 25, GENERIC 5, FLAG 8). **Next = human pastes `CD_DELTA_6_HANDOFF.md` into Claude Design (Wave 1 first), then Phase 08 T00.** Zero `src/`.
- **E-1-FIX — human-reported hosting regions (2026-09-25, Cursor Grok 4.7 — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-e1-fix-regions` from `origin/main` `59d19fb` (PR #65 merge). **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. Values are human-reported; this window did not re-measure them. **V1** database: Frankfurt, Germany (Amazon Web Services), IP geolocation of the database host; dashboard region code unread. **V2** Auth and Storage: **UNKNOWN — human to confirm** (same project, not separately measured). **V3** Vercel functions: `fra1` (Frankfurt), method `regions: ["fra1"]` on production deployment `dpl_DMq7UFDmHXFECYKVEF2BDsC82kRK`; project record has no region field; edge and preview not measured. **V4** PostHog destination: **UNKNOWN — human to confirm** (EU project exists; deployed host not read; code default US host `src/services/posthog.ts:17`). **V5** Sentry: **UNKNOWN — human to confirm** (data-region setting and live DSN hostname not read). **V6** TorvoSMS API host: Frankfurt, Germany (Hostinger), DNS + IP geolocation; message storage **UNKNOWN** (Torvo has not stated). **V7** unchanged UNKNOWN. **Next = human skims `COUNSEL_BRIEF.md` and sends it to counsel, then D-1.**
- **E-1 — legal gate pack (2026-09-23, Cursor Grok 4.7 high — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-e1-legal-gate-pack` from `origin/main` `f41e985` (PR #64; approval commit `ac392d8`, approved content `3bec2ea`). **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. Files: `docs/01-product/legal/E1_LEGAL_GATE_PACK.md`, `COUNSEL_BRIEF.md`, eight drafts under `drafts/`, plus this file, `DEVELOPMENT_JOURNAL.md`, and `PRECEDENTS.md`. No finding. Regions: Supabase DB, Auth, and Storage **UNKNOWN** (`get_project` is not in the MCP catalog); application host **UNKNOWN** (no `vercel.json`); PostHog, Sentry, Resend, TorvoSMS, Google, WhatsApp, and Bosta **UNKNOWN**. REG-77 re-measured: required checks `["RLS smoke (staging)"]`, rulesets `[]`, status stays **OPEN**. **Next = human sends `COUNSEL_BRIEF.md` to Egyptian counsel, then D-1.**
- **Stage C-FIX — record D1–D4 on the unsigned schema-delta plan (2026-09-23, Cursor Grok 4.7 high — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-stage-c-schema-delta`. **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. D1: M5 cancels the five non-cancelled zombies (`cancelled`, `cancelled_by = system`), one history INSERT each (`changed_by` NULL, `changed_by_type = system`, notes cite N27), scoped disable of `trg_enforce_order_transition` only, original 7 md5s unchanged and exactly 5 new rows. D2: admin supplies `refunded_subtotal`; CHECK bounds it. D3: `submit_seller_application` argument list unchanged in Phase 08; Phase 09 owns the caller. D4: nine empty keys, each consumer fails closed. `checkout_from_cart` EXECUTE stays revoked until M7. Approval block **UNSIGNED**. **Next = human signature.** Zero `src/` / zero SQL applied.
- **Stage C — v2 schema-delta plan (2026-09-23, Cursor Grok 4.7 high — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-stage-c-schema-delta` from `origin/main` `c88465a`. Register re-read: highest REG-92, no REG-93. **None taken.** Next free stays **REG-93**, **OD-22**, **ADR-026**. Plan is `docs/03-database/BETK_V2_SCHEMA_DELTA_PLAN.md`. Approval block is **UNSIGNED**. Live 43 re-measured. The 7 orders are mapped, not deleted. The two `confirmed` rows are an unresolved decision. **Next = human signature.** Phase 08 does not execute until that block is signed. Zero `src/` / zero SQL applied. **Superseded in place by C-FIX** on the status of those five rows (D1). The rest of this bullet is the draft that C-FIX amended.
- **B7 — SESSION_CONTEXT + PRECEDENTS + register reconciliation (2026-09-23, Cursor Grok 4.7 high — satisfies the former Opus row; CF-9).** DOCS ONLY on `v2-b7-reconcile` from `origin/main` (B6 merged), plus `.cursor/rules/00-betk-core.mdc`. Register re-read: 91 rows, only gap REG-65, highest REG-92, no REG-93. Wrote the REG-65 row (open, Phase 09). Superseded REG-57 (Phase 07 owns nothing). Closed REG-87. Next free stays **REG-93**, **OD-22**, **ADR-026**. None taken. **Next = Phase 08** after this merges and the §0 entry criteria hold. Time-driven choice is in `BETK_PHASES.md` §8: earlier phases write in-app `notifications` rows; Phase 17 adds the center and SMS. Zero `src/` / zero SQL.
- **B6 — rewrite BETK_PHASES for v2 (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b6-phases` from `origin/main` (B5 merged). Register re-read: next free **REG-93**, none taken. No new OD. No ADR. Numbering: **01–06 signed, 07 retired and never reused, 08–20 the v2 build.** Phase 08 executes Stage C, including the four B5 carry-forwards (stock-trigger detach before a `confirmed` backfill; the new `checkout_from_cart` inserts `delivery_fee` and `total_amount` from locals and never SELECTs or RETURNs them; ADR-023 on both pickup writes and `stores.governorate` updates; `converted_to_order_id` stays with no writer). Salvage: `requireAdmin` → Phase 12, buyer address query/action → Phase 11, phone-gate unit test rewire → Phase 11, `modlog_admin_insert` already live. REG-92 is a lint or a runtime test, not a typecheck. **Next = B7.** Zero `src/` / zero SQL.
- **B5 — ADR disposition for v2 (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b5-adr` from `origin/main` (B4 merged, `b0f5285`). Register re-read: next free **ADR-020**, next free **REG-92**. Took **ADR-020..ADR-025** and **REG-92**. Next free **ADR-026**, **REG-93**. No new OD. ADR-001..019 each have one verdict. A–F recommendations accepted. REG-89 and REG-91 closed. REG-78 principle closed, mechanism open. REG-90 grant is ADR-020. Zero `src/` / zero SQL.
- **B4-FIX2 (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b4-ui-spec`. Register re-read: next free **REG-92**, none taken. No ADR. No new OD. **OD-21 corrected in place to 79.** P78 `/auth/phone` (OD-4, FR-AUTH-4) and P79 `/blocked` (R-A05, OD-2) admitted. Auth band is 5. 26/26 built `page.tsx` files map to one P-number. REG-79 surface is `/auth/phone` and `/auth/verify`; trigger stays OPEN. PRECEDENTS: freeze counts reconcile against built `page.tsx` files (REG-67). Zero `src/` / zero SQL.
- **B4-FIX (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b4-ui-spec`. Register re-read: next free **REG-92**, none taken. No ADR. **REG-90 at the DB layer:** `refunded_subtotal` is the goods portion (fee component stays on `payments`, seller SELECT none); `revenue_egp` is subtotal-based with the writer unpinned; payout INSERT is capped at the derived available net. FR-SEL-17 still computable under seller RLS. **REG-67 map of `origin/main` `page.tsx`:** 24 map to a P-number; `/auth/phone` and `/blocked` do not. STOP. Corrected count if admitted: **79**. OD-21 stays **77**. **REG-91** stays open; B5 candidate recorded (INVOKER origin = public `stores.governorate`). Zero `src/` / zero SQL.
- **B4 — rewrite BETK_UI_SPEC for v2 and freeze the page count (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b4-ui-spec` from `origin/main` (B3 merged). Counting rule written first: one route pattern per role, locales once, tabs/modals/drawers/steps do not count. v1 measured **65** under that rule (59 was 60 headings minus the WhatsApp tab). **OD-21 = 77.** Took **REG-90** (closed: seller sees subtotal, commission, net only) and **REG-91** (open: buyer-safe delivery total). Next free **OD-22**, **REG-92**. Gap list `BETK_UI_SPEC.md` §8. REG-79 and REG-85 left OPEN. Zero `src/` / zero SQL. Next: B5 (ADR-020+) and B6 (phases cite the UI spec).
- **B3-FIX — audit of B3 (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b3-erd`. Live `information_schema` column scan for buyer name, phone, address, governorate, and city. Every seller verdict under the target map is NO. REG-44 closed WON'T-FIX (N28 + REG-83); row-39 public name/governorate branch struck. FR-SEL-17 was not computable under seller RLS; `seller_orders` gains `balance_confirmed_at`, `refunded_amount`, `payout_eligible_at` and still has no proof columns. The three column drops are not required; `buyer_id`, `delivery_address_id`, and `betk_ref` stay. Dependency list is in `BETK_ERD.md` §4.1. REG-78 candidate revised: admin label is the admin’s own RLS read; no definer; no service role on that path. REG-89 candidate recorded; REG-89 stays open. Register re-read: next free REG-90, none taken. No ADR. Zero `src/` / zero SQL. Next: B4 (pages) and B5 (ADRs).
- **B3 — rewrite BETK_ERD for v2 and freeze the table count (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b3-erd` from `origin/main` `6b95bb8` (PR #58 B2-FIX merged). Live `pg_tables`: **43** (`betk` 41 + `betk_analytics` 2). Target **51** under **OD-20** (0 dropped, `orders` renamed to `seller_orders`, 8 new). Pages unfrozen. REG-82/83/84 closed as pins; REG-82 propagated to R-C07, AC-CART-7, MVP_SCOPE §3.5. REG-83/84 contradicted OPEN lines, so those PRD rows were amended. Took **REG-88** and **REG-89** (next free was REG-88). REG-78 resolved (no courier RLS principal). N22 validated. N27 feasible (history rules + NO ACTION FK measured; 7 rows pasted in the ERD). `enforce_order_transition` and `decrement_stock_on_confirm` marked REWORK. ADR candidates listed, not written (next free ADR-020). PRECEDENTS: no-courier-RLS-principal. Zero `src/` / zero SQL. Next: B4 (pages) and B5 (ADRs). Stage C writes migrations.
- **B2-FIX (2026-09-22, Cursor Grok 4.7 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b2-prd-fix` from `origin/main` `0341098` (PR #57 B2 merged). REG-80 **CLOSED** (scope owner): boosts IN as retained v1 scope; listed codes PENDING → RETAINED; no new boost requirement. R-O03 cites journeys §5.2; that citation **closes REG-73** (deposit-confirmed case is in the quote). Register re-read: next free REG-81; took **REG-81..REG-87**; F-MODE left (b) on the B3 entry checklist. Next free REG-88. Zero `src/` / zero SQL. Next: B3 (ERD).
- **B2 — rewrite BETK_PRD for v2 (2026-09-19, Cursor Grok 4.6 — architecture/docs, typically Opus).** DOCS ONLY on `v2-b2-prd` from `origin/main` `7137955`. Rewrote `docs/01-product/BETK_PRD.md`. Occupied R-A01–06 / R-S01–09 / R-L01–10 / R-O01–10 / R-R01–07 / R-D01–06 / R-N01–06 / R-M01–06 / R-B01–05; FR-PUB-1–5 / FR-AUTH-1–3 / FR-BUY-1–13 / FR-SEL-1–22 / FR-ADM-1–17; spelled-out AC-BUY-6, AC-SEL-14, AC-ADM-9, AC-AUTH-2. **Took** R-A07, R-S10, R-L11–22, R-O11–29, R-N07–08, R-M07–08, R-C01–07, R-Q01–08, R-E01–05, R-F01–05, R-U01–05, R-G01–08, R-K01–09, R-V01–04; FR-AUTH-4, FR-ADM-18–21, FR-CART/CHK/PAY/QTE/SLA/ESC/STK/RET/AGR/COU/VIS/CLO/COM/CAT-1(+PAY-2). AC-BUY-6 and AC-SEL-14 **RETIRED**. REG-79 written OPEN. REG-78 behaviour-not-principal. REG-80 PENDING in place. Counts still UNFROZEN. No REG minted (next free still REG-81). PRECEDENTS: capability-FRs-not-page-FRs. Zero `src/` / zero SQL. Next: B3 (ERD).
- **B1-FIX (2026-09-19, Cursor Grok 4.6 — architecture/docs).** DOCS ONLY on `v2-b1-mvp-scope`. Confirmed OD-9..OD-19 citations in `BETK_MVP_SCOPE.md` §4.2. Marked SESSION_CONTEXT communication posture + PRECEDENTS no-off-platform row **SUPERSEDED in place** (v1 self-delivering seller IS the courier / reads the label → v2 seller sees no buyer identity or location). Re-read register: next free **REG-78**; took **REG-78, REG-79, REG-80**; header now REG-01..REG-80 / next free REG-81. Zero `src/` / zero SQL. Next: B2 (must not invent REG-79). **B2 then did not invent.**

## Update template (append per session)
```
Date | Model | Phase | Task | Files changed | Issues | Next
```
