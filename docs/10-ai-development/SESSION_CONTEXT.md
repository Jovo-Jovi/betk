# SESSION_CONTEXT.md
> Running state Claude reads at the start of every Cursor session to restore context without relying on chat memory. Update at the end of each session. (Dev OS Step 7 / Memory Guard.)

## Project state
- **BETK is Arabic-first RTL marketplace (Egypt) — Next.js 15 + Supabase + Tailwind/shadcn + TS strict + Zod.** Phases 01–06 are COMPLETE, MERGED, and SIGNED OFF on `main`: Phase 01 — Foundation (signed off 2026-06-23) · Phase 02 — Auth (signed off 2026-06-30) · OD-7 Bilingual AR/EN + Theme (merged PR #34 @ `648ed4f`, 2026-07-02) · Phase 03 — Catalog & Discovery (merged PR #43 @ `13efd38`, 2026-07-19) · Phase 04 — Seller Onboarding & Store Management (merged PR #44 @ `a80a454`, 2026-07-21) · PERF-01/02 batch (merged PR #45 @ `d3dc150`, 2026-07-21) · Phase 05 — Listings & Inventory (merged PR #46 @ `acb3f1f`, 2026-07-22) · Phase 06 — Messaging & Inquiries (merged PR #47 @ `881832f`, 2026-07-22). **NEXT: Phase 07 — Checkout & Orders**, branch `feature/phase-07-orders`, pack `docs/10-ai-development/phase-packs/PHASE_07_ORDERS.md` — T00 (2026-07-22) closed out Phase 06 + reorganized this file; **T01 (DB & RLS foundation + contract pinning, Opus Max) is DONE (2026-07-23)** — REG-09 + REG-48 closed, REG-49 opened (owed by T02), the ORDER-SET CONTRACT block is pinned below, migration `20260723074953` landed (ledger 29). **T02 (checkout write layer — atomic order + items + two payments, Opus) is next**; it consumes the ORDER-SET CONTRACT + owns REG-49. HOLD until then. Full per-task narrative history for every phase/task above → `docs/12-changelog/DEVELOPMENT_JOURNAL.md` (append-only log, one entry per task). Recurring patterns/precedents → `docs/PRECEDENTS.md`.

## Standing facts
- **Stack:** Next.js 15 · Supabase · Tailwind/shadcn · TS strict · Zod · Resend · PostHog · Sentry · Vercel.
- **ADR-010 (auth model) = Model A (GoTrue-canonical).** GoTrue owns OTP/OAuth + sessions; `betk.users` = find-or-create mirror of `auth.users.id`. `betk.otp_tokens` → app-layer ≤5-attempt limiter (T02, never holds raw OTP); `betk.sessions` → UNUSED in MVP. AC-AUTH-2: GoTrue covers raw-OTP-never-persisted + expired/used-rejected + 60s expiry + session creation; ONLY "≤5 attempts/token" needs app-layer work (T02 verify action, backed by otp_tokens.attempt_count + chk_otp_attempts). Full record: docs/02-architecture/ADR.md.
- **Docs baseline:** full Dev OS set in `docs/`. UI Spec = 59 pages (verified 2026-07-16, R4 — was miscounted "56"/"61" in places; see `BETK_UI_SPEC.md` count-reconciliation note); schema = 43 tables (`betk` 41 + `betk_analytics` 2).
- **Guard suite = 4** (`check-service-import` · `check-zod-coverage` · `check-no-hardcoded-arabic` · `check-i18n-parity`, the last closed REG-27). Latest known-green counts (Phase 07 / T01, 2026-07-23): zod-coverage 30 files · hardcoded-arabic 58 files · i18n-parity **736/736** leaf keys, 0 orphans · unit **120/120** (8 files) · full staging integration **188 passed + 2 skipped** (25 files — +`order.rls.test.ts`; the 2 skips = smoke A5 + the order-set opt-in append-only, both structurally guaranteed) · build **44 routes** both locales.
- **Migration ledger = 29/29** (remote↔local 1:1; last `20260723074953_order_rls_and_conversion_link`). Security advisor baseline = **8** rls-no-policy INFO / 6 search_path WARN / 2 extension WARN / 4 sec-definer-exec WARN / 1 leaked-password WARN (Phase 07 / T01 dropped rls-no-policy 13→8 — the 5 order-set tables gained policies; the new `set_inquiry_converted_order` DEFINER trigger is search_path-pinned + EXECUTE-revoked so it adds ZERO finding) — STATE this baseline at the start of every future migration task and re-state the new baseline after applying.
- **Storage posture:** `docs` bucket = PRIVATE, own-prefix INSERT, no UPDATE/DELETE (R-S08 retention — a new upload is a new path, the prior object persists). `media` bucket = PUBLIC (serves public URLs), own-prefix INSERT/UPDATE, `.list()` DENIED to non-owners (T01-FIX), NO DELETE policy anywhere (ADR-013/FLAG-1 — row-remove only; the storage object is retained by design).
- **REG-40 (standing rule, load-bearing):** on any ISR route under `[locale]` whose `generateStaticParams` returns `[]`, thread `locale` EXPLICITLY on every render path (`getTranslations({locale})`, `locale` as a prop into Suspense-wrapped children, `locale=` on server `<Link>`) — next-intl's `setRequestLocale` cache-store is not reliably resolved across independently-rendered scopes on the on-demand/non-default-locale path (symptom: 500 on `/en/…`, 200 on `/…`). Full rule: `docs/02-architecture/CACHING_STRATEGY.md §7`.
- **Design system (Claude Design):** the visual system lives in `components/ui` + `components/shared`, OWNED by Claude Design (brief: `docs/00-design/BETK_DESIGN_BRIEF.md`); Cursor composes + wires data, NEVER restyles. Current kit (as of CD-DELTA-4, 2026-07-21): shared = 37 files, `ui/*` = 14 files. Full land-by-land history (DS-LAND/DS-GATE/CD-DELTA-1..4) → journal.
- **SMS/OTP delivery:** production = TorvoSMS via the Supabase Send SMS Hook → Edge Function `send-sms-hook`; registered sender ID `3MS EGY`. **REG-12 + REG-13 are CLOSED (2026-07-22)** — handset delivery confirmed on a physical device AND operator-level sender-ID registration confirmed propagated; the hard pre-launch SMS gate is RETIRED (standing note: if the sender ID ever changes/expires, REG-13 reopens). Full provider/secrets history → journal (this session's archive-fold entry).
- **Staging config:** Supabase Auth provider settings (OTP/OAuth toggles, test phone numbers, OTP length/expiry) are a manual Dashboard action, human-confirmed applied; `supabase/config.toml [auth]` is the version-controlled source-of-truth for local dev; any drift between them is an ops issue, not a code issue.
- **Effort rule (pinned 2026-07-22, Phase 07 / T00):** **Opus+Max** = exit gates, RLS/policy design, ADR decisions, read-first audits · **Opus+High** = write layers, migrations, security review · **Sonnet+Medium** = compose-only UI on a settled query layer · **Sonnet+High** = delicate docs/preservation work · **Sonnet+Low** = mechanical housekeeping. Every future pack task row carries model + thinking.
- **Execution model:** skills auto-attach via `.cursor/rules/*.mdc` (core always-on + glob-scoped DB/actions/ui/tests + agent-requested security/UI reviews). You pick the model only; the core rule flags a model mismatch. Ref: `HOW_RULES_AUTORUN.md`.
- **Communication posture (load-bearing, human posture 2026-07-23):** **ALL buyer↔seller communication is IN-APP only** (the `inquiries` thread). There is **NO off-platform contact affordance, ever** — a doc/UI line naming an external channel (WhatsApp/FB/Instagram/etc.) is ONE of exactly three things: a **share** (channel-agnostic device share sheet on a public link — listing/storefront), a **BETK→user notification** (WhatsApp templates under Admin → Settings, OD-5; delivery, not conversation), or **BETK support** (BETK→user support channel, e.g. a support WhatsApp deep-link on the footer, REG-50) — **never counterparty contact.** Identity/address DO cross at fulfilment, but only via the **courier/shipping label**: the BETK-generated label prints the buyer's name + phone + address for delivery, and per **REG-14 the `delivery` mode covers self-delivery — so a self-delivering seller IS the courier** and legitimately reads the label. This is the standing basis for REG-45 (closed-as-not-a-defect) and `docs/PRECEDENTS.md`.

## Frozen decisions (MVP Freeze Sheet — respect exactly)
- OD-1 low-stock DERIVED (no inventory_alerts table). Schema: NO.
- OD-2 account = DEACTIVATE-only. Schema: YES — add `users.deleted_at`, `users.anonymized_at` (nullable; no MVP behavior beyond deactivation).
- OD-3 broadcast = no campaign entity (fan-out to notifications). Schema: NO.
- OD-4 Google OAuth IN. Schema: YES — `users.phone_number` nullable+UNIQUE; add enum `auth_provider` + `users.auth_provider`. Phone-OTP + Google OAuth sign-in; **verified phone required before transacting** (checkout/become-seller/payout). R-A01 amended.
- OD-5 sessions UI OUT; WhatsApp templates under Admin → Settings → Notifications tab. Schema: NO.
- OD-6 table count = 43 (authoritative inventory in BETK_ERD.md §1.1). Schema: NO.
- OD-7 bilingual AR/EN (next-intl, AR default unprefixed, EN under /en) + light/dark theme (next-themes) — presentation-layer only, *_ar/*_en columns + localizedName COALESCE, no translation service, no new content columns. Schema: NO. (PR #34)
- OD-8 **custodial payments & platform commission (SIGNED 2026-07-23; ADR-016 supersedes ADR-002).** The buyer pays **BETK** (not the seller); BETK settles to the seller net of a flat percentage commission on `subtotal`, snapshotted at order creation. Deposit verification moves seller→**ADMIN** (against a buyer-uploaded transfer screenshot); order acceptance stays the **seller's** act. Seller balance is **DERIVED** (no wallet/ledger table). R-O04 (COD auto-confirm) retired; no pure-COD path (§3.2). Schema: **YES = 3 additive columns** (`payments.proof_path`, `orders.commission_rate`, `orders.commission_amount`) — owed by Phase-07 T02's migration. **Table count 43 HOLDS** (no new table); **page count 59 HOLDS** (no new page). Full record: `docs/10-ai-development/OD8_CUSTODIAL_PAYMENTS.md` + ADR-016.

## Consolidated issue register (REG-01..REG-61; next free = REG-62) — canonical, adopted 2026-07-16 (R4)
> **Single namespace going forward.** Supersedes the scattered `open-issue #n` / T14-findings numbering (full per-task narrative history → `docs/12-changelog/DEVELOPMENT_JOURNAL.md`); "Old ref" cross-references the pre-register numbering for traceability. Refer to REG-nn for open work from here on. Adopted verbatim from `docs/10-ai-development/REPAIRS/BETK_REPAIR_PLAN.md`, statuses reconciled to what's actually run at this point (R1/R2/R3/R4 done; R5 done 2026-07-16 → REG-08 closed).

| REG | Item | Old ref | Owner / gate | Status |
|---|---|---|---|---|
| REG-01 | Migration ledger mismatch (local `20260701021800` vs remote `20260630232657`) | audit gap #8 | **R1** | ✅ Closed by R1 (2026-07-16) |
| REG-02 | `decrement_stock_on_confirm` owed to source + live | #4 | **R2** | ✅ Closed by R2 (2026-07-16) |
| REG-03 | pg_cron Cairo/UTC offset (3 daily jobs) | T14 finding | **R3** | ✅ Closed by R3 (2026-07-16) |
| REG-04 | ADR numbering collision (OD-7 "ADR-002" → **ADR-011**) | OD-7 carry | **R4** | ✅ Closed by R4 (2026-07-16) |
| REG-05 | MVP_SCOPE §6 vs §8 citation inconsistency | audit gap #12 | **R4** | ✅ Closed by R4 (2026-07-16) |
| REG-06 | Stale current-counts in docs (policies 34→39, indexes 34→41) | T14 corrections | **R4** | ✅ Closed by R4 (2026-07-16) |
| REG-07 | Empty `docs/05-features/`, `docs/11-decisions/` | audit gaps #9–10 | **R4** | ✅ Closed by R4 (2026-07-16) |
| REG-08 | rls-smoke opt-in on migration PRs | standing rec | **R5** | ✅ Closed by R5 (2026-07-16) — conditionally-required on migration-touching PRs |
| REG-09 | `orders` permissive ownership INSERT policy missing | #1 | Phase 07 checkout task | ✅ **Closed by Phase 07 / T01 (2026-07-23, Opus Max).** Migration `20260723074953_order_rls_and_conversion_link` added permissive `orders_insert` (`WITH CHECK buyer_id = auth.uid()`), ERD §3 row-54 verbatim; it COMBINES with the pre-existing RESTRICTIVE `orders_phone_gate` (OD-4 verified-phone gate bites here). Integration-proven BOTH halves (`order.rls.test.ts`): phone-verified buyer INSERTs own order (PASS), phone-NULL user DENIED (gate finally bites), cross-user `buyer_id` DENIED. Flipped the RLS-smoke **A4b FINDING→PASS** (+ retired the stale F2). Backfilled into `BETK_DATABASE_SCHEMA.sql`; advisor unchanged by this policy (references only `auth.uid()`). |
| REG-10 | `seller_profiles` permissive ownership INSERT policy missing | #1 | Phase 04 become-seller task | ✅ Closed by Phase 04 / T01 (2026-07-19, Opus 4.8) — migration `20260719133011_seller_ownership_insert_rls` added permissive `sp_insert` (`WITH CHECK (id = auth.uid())`), ERD §3-verbatim. Integration-proven both directions: phone-verified user INSERTs own row (sp_insert + RESTRICTIVE `seller_profiles_phone_gate` COMBINE), phone-NULL user DENIED (gate still bites), cross-user id DENIED. Backfilled into `BETK_DATABASE_SCHEMA.sql`. |
| REG-11 | Live Google OAuth consent E2E (real PKCE round-trip) | #13 | Pre-launch Playwright gate | Open (owned) |
| REG-12 | Handset SMS delivery + sender `3MS EGY` on device | SMS section | **HARD pre-launch gate** | ✅ **CLOSED (2026-07-22, human-supplied evidence).** Real OTP delivered to a physical handset; message rendered with sender "3MS EGY" (TorvoSMS log: 2 sends Jun 30, status `sent`, 58 chars/1 seg Arabic route; human confirmed on-device display). **The hard pre-launch SMS gate is retired.** |
| REG-13 | NTRA/operator sender-ID registration | memory item | External clock (was: start NOW, don't wait for pre-launch) | ✅ **CLOSED-WITH-EVIDENCE (2026-07-22, human-supplied evidence).** Sender ID "3MS EGY" active at the aggregator (created Jun 29 2026) AND operator-rendered verbatim on device — registration provably propagated. **Standing note: if the sender ID ever changes or shows an expiry, this REOPENS** — re-verify on any provider change. |
| REG-14 | `StoreDeliveryOptions.modes` mirrors store-side enum | #5 | Verify before Phase 04/07 consume | ✅ Closed-with-evidence by Phase 04 / T01 (2026-07-19, Opus 4.8) — **MATCH**. Live `betk.delivery_preference = {delivery, pickup, remote}` (schema source L54 agrees); `src/types/jsonb.ts` `StoreDeliveryOptions.modes = "delivery"|"pickup"|"remote"` — identical, no drift, no type change. The `self_deliver/bosta/pickup/remote` 4-value wording (Phase-01 T12 note → `BETK_UI_SPEC.md §3` L336/L384) is a **product/UI-doc description** (self-deliver + Bosta are both "delivery" fulfillment), NOT a DB enum — recorded as a doc-vs-schema divergence for the T08 docs sync; schema/type untouched. **Consumer correction:** T07's "4 mode toggles" line is corrected to **3 toggles** ({delivery, pickup, remote}) — live-verified. **T08 (2026-07-20): fully closed with BOTH evidence legs — type-level MATCH (T01) + runtime EXACT-shape JSONB round-trip (T07 `store.settings` integration: 3-mode set, no extra/missing keys). The doc-vs-schema divergence is now corrected in `BETK_UI_SPEC.md` §3 (Seller Onboarding step 4 + Delivery Settings) and the pack's T07 line; the "distinguishing courier" 4th-mode product option was declined-by-default (would need an OD amendment).** |
| REG-15 | `title_en` required-in-form decision (`title` required AR+EN at the Zod/listing-form layer; `title_en` stays nullable in DB) | OD-7 note | **Phase 05 listing form task** (corrected at Phase 04 / T08 — the create/edit-listing form is Phase 05, NOT Phase 04; the Phase-04 seller build has no listing-title field) | ✅ **FULLY CLOSED (schema half T02 + form half T04), T06-verified (2026-07-22, Opus).** **SCHEMA HALF (T02):** `createListingSchema`/`updateListingSchema` in `src/validations/listings.ts` require BOTH `titleAr` AND `titleEn` (`z.string().trim().min(1).max(80)` each); unit-proven (`listingRules.unit.test.ts`). **FORM HALF (T04):** `ListingForm.tsx` renders BOTH titles as REQUIRED inputs and blocks save when either is empty (client `validate()` L185-186). `listings.title_en` stays NULLABLE in DB — NO schema change; publish gate R-L03 keys on `title_ar` only. **CLEANUP NOTE (FLAG-2, T06 — no rework, recorded only):** the form's client `validate()` HAND-MIRRORS the T02 Zod shape, it does NOT derive it via `.pick(...)`/`.extend(...)` — there is no shared client-schema-from-server-schema helper in this repo to pick from (same manual-mirror precedent as `StoreProfileForm`/`OnboardingWizard`). The `src/validations/listings.ts` header comment claiming the form "mirrors this schema via `.pick(...)`" is therefore inaccurate — a doc-comment fix (and, optionally, a genuine shared client-schema helper) is a low-priority cleanup, not a gate blocker. |
| REG-16 | BL-01 locale-preserving Server-Action redirects | OD-7 carry | Optional cleanup, any Sonnet window | Open (optional) |
| REG-17 | BL-02 `errorAr` → `errorMessage` rename | OD-7 carry | Optional cleanup (pairs with REG-16) | Open (optional) |
| REG-18 | StatusBadge `flag`-domain labels (partial map) | OD-7 carry | **Claude Design decision** before any admin moderation badge renders | **DECIDED — keep-partial (applied at CD-DELTA-4 LAND 2026-07-21, brief §5.5; revisit at the admin phase when a moderation-flag badge first renders).** |
| REG-19 | `betk.users` writes stay service-role (no self-UPDATE policy until grant re-scoped) | #10 | Standing pattern — revisit only deliberately | Standing |
| REG-20 | `NODE_ENV`-conditional env requiredness | deploy TODO | Phase 14 / first prod deploy | Open (owned) |
| REG-21 | GitHub Actions version bumps (Node 20 deprecations) | CI housekeeping | Any convenient window | Open (housekeeping) |
| REG-22 | Live OAuth + config.toml vs dashboard drift is ops-owned | staging-config note | Ops awareness item | Standing. **Staging-config note folded at Phase 05 / T06 (2026-07-22):** GoTrue `auth.admin.updateUserById(..., { phone })` 500s on this staging project (likely an auth-provider/SMS config gap) — surfaced by the `scripts/dev/set-test-role.ts` dev harness, which WARNS + CONTINUES on that specific failure. Impact is nil for the app: the `betk.users.phone_number` mirror write (the read-path the app + verified-phone gate actually consume) succeeds; only GoTrue's own `auth.users.phone` field is left unset in that dev flow. Ops-awareness only, no code/gate impact. |
| REG-23 | Design-repo remote + pinned SHA still unresolved for the brief's living-reference pointer (`<DESIGN-REPO-URL>`/`<SHA>` placeholder) | BRIEF-RESOLVE note (2026-07-16) | Design decision — asked, not invented | Open (owned) |
| REG-24 | Direct `supabase` CLI binary aborts on `config.toml` hook-secret validation (missing `SEND_SMS_HOOK_SECRET`) — MCP `apply_migration`/`execute_sql` is the confirmed working path for DB writes/reads in this env | R2/R3 tooling note | Ops/tooling awareness | Standing |
| REG-25 | `listings_public` RLS (`status='active'`) hid a genuinely `sold_out`-status listing that FR-PUB-4/R-N06 require to STAY publicly visible with a restock CTA (the live `decrement_stock_on_confirm` trigger flips the enum at `stock_qty=0`) | T05 FINDING 2 | REG-25-FIX (dedicated RLS review/policy-fix task) | ✅ Closed by REG-25-FIX (2026-07-19, Opus 4.8) — migration `20260718230302_listings_public_sold_out_visibility` amended `listings_public` + child `listing_images_public`/`listing_tags_public` to `status IN ('active','sold_out')` (verbatim-consistent, so a sold_out DETAIL page keeps its gallery+tags); browse grids stay `active`-only via the query layer (UI_SPEC L73/85/97/121 — detail-only, not invented); draft/paused/removed/soft-deleted still hidden; both-direction integration tests green; schema backfilled (incl. the never-backfilled T01-FIX child block) |
| REG-26 | `view_count` increment mechanism (FR-PUB-4) is UNPINNED — no trigger/function touches `listings.view_count` and anon has no UPDATE path, so the listing-detail page ships WITHOUT incrementing it (CONFIRMED T05, not improvised — no service-role reach-around) | T05 (view_count confirmation) | Owning-phase decision — a dedicated increment mechanism (RPC / seller-analytics path) | Open (owned). **RESTATED at Phase 05 / T06 (2026-07-22):** the Listings-Management "views" column renders the raw static `view_count` as-is (T03) — still no increment mechanism added this phase; stays OPEN for a later analytics/seller-facing phase to spec. |
| REG-27 | i18n message-catalog parity guard script is MISSING — `messages/{ar,en}.json` key parity is verified ad-hoc per task (T03/T04 ran one-off recursive key-diffs) but there is no committed guard beside the other 3 (`check-service-import`/`check-zod-coverage`/`check-no-hardcoded-arabic`) | T03/T04 i18n parity checks | Tooling task — add a 4th guard script + wire into CI | ✅ Closed by PRE-GATE-FIX (2026-07-19, Opus 4.8) — new `scripts/check-i18n-parity.mjs` (pure-Node leaf-key deep-walk of ar.json vs en.json; reports totals + orphans each direction; exit 1 on any orphan OR leaf-count mismatch). Wired as **Guard D** in the CI `guards` job AND a new local `pnpm guards` aggregate (all 4). Current parity **305/305 leaf keys, 0 orphans**. **The guard suite is now 4** (`check-service-import`/`check-zod-coverage`/`check-no-hardcoded-arabic`/`check-i18n-parity`). |
| REG-28 | 404 chrome drift (NF-01) — the top-level `[...rest]` catch-all hard-404 renders Next's bare `__next_error__` shell (no `<html lang/dir>`, no chrome, generic title), contradicting BL-01-FIX's 2026-07-02 runtime-verified locale-aware/chrome-preserving 404 claim (likely an intervening-track side effect; `[...rest]` is a sibling of `(public)`, so unrelated to T04's own `loading.tsx` fix) | T04 SECOND FINDING | Dedicated 404-chrome pass | ✅ Closed by PRE-GATE-FIX (2026-07-19, Opus 4.8). **ROOT CAUSE:** the app's only `<html>`/`<body>` live in the dynamic `[locale]/layout.tsx` (there is no `app/layout.tsx`). On next@15.5 a `notFound()` that bubbles out of that dynamic-root segment renders on the special `/_not-found` route which sits ABOVE `[locale]`, so the layout's `<html lang/dir>` never re-applies → the bare `__next_error__` shell (vercel/next.js#64870, "still the case in 15.5"). This was a standing framework limitation, NOT a change introduced by a specific commit — BL-01-FIX's runtime claim predates the drift becoming visible. **FIX:** enabled `experimental.globalNotFound` + added `src/app/global-not-found.tsx` (full localized HTML document — reads negotiated locale from the `NEXT_LOCALE` cookie the middleware sets, `<html lang/dir>` + NextIntlClientProvider + kit `EmptyState`). Removed the `[locale]/[...rest]` catch-all so unknown paths are genuinely unmatched → routed to `global-not-found` (verified: `/xx`,`/does-not-exist`,`/en/does-not-exist`,`/xx/admin` → hard 404 + `noindex` + correct localized full-doc shell). `[locale]/not-found.tsx` kept (now bilingual via next-intl, was hardcoded-Arabic) for explicit `notFound()` from matched detail pages. **RESIDUAL (accepted):** explicit `notFound()` from a MATCHED route (listing/category/store missing record) still renders localized EmptyState CONTENT inside Next's default shell (no `lang/dir` wrapper) — the same #64870 limitation, unfixable without abandoning the unprefixed-default-locale architecture; status stays hard 404 + `noindex`. |
| REG-29 | `betk.store_follows` was RLS-ENABLED with ZERO policies (ERD §3 line 45 self-scope SPECCED but the `CREATE POLICY` statements were omitted from the Phase-01 SQL contract → default-deny for everyone incl. the owning buyer) — 2nd instance of the open-issue-#14 / T01-FIX class | Discovered by T06 (`toggleFollow` + storefront FollowButton state read) | Restored additively by T06 migration `20260718153021_store_follows_self_scope_rls.sql` (authorized flagged expansion of "no new policies", #14 precedent) | ✅ Closed by T06 (2026-07-18) — sf_select_self/sf_insert_self/sf_delete_self, no UPDATE policy per ERD; backfilled into `BETK_DATABASE_SCHEMA.sql`; T07 PR will now fire the R5 rls-smoke job (expected) |
| REG-30 | `experimental.globalNotFound` (next@15.5) is an EXPERIMENTAL Next flag — enabled in `next.config` by PRE-GATE-FIX to route unmatched paths to `src/app/global-not-found.tsx` (the localized full-doc 404 that fixed REG-28). Experimental API surface may change/be renamed/graduate/regress across Next minor+major upgrades; the fix also depends on the `NEXT_LOCALE` cookie contract + the no-`app/layout.tsx` dynamic-root-only `<html>` architecture (vercel/next.js#64870) | REG-28 fix side-effect (T07 mint 2026-07-19) | **Standing — re-verify on every Next upgrade** (confirm the flag still exists/behaves, unknown paths still hard-404 with localized full-doc shell + `noindex`; watch #64870 for a stable non-experimental path to retire the flag) | Open (owned) |
| REG-31 | `betk.stores` had NO INSERT policy (`stores_public` SELECT + `stores_manage` UPDATE only) despite ERD §3 speccing INSERT = own — 3rd instance of the open-issue-#14 / REG-29 class (ERD-specced policy omitted from the Phase-01 SQL contract; the Phase-03 T07 ERD audit listed `stores` as present-&-correct and missed the uncovered INSERT command) | Minted by Phase 04 / T01 (2026-07-19, Opus 4.8) | ✅ Closed by Phase 04 / T01 — same migration `20260719133011_seller_ownership_insert_rls` added permissive `stores_insert` (`WITH CHECK (seller_id = auth.uid())`), ERD-verbatim, no phone gate (ERD gates only orders/seller_profiles/payouts). Integration-proven: owner INSERTs own store, cross-user seller_id DENIED. #14 precedent = authorization. Backfilled into `BETK_DATABASE_SCHEMA.sql`. |
| REG-32 | `betk`-schema RPC (`Functions`) signatures are HAND-MAINTAINED in `src/lib/supabase/types.ts` — the CI types-drift regeneration (`supabase gen types --schema betk,betk_analytics`) does not reliably emit them; first instance `submit_seller_application` (T03), alongside pre-existing `is_admin`/`my_store_id`. The T03 action's typed `.schema("betk").rpc("submit_seller_application", …)` call REQUIRES the signature (typecheck depends on it — removing it breaks the build) | Minted by Phase 04 / T04 (2026-07-20, Sonnet) | ✅ **CLOSED by T08-FIX (2026-07-20, Opus 4.8, PR #44, CI run `29742475884`).** The AUTHORITATIVE CI `types-drift` job (real `--project-id` + `SUPABASE_ACCESS_TOKEN` secrets) PROVES `supabase gen types --schema betk,betk_analytics` emits `betk.Functions` (`is_admin`/`my_store_id`/`resubmit_seller_application`/`submit_seller_application`) NATIVELY — the failure was hand-edit-vs-generated (alphabetical Functions/Args ordering, no hand-comment, every Arg non-null — pg function-parameter metadata carries no nullability, unlike table columns). **FIX:** pulled the failed job's log (job `88352781673`) via the GitHub API and applied its printed diff to `types.ts` VERBATIM (confirmed byte-identical: local `git diff` reproduces the exact same blob hashes `aa01981..8dedd95` as the CI diff) — the hand-comment + hand-ordering removed, `types.ts` is now 100% generated, zero hand-maintenance. **Non-null Args broke 2 call sites that pass optional fields as `null`** (`submitSellerApplication.ts` prod action + the direct-rpc phone-gate test in `seller.submit.test.ts`) — fixed with ONE documented cast each at the `.rpc()` boundary (a locally-typed nullable `*RpcArgs`/`rpcArgs` object built first, cast to the generated `Args` type only at the call, comment cites the migration + REG-32 + "do not fix by editing types.ts"). `resubmitSellerApplication.ts`'s call site needed NO change (its 2 args are both required strings already). **Full RE-RUN green:** typecheck 0 errors · lint clean (pre-existing warnings only) · guards 4/4 · unit 82/82 · build 33 routes both locales · staging integration spot-check 14/14 (`seller.submit.test.ts` 7/7 incl. happy-path submit, `seller.resubmit.test.ts` 7/7 incl. rejected→resubmit) — zero residue (afterAll cleanup). **Local `pnpm db:types` still requires `SUPABASE_ACCESS_TOKEN` (absent locally) — regeneration is CI-authoritative going forward; for any future betk-schema rpc: add the migration, let CI's `types-drift` job produce the signature, apply its printed diff verbatim (never hand-add).** | Closed — see PR #44 body for the human-facing note |
| REG-33 | R-S03 slug change-once is enforced by the APP LAYER ONLY — live `betk.stores` has NO trigger/CHECK/constraint governing the change (only `chk_store_slug_fmt` / `uq_stores_slug` / `uq_stores_seller`, live-verified via pg_constraint + pg_trigger). `updateStoreProfile` (T06) is the sole guard: it rejects a changed slug when `slug_changed_at IS NOT NULL` and additionally scopes the slug UPDATE with `slug_changed_at IS NULL` (race-safe). A malicious direct client bypassing the action could, in principle, UPDATE its own store's slug twice (stores_manage RLS permits own-row UPDATE and does not inspect slug_changed_at) | Minted by Phase 04 / T06 (2026-07-20, Sonnet) | **Candidate hardening** — a `BEFORE UPDATE` trigger on `betk.stores` raising when `NEW.slug <> OLD.slug AND OLD.slug_changed_at IS NOT NULL` would make R-S03 DB-authoritative. No DB object added in T06 (task-scoped app-layer). **WHY it matters (T08): today `updateStoreProfile` is the ONLY writer of `stores.slug`, so the app guard is sufficient in practice — but future multi-writer paths break that assumption. Specifically a Phase-14 admin edit/backfill (or any second server-side writer that touches `stores`) would bypass an app-only guard and could change a slug twice, since `stores_manage` RLS permits own-row UPDATE and does not inspect `slug_changed_at`. The BEFORE UPDATE trigger makes R-S03 DB-authoritative regardless of writer.** Owner = the FIRST second-writer task (Phase-14 admin store edits) OR an opportunistic DB-hardening batch, whichever lands first. | Open (owned) |
| REG-34 | `betk.listing_images` / `listing_tags` owner-write policies (INSERT/UPDATE/DELETE = own store) SPECCED in ERD §3 row 48 but omitted from the Phase-01 SQL contract (children carried ONLY their public SELECT policy → owner write default-denied) — 4th instance of the open-issue-#14 / REG-29 / REG-31 class | Pre-reserved by `PHASE_05_LISTINGS.md`; minted by Phase 05 / T01 (2026-07-21, Opus) | ✅ **Closed by Phase 05 / T01 (2026-07-21, Opus).** Additive migration `20260721111355_listing_children_owner_write_rls` added `listing_images_seller` + `listing_tags_seller`, each `FOR ALL USING (EXISTS(betk.listings l WHERE l.id=<child>.listing_id AND (l.store_id=betk.my_store_id() OR betk.is_admin())))` — mirroring the parent `listings_seller` scoped via the owning listing (the USING doubles as the INSERT/UPDATE WITH CHECK per the Postgres FOR ALL pattern). The pre-existing public SELECT policies (`listing_images_public`/`listing_tags_public`) are UNTOUCHED and OR-combine, reconstructing ERD row 48's "follows listing" SELECT (own-store any-status via the new policy OR public active/sold_out via the public policy) — which also unblocks T02's own-draft-children reads. References only the SECURITY DEFINER helpers `my_store_id()`/`is_admin()` (no bare `auth.*`) → advisor sweep byte-identical to baseline, 0 new findings. Ledger 25→26 (remote↔local 1:1); backfilled into `BETK_DATABASE_SCHEMA.sql`. `restock_alerts` verified RLS-enabled zero-policy + NOT touched (Phase-12/notifications-owned). Storage verified zero-change (media own-prefix reuse; no DELETE policy by design). Integration `tests/integration/listing.childrenRls.test.ts` 7/7 on staging (owner INSERT/UPDATE/DELETE + own-draft SELECT; cross-seller + anon writes denied; public SELECT unregressed; draft children hidden), zero residue. |
| REG-35 | Production Vercel domain + preview-domain pattern missing from the Supabase Auth redirect allow-list (the Site URL fix already human-applied covers the primary redirect; the allow-list is the broader set) | DIAG-PERF-01 D3/DIAG-14 (2026-07-20, Opus 4.8) | Pre-launch (dashboard-config, beside REG-11) | ✅ **CLOSED (2026-07-21, human-confirmed, HOUSEKEEP-P5).** Production redirect allow-list configured in the Supabase dashboard, including the preview wildcard `https://betk-*-jiovannys-projects-0219772b.vercel.app/auth/callback`. Dashboard-config only, no code/migration change. |
| REG-36 | `auth_rls_initplan` — ~24 `betk` policies + 5 `storage.objects` policies re-evaluate a bare `auth.<fn>()`/`auth.uid()` per row instead of `(select auth.<fn>())` (incl. the Phase-04 `sp_insert`/`stores_insert`) | DIAG-PERF-01 C2/DIAG-06 (2026-07-20, Opus 4.8) | Dedicated reviewed RLS/policy task (widespread — every betk write policy) | Open (owned) |
| REG-37 | CACHING_STRATEGY standing item: `rating_aggregates` inherits the homepage's 60s ISR TTL (no distinct 5-min TTL) + ~37 unindexed FKs on hot read paths (harmless on near-empty staging; covering indexes wanted before scale) | DIAG-PERF-01 A4+C3/DIAG-04+05 (2026-07-20, Opus 4.8) | Standing — CACHING_STRATEGY doc + an indexing pass before scale | **Caching-doc leg ✅ CLOSED-WITH-EVIDENCE by PERF-02 (2026-07-21, Opus, `feature/perf-01`)** — `docs/02-architecture/CACHING_STRATEGY.md` authored (per-route posture table for every public+authed+API route with WHY, anon-vs-cookie client rule, the `rating_aggregates`-inherits-60s decision, the store-ISR future candidate, the category load-more trade-off, the middleware guest fast-path contract, and §7 the next-intl on-demand-ISR locale rule = REG-40), cross-linked from `BETK_ARCHITECTURE.md §7`. `/category/[slug]` + `/listing/[id]` flipped to ISR (revalidate 60), verified via build route table + runtime cached-hit timing (EN category 567ms→35ms repeat) + hard-404-on-repeat both locales. **FK-index leg stays OPEN (owned)** — the ~37 covering indexes before scale are a DB task, untouched by PERF-02. **Noted at HOUSEKEEP-P5 (2026-07-21): pending, alongside CD-DELTA-4 (requested, in flight — see REG-38/REG-39).** |
| REG-38 | Locale-switch UX: `AppChrome`/`LanguageSwitcher`'s `router.replace(pathname,{locale})` is a full server round-trip (next-intl remounts the whole `[locale]` subtree) with no visible pending state, compounded by NO `(public)` route-level `loading.tsx` (forbidden by the hard-404 binding rule) — the locale toggle "feels dead" and in-page navigation "feels stuck" | DIAG-PERF-01 A1+A2/DIAG-01+02 (2026-07-20, Opus 4.8) | Two legs: (1) app-layer pending-state on the toggle itself; (2) `(public)` route-level skeleton-streaming strategy — Claude Design (UI Spec §6 / Brief §5.24) | **Leg (1) ✅ CLOSED by PERF-01 (2026-07-21, Sonnet, `feature/perf-01`)** — both locale toggles (`AppChrome` + `account/LanguageSwitcher`) wrap `router.replace` in `useTransition`; `LanguageSwitcher`'s plain `<select>` (app-layer, already unstyled) gets `disabled`+`aria-busy`+token `opacity-60`/`cursor-progress` directly; `AppChrome`'s wrapper div gets `aria-busy`+`opacity-60`+`pointer-events-none` while pending (its own click-handler also no-ops while pending) — **STOP-and-flag, CD-DELTA-4 candidate:** `AppTopbar`'s language `<button>` (frozen `components/shared`) has no `disabled`/`className` passthrough, so the button itself can't carry the pending affordance directly; code-commented in `AppChrome.tsx`. **Leg (2) stays Open (owned, Claude Design)** — no route-level `(public)` `loading.tsx`/skeleton strategy added (binding rule holds; still a holistic Design decision). PERF-01 additionally shipped a narrower, complementary mitigation on 2 specific `(public)` routes — component-level `<Suspense>` streaming (not a route `loading.tsx`) on `/category/[slug]` (`CategoryListingsSection`) and `/store/[slug]`'s Listings tab (`StoreListingsSection`), both `SkeletonGrid`-fallback, homepage-precedent — so header/chrome paints immediately while just the listings query streams in; `/search` and `/listing/[id]` are untouched (out of PERF-01's STEP 1 scope) and still have no per-route feedback, so leg (2)'s general strategy decision remains open. **Noted at HOUSEKEEP-P5 (2026-07-21): leg (2) is pending CD-DELTA-4 (Claude Design — requested, in flight).** ✅ **CLOSED by CD-DELTA-4 LAND (2026-07-21, Sonnet, `feature/phase-05-listings`)** — leg (1)'s AppTopbar API gap is filled: the shared `AppTopbar` gained the additive `langPending` prop (disabled + `aria-busy` + token `opacity-60`/`cursor-progress` ON THE BUTTON, zero visual change unset — the only sanctioned edit point, every other prop/JSX byte-preserved, diff-verified vs main); `AppChrome` now passes `langPending={isLocalePending}` and the PERF-01 wrapper workaround (the `pointer-events-none`/`opacity-60` chrome `<div>`) was REMOVED since the button fully expresses pending; `account/LanguageSwitcher` unchanged. Leg (2) landed as the token-only shared `RouteProgress` top bar (REG-38b ADOPTED — `fixed inset-x-0 top-0`, `z-60`, indeterminate `--primary` segment, renders null at rest) mounted in BOTH the public/buyer `AppChrome` and the seller `SellerChrome` shells (driven by a `useTransition` route-transition state), plus 3 additive route-transition skeletons (`CategoryPageSkeleton`/`ListingDetailSkeleton`/`StorefrontSkeleton`) on `CatalogSkeletons` (additive exports only, nothing renamed/removed). Brief §5.12/§5.13/§5.24 addenda applied. |
| REG-39 | 8 seeded category `icon_url` paths (`/icons/categories/{clothing,food,home,beauty,jewelry,arts,books,services}.svg`) all 404 in production (evidence: Vercel prod logs, 2026-07-20) — the icon assets were referenced in seed data but never produced/deployed to that path | Minted by PERF-01 (2026-07-21, Sonnet) | Real assets = **CD-DELTA-4** (Claude Design). PERF-01 ships a feature-layer stopgap only (drop the known-missing URLs at the mapping so the shared `CategoryGrid`'s Lucide default renders). | **Stopgap ✅ CLOSED by PERF-01 (2026-07-21, Sonnet, `feature/perf-01`)** — `HomeCategoryGrid.tsx` (feature layer, NOT `components/shared` — the `<img>` was already composed here) now drops any `iconUrl` under the known-missing `/icons/categories/` prefix at the mapping (`resolveIconUrl`), so `icon` resolves to `undefined` and the shared `CategoryGrid` renders its own Lucide `<Tag>` default; code-commented `REG-39, real assets via CD-DELTA-4`. Runtime-verified on the homepage: 0 actual `<img>` tags reference `/icons/categories/*` (zero browser-fetchable requests — the raw string still appears once, harmlessly, inside the RSC hydration payload's serialized props JSON, which is not a fetch target), 8/8 top-level categories render the `lucide-tag` fallback glyph instead. **Real-assets leg stays Open (owned, CD-DELTA-4).** **Noted at HOUSEKEEP-P5 (2026-07-21): CD-DELTA-4 has been requested and is in flight (not yet delivered).** ✅ **CLOSED by CD-DELTA-4 LAND (2026-07-21, Sonnet, `feature/phase-05-listings`)** — the 8 theme-neutral single-color (fixed brand teal `#1c8d7e`, sanctioned asset literal — CSS vars can't reach inside an `<img>`) 24×24 outline glyphs now ship at their exact seeded paths `public/icons/categories/{clothing,food,home,beauty,jewelry,arts,books,services}.svg` (byte-diff-verified vs the handoff templates). The `HomeCategoryGrid` `resolveIconUrl` stopgap + its REG-39 comment were REMOVED so the real `icon_url` paths render again. Runtime-verified on `next start`: the homepage HTML references all 8 `/icons/categories/*.svg`, each returns HTTP 200 (`image/svg+xml`), and there are ZERO `lucide-tag` fallbacks for the 8 top-level categories. Both legs now closed. |
| REG-40 | next-intl + on-demand ISR locale rule (STANDING, load-bearing): on an ISR route under `[locale]` whose `generateStaticParams` returns `[]` (nothing prebuilt → every path generated on demand at first hit / revalidation), next-intl's `setRequestLocale` `cache()` store is NOT reliably resolved across the independently-rendered layout / page / Suspense-child scopes for the NON-default locale — a store-based `getLocale()`/`getTranslations()` (or server `<Link>`, which calls `getLocale()` internally) then falls back to `headers()` → static generation aborts with `DYNAMIC_SERVER_USAGE` → **500 on `/en/…` while `/…` (AR) is 200** (the default locale is silently masked by next-intl's fallback). Build-time-prerendered routes (homepage, `/en/search` shell, authed shells) mask it — the failure only shows on the on-demand / revalidation path. | Discovered + fixed by PERF-02 (2026-07-21, Opus, `feature/perf-01`) during the STEP-4 runtime smoke | ✅ **Fix applied + STANDING RULE recorded** (`CACHING_STRATEGY.md §7`): thread `locale` EXPLICITLY on every ISR render path — `getTranslations({ locale, namespace })` everywhere (incl. the shared `(public)/layout.tsx`, which wraps all public pages), `locale` as a prop into Suspense-wrapped async children (`CategoryListingsSection`), and `locale={locale}` on server `<Link>` (`SubcategoryChips` + the category empty-state links). `setRequestLocale` is still called (primes the client provider) but the render path no longer depends on it. Any future ISR route under `[locale]` MUST follow this (verify: `next start`, hit `/en/<route>` first+repeat — a 500 on `/en` while `/` is 200 is this bug). | Standing (rule) — closed for category/listing; applies to all future ISR routes |
| REG-41 | `inquiries` + `inquiry_messages` ERD §3-specced RLS policies absent (live-verified: `inquiries` carried ONLY `inq_buyer` SELECT; `inquiry_messages` RLS-enabled ZERO-policy → default-deny) — 5th instance of the open-issue-#14 / REG-29 / REG-31 / REG-34 class | Pre-reserved by `PHASE_06_MESSAGING.md`; minted+closed by Phase 06 / T01 (2026-07-22, Opus) | ✅ **Closed by Phase 06 / T01 (2026-07-22, Opus).** Additive migration `20260722115026_inquiry_messaging_rls`, ERD §3 rows 51-52 verbatim: **inquiries** — `inq_insert` (INSERT WITH CHECK `buyer_id = auth.uid()`, no phone gate — ERD §1.2 gates only orders/seller_profiles/payouts) + `inq_update` (UPDATE USING+CHECK `store_id = my_store_id() OR is_admin()` = the confirm→checkout transition surface); **SELECT was NOT re-added** — the pre-existing `inq_buyer` USING is already `((buyer_id=auth.uid()) OR (store_id=my_store_id()) OR is_admin())` = row 51 "buyer or store or admin" verbatim (live-quoted), so a 2nd SELECT policy would be a redundant multiple-permissive; `inq_buyer` LEFT UNTOUCHED. **inquiry_messages** — `inq_msg_select` + `inq_msg_insert` (thread parties = parent inquiry buyer OR store via EXISTS; INSERT pins `sender_id=auth.uid()`) + `inq_msg_update` (sender own rows only); NO DELETE (ERD "—"). Bare `auth.uid()` + SECURITY DEFINER helpers, default (public) roles — existing-style, REG-36 wrap NOT smuggled. Ledger 26→27 (remote↔local 1:1); backfilled into `BETK_DATABASE_SCHEMA.sql`; security advisor RESOLVES the `inquiry_messages` rls_enabled_no_policy INFO (14→13), 0 new findings. Integration `tests/integration/inquiry.rls.test.ts` **13/13** on staging (buyer create+read; owning seller reads same; unrelated seller/buyer + anon read → 0; buyer impersonation INSERT denied; both parties send + read; outsider send denied; sender-only UPDATE [other party's is_read flip → 0 rows]; no DELETE; seller status UPDATE ok, buyer + unrelated-seller status UPDATE → 0 rows), zero residue. `order_messages`/`notifications`/`restock_alerts` untouched. |
| REG-42 | Unread WRITE-path gap: the unread mechanism column is `inquiry_messages.is_read` (boolean NOT NULL default false — live-cited), but ERD §3 row 52 specs `inquiry_messages` UPDATE = **sender** only, so the RECIPIENT cannot flip `is_read` on the OTHER party's messages (exactly the rows a reader needs to mark read). The column EXISTS but the reader-driven mark-read WRITE is NOT RLS-achievable additively under ERD-verbatim (integration-proven: seller's UPDATE of the buyer's message → 0 rows). Broadening UPDATE beyond "sender" would violate ERD row 52. | Minted by Phase 06 / T01 (2026-07-22, Opus) — cite-or-flag discipline (a column exists so no STOP; the write-path tension is flagged, not improvised) | ✅ **CLOSED by Phase 06 / T02-FIX (2026-07-22, Opus): DECISION 3 REVISED = (b), grounded by an AUTHORIZED ERD §3 row-52 amendment.** The human authorized the amendment (BETK_ERD.md §3, 2026-07-22): the row's original "sender" UPDATE wording described **content-edit** rights; `is_read` is **definitionally receiver-driven** (a sender flipping read-state on their own message is a no-op). Migration `20260722124510_inquiry_read_receipt_rls` (additive + grant re-scope, ledger 27→28): **column safety = GRANT** — `REVOKE UPDATE ON betk.inquiry_messages FROM authenticated; GRANT UPDATE(is_read) … TO authenticated` (an authenticated caller may UPDATE ONLY is_read — a `body` edit is DENIED BY THE GRANT, error 42501, not zero rows; service_role/postgres/anon grants untouched — live-verified); **row safety = policy** — permissive `inq_msg_read_receipt` (`FOR UPDATE TO authenticated`, USING+CHECK = party to the parent inquiry AND `sender_id <> auth.uid()`), OR-combined with the untouched `inq_msg_update` (sender). `markInquiryRead(inquiryId)` action built (no service-role; idempotent; outsider→not_found); `unreadCount` wired into getOwnInquiries/getStoreInquiries + per-message `isRead`+thread `unreadCount` into getInquiryThread. **(a) DEFER superseded** (the only blocker was the missing cite — the amendment supplies it). **(c) DEFINER rpc STILL rejected** (advisor 0029, ADR-012's reject reason). ADR-015 records the decision. Advisor sweep = exact post-T01 baseline (13 rls-no-policy INFO), 0 new. Integration `inquiry.readReceipt.test.ts` 10/10 staging zero-residue; T01's 13 assertions unregressed (the one that encoded the now-closed gap revised to the amended behavior). |
| REG-43 | `inquiries.last_message_at` maintenance gap: column is `NOT NULL DEFAULT now()` with **no trigger** maintaining it (live-verified — all inquiries/inquiry_messages triggers are internal FK `RI_ConstraintTrigger`s), so the plan is an app-layer bump in T02. BUT ERD §3 row 51 specs `inquiries` UPDATE = **store/admin** only (`inq_update`), so a BUYER-sent message CANNOT bump `last_message_at` via RLS (buyers have no inquiries UPDATE); seller-sent bumps DO work (store/admin). | Minted by Phase 06 / T01 (2026-07-22, Opus) | ✅ **CLOSED-BY-DECISION — DECISION 4a DERIVE-AT-READ (Phase 06 / T02; re-affirmed at the T05 exit gate 2026-07-22, Opus; zero migration).** ⚠️ **STANDING WARNING (load-bearing for Phase 07/12):** `inquiries.last_message_at` is **UNMAINTAINED** — NO trigger, NO app write; it stays at its INSERT default (`= created_at`). Any future phase that needs "latest activity" MUST derive it from `max(inquiry_messages.sent_at)` (see `latestActivityAt` in `messagingRules.ts`); NEVER read `last_message_at` as truth. This warning is mirrored into the Phase-07 entry checklist + the ERD §3 order-set map row so Phase 07/12 see it. `last_message_at` is NEVER written (stays at its INSERT default = `created_at`, documented stale in the query + action headers); inbox ordering is DERIVED in the queries from `max(inquiry.created_at, max inquiry_messages.sent_at)` (`latestActivityAt`, `messagingRules.ts`), which re-sorts a thread on a BUYER's newest message for BOTH parties — proven by integration ("a BUYER's newest message re-sorts the thread to the top") + `last_message_at` asserted UNCHANGED after messages. WHY (b) a SECURITY DEFINER INSERT trigger was rejected: it is advisor-clean (trigger-only, EXECUTE revoked PUBLIC) but adds a migration + permanent DB object for a pure read-ordering concern that the query layer handles — ADR-013 ethos (do not add DB machinery for a need app-layer covers). Do NOT broaden the buyer's inquiries UPDATE (would violate ERD row 51). |
| REG-44 | Seller cannot read a BUYER's display name via RLS: ERD §3 row 39 specs `buyer_profiles` SELECT = "self or admin (**public name/gov for discovery**)" but the live `bp_self` policy is `id = auth.uid() OR is_admin()` (self/admin only — the public-name/gov branch the ERD text specs is ABSENT), so a seller reading their store inbox cannot surface the buyer's name. Same "specced-but-absent" #14 / REG-29/31/34/41 class, discovered at Phase 06 / T02 while building `getStoreInquiries`. | Minted by Phase 06 / T02 (2026-07-22, Opus) — cite-or-flag (flagged, NOT worked around with service-role) | Open (owned) — **REFRAMED 2026-07-23 (CORRECTION-02): REG-44 STANDS ALONE.** The prior "ONE POSTURE / bilateral pre-transaction identity opacity" coupling with REG-45 is **RETIRED** (REG-45 is now closed-as-not-a-defect — there is no off-platform contact affordance to serve, so there was never a symmetric "buyer can't read the seller's phone" defect; all buyer↔seller communication is in-app only). REG-44 resolves on the narrower, real ground that **the seller needs the buyer's NAME + delivery ADDRESS to FULFIL** — fulfilment (the courier/shipping label) necessarily exposes buyer name+phone+address to the seller / self-delivering seller at order time (Phase 07; REG-14 self-delivery). **No contact-exchange product decision is owed** (that was an artefact of the retired coupling); the only remaining question is the additive `buyer_profiles` name-read policy for the store inbox (ERD row 39's "name/gov for discovery" branch, still ABSENT). T02 surfaces `buyerId` only (no name); **T04 (2026-07-22, Sonnet) renders the neutral keyed label as planned** (`t("buyerLabel")` on `/seller/inbox` + `/seller/inbox/[id]`, no invented pseudonymous handle), runtime-proven in the DOM. NOT fixed here — migration for this is NOT authorized by the T02 decision set and it's a broader concern (also affects review author names etc.), so it belongs to a dedicated additive-policy task (add a scoped public name/gov SELECT branch to `bp_self` per ERD row 39, or a narrow buyer-name read for thread
| REG-45 | Buyer-side WhatsApp deep-link to the seller (UI_SPEC-pinned affordance on the inquiry thread) is RLS-unreachable: `stores`/`users` do not expose the seller's phone number to a buyer thread party (`users_self`-class SELECT policies are self/admin only; `stores_public` doesn't carry a phone column) — a buyer cannot resolve the seller's WhatsApp number without a service-role reach-around. | Minted by Phase 06 / T03 (2026-07-22, Sonnet) — discovered composing `/inbox/[id]`, flagged not improvised | ✅ **CLOSED AS NOT-A-DEFECT (2026-07-23, CORRECTION-02).** Misread: the UI_SPEC inbox "WhatsApp deep-link" line was read as buyer↔seller contact, but **NEITHER inbox screen's data requirements provision a counterparty phone column** — so RLS was never the obstacle, and there is nothing to serve because **all buyer↔seller communication is in-app only** (no off-platform contact affordance, ever — see the standing "Communication posture" fact + `docs/PRECEDENTS.md`). T03 (buyer) and T04 (seller) already OMITTED the button, so **ZERO code change is owed**; runtime-proven (no `whatsapp`/`wa.me` in either rendered DOM). The stale UI_SPEC lines are corrected in this cascade (buyer + seller inbox WhatsApp deep-links deleted; Listing-Detail share made channel-agnostic; a channel-agnostic storefront share ADDED as a human-authorized addition — REG-51). The prior "one posture / bilateral pre-transaction identity opacity" coupling with REG-44 is **RETIRED** — REG-44 now stands ALONE (seller needs the buyer's name+address to fulfil; see REG-44). |
| REG-46 | `notFound()` inside `/inbox/[id]` (and, latently, the pre-existing `(buyer)/account` `notFound()`) rendered the correct 404 CONTENT but shipped **HTTP status 200** — root cause: `src/app/[locale]/(buyer)/loading.tsx` (an OD-7-era shared Suspense boundary for the whole `(buyer)` route group) wraps every buyer route including `/inbox/[id]`; Next.js locks in the response status at the FIRST flush once any ancestor Suspense boundary can show a fallback, so by the time the awaited `getInquiryThread` resolves to `null` and throws `notFound()`, the 200 header is already committed — the exact BL-01-FIX / Phase-04-T04 / Phase-05-T04 class of bug, this time at the GROUP level rather than a single segment. | Discovered by Phase 06 / T03 runtime smoke (2026-07-22, Sonnet) — a forged-cookie HTTP check caught the status-code drift that a content-only assertion would have missed | ✅ **CLOSED (fixed) by Phase 06 / T03 (2026-07-22, Sonnet); whole-app re-sweep re-confirmed clean at the T05 exit gate (2026-07-22, Opus). Same precedent as BL-01-FIX/T04 ("fixed by DELETING the offending `loading.tsx`, never by adding one").** Deleted `src/app/[locale]/(buyer)/loading.tsx` — `(buyer)/layout.tsx` is purely structural (mounts `AppChrome`, no auth logic per its own header comment) so removing the shared fallback has no auth/UX-gate impact, only trades an instant Suspense skeleton for the browser's native pending state on buyer-route navigations (the same trade-off already accepted for every `(public)` detail page). Re-verified: `/inbox/[id]` now returns a genuine HTTP 404 for outsider/malformed/unknown ids (forged-cookie smoke, status code asserted, not content shape). **NOT independently re-verified for `/account`'s notFound() case** (same boundary, same fix, but out of T03's page scope) — flagged for whoever next touches `(buyer)/account`, `/wishlist`, `/orders`, `/notifications`, `/checkout`, or `/disputes` to confirm no regression expectation on the removed loading state. |
| REG-47 | Guard E — no `loading.tsx` at/above any route segment whose page can reach `notFound()` (the BL-01-FIX / T04-Phase-03 / REG-46 class: a group/segment Suspense fallback commits HTTP 200 at first flush BEFORE an awaited `null`→`notFound()` can set 404) | Minted by Phase 06 / T05 exit gate (2026-07-22, Opus) — **RECORD-ONLY** | **Open (owned) — housekeeping, NOT built here.** Three runtime-only catches (BL-01-FIX, T04-Phase-03, REG-46) is enough evidence the discipline needs a committed CI guard — same reasoning that promoted REG-27's ad-hoc i18n-parity checks to a guard. Scope when built: a `scripts/` check that fails when a `loading.tsx` sits at or above any segment whose `page.tsx` can (transitively) call `notFound()`. Schedule into any convenient housekeeping window; **do NOT build in this exit gate** (ZERO feature-code). |
| REG-48 | Order-set children RLS absent (ERD §3 rows 53-59 specced, live-verified ZERO-policy: `order_items`, `order_status_history`, `order_messages`, `shipments`, `shipment_tracking_events`) — 6th instance of the open-issue-#14 / REG-29/31/34/41 class | Pre-reserved by `PHASE_07_ORDERS.md`; minted+closed by Phase 07 / T01 (2026-07-23, Opus Max) | ✅ **Closed by Phase 07 / T01 (2026-07-23, Opus Max).** Same migration `20260723074953`. Parent-scoped via the owning `orders` row (buyer own OR `my_store_id()` OR `is_admin()`): `order_items` (SELECT parties + INSERT buyer-of-parent = "system(checkout)"), `order_status_history` (SELECT parties + INSERT any-party; UPDATE/DELETE stay blocked by the live append-only RULES `no_update/no_delete_order_history` → no policy added), `order_messages` (SELECT parties + INSERT pins `sender_id=auth.uid()`; read-state NOT pinned — row 53 not REG-42-amended, `is_read` unused). **SHIPMENTS DECISION:** `shipments`/`shipment_tracking_events` **READ** policies landed now (FR-BUY-9 `/orders/[id]` tracking); store/courier **WRITE** deferred to Phase 08 (BETK_PHASES L52-54). Advisor `rls_enabled_no_policy` **13→8 INFO**, all WARN byte-identical. Integration `order.rls.test.ts` **15/15 + 1 opt-in skipped** on staging (REG-09 both halves, children both-direction reads, item INSERT scoping, order_messages pinned-sender + isolation, shipments READ + WRITE-denied, status-history outsider-INSERT denied, tension proof), zero residue. Backfilled into `BETK_DATABASE_SCHEMA.sql`. |
| REG-49 | `orders` UPDATE policy + `payments` INSERT/UPDATE policies ABSENT (T01 landed only the INSERT/READ foundation) — owed by T02's write layer | Minted by Phase 07 / T01 (2026-07-23, Opus Max) — cite-or-flag (surfaced during the read-first audit, not worked around) | **Open (owned by Phase 07 / T02).** T02's checkout RPC + seller order-mgmt actions need: `orders` UPDATE (seller confirm→preparing + buyer cancel R-O03 + the COD INSERT-pending-then-UPDATE two-step that fires `decrement_stock_on_confirm`) and `payments` INSERT (the two-row deposit+balance create, C2 §3.8/ADR-002) + `payments` UPDATE (deposit/COD confirmation). Mint per ERD §3 rows 54/57-58 when T02 builds the write layer; ERD-verbatim, advisor-clean, MCP→local→ledger→backfill (REG-24). Do NOT `SECURITY DEFINER` the checkout RPC (advisor 0029; let the phone gate bite — Phase-07 checklist). |
| REG-50 | BETK **support** contact has no home. Footer "Contact us" should repoint to a **support WhatsApp deep-link** — a **BETK→user support channel**, NOT counterparty contact, so it is posture-compliant (brief §9(ii)'s own "repoint" option). Needs a support number + a landing home; **"Help center" STAYS unrouted**. A dedicated web-app support PAGE would take the count **59→60 = a new OD (OD-9), NOT authorized**. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned) — needs a support number; no page added. |
| REG-51 | **Channel-agnostic share** affordance (device share sheet on a **public link** — product listing + seller storefront; WhatsApp/FB/Instagram/link downstream, NOT enumerated). A share is not counterparty contact → posture-compliant. Storefront share ADDED here as a **human-authorized addition**; Listing-Detail share reworded channel-agnostic. | Minted by CORRECTION-02 (2026-07-23) — owner **Claude Design CD-DELTA-5** | Open (owned, CD-DELTA-5) — implement the share component. |
| REG-52 | Seller-onboarding category/subcategory picker is poorly organized (UX). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 / onboarding UX). |
| REG-53 | Seller-onboarding delivery modes should **DERIVE from category** (a remote/digital service vs a physical product implies different valid modes) rather than offering all three unconditionally. No schema change (REG-14 `delivery_preference` modes unchanged). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned) — product/UX decision. |
| REG-54 | Docs say `seller_analytics_snapshots` / `platform_analytics_snapshots`; the LIVE tables are **`seller_snapshots` / `platform_snapshots`** (ERD §1 inventory + `betk_analytics` is authoritative). Fixed in `BETK_UI_SPEC.md` (Earnings) here; **doc sweep still owed** for `BETK_ERD.md §2` + `BETK_PRD.md` (FR-SEL/FR-ADM analytics rows) + any other doc. | Minted by CORRECTION-02 (2026-07-23) — docs-hygiene | Open (owned) — doc sweep only; live schema is correct, no code/DB change. |
| REG-55 | Public storefront rendered the **seller's `payment_methods` handles to buyers** (`store/[slug]` About tab + checkout instructions) — an active leak under custody (a buyer could pay the seller directly, bypassing BETK + commission). Doc half corrected here (UI_SPEC storefront/checkout/confirmation now render BETK's `admin_settings` handles). | Minted by CORRECTION-02 (2026-07-23) — owner **CORRECTION-02B** (code) | Open (owned, CORRECTION-02B) — remove `payment_methods` from the buyer-facing storefront render + repoint checkout instructions to BETK's handles. |
| REG-56 | Terminal `order_status` for admin **order closure** (OD-8 §3 step 7) — **FLAGGED per STEP 0(a): the pinned enum `{pending,confirmed,preparing,dispatched,delivered,cancelled,returned}` has NO "closed"/"settled" member beyond `delivered`.** Per cite-or-flag, **no enum member is invented**. **Closure is expressed as a DERIVED condition — both payment rows `confirmed` AND `orders.status='delivered'`** (admin closure is the operational signal on `/admin/payments`; OD-8 §6 pairs it with `delivered_at + return_hold_hours` for the settlement/return-hold window). | Minted by CORRECTION-02 (2026-07-23) — cite-or-flag | Open (owned) — a first-class closure signal would be an OD amendment (new enum member = schema change), NOT invented here. |
| REG-57 | Phase-07 pack (T02–T06) must be **REGENERATED** for the custodial model (OD-8): no COD auto-confirm (§3.1, R-O04 retired), **no pure-COD path (§3.2 — DECIDED-retired, every order carries the 50/50 split)**, admin deposit-verification gate, commission snapshot, BETK's rails. The pack still describes the pre-custody model → this **BLOCKS Phase-07 pack regeneration / T02 start**. (NB: §3.2 pure-COD is DECIDED, not open — the OPEN item is the pack rebuild.) | Minted by CORRECTION-02 (2026-07-23) | Open (owned) — regenerate T02–T06 before building; T00/T01 stay DONE (artifacts survive, CORRECTION-01 §G3). |
| REG-58 | Dark-theme search-bar text/background contrast fails a11y (AA). | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 a11y). |
| REG-59 | `/account` renders unstyled. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5). |
| REG-60 | Seller-console logo does not navigate home. | Minted by CORRECTION-02 (2026-07-23) — record-only | Open (owned, CD-DELTA-5 / SellerChrome). |
| REG-61 | R-S09 publish gate: `cod_enabled` no longer satisfies "seller can be paid" under OD-8 §7 — `hasPaymentMethod` in `src/features/listings/listingRules.ts` must become "**≥1 settlement handle set**" (`cod_enabled` alone stops satisfying it; a COD-only seller newly fails the gate). Doc half corrected here (UI_SPEC Payment Methods + publish validation + onboarding, "seller can be paid" wording). | Minted by CORRECTION-02 (2026-07-23) — owner **CORRECTION-02B** (code) | Open (owned, CORRECTION-02B) — change `hasPaymentMethod`. |
> **CD-DELTA-2 shared-kit defects are NOT REG-numbered** (reconciled 2026-07-19): the three Claude-Design kit gaps — `ImageGallery` missing its own `"use client"`, `WishlistButton` heart-click bubbling into card navigation, `ListingCard` lacking `wishlistAddLabel`/`wishlistRemoveLabel` passthrough — are tracked + ✅ CLOSED as **CD-DELTA-2** (landed T06, 2026-07-18; full detail → journal). They had briefly collided with REG-26/27/28; those numbers are now restored to their canonical items above (view_count / i18n-parity guard / 404-chrome).

## Phase 07 entry checklist (carry-forwards into Checkout & Orders — schedule into owning tasks, don't lose them)
> Pinned 2026-07-22 (Phase 06 / T05 exit gate, Opus). **Phase 07 = Checkout & Orders** (`BETK_PHASES.md`: AC-BUY-6 — an order can be created ONLY from a seller-confirmed inquiry; atomic order + payment record(s); order lifecycle + shipments). These are first-class tasks of the owning Phase-07 build, not surprises. Consumes the Phase 06 CONFIRM→CHECKOUT CONTRACT block below.
- ✅ **REG-09 — `orders` permissive ownership INSERT policy — DONE (Phase 07 / T01, 2026-07-23).** `orders_insert` (`WITH CHECK buyer_id = auth.uid()`) landed, combining with the RESTRICTIVE `orders_phone_gate`; migration `20260723074953`. The **verified-phone transaction gate bites HERE** — T02's `requireVerifiedPhone` (OD-4, ERD §1.2) still gates checkout order-create at the app layer on top of the RLS gate.
- ✅ **Order-set RLS rows (ERD §3 map rows 1–5) — DONE (Phase 07 / T01, REG-48).** `order_items`/`order_status_history`/`order_messages` READ+INSERT + `shipments`/`shipment_tracking_events` READ landed; shipment WRITE deferred to Phase 08. **`order_messages` is the ORDER-thread (Phase 07) — NOT the inquiry thread (`inquiry_messages`, Phase 06); do not conflate them.** ⚠️ **REG-49 owed by T02:** `orders` UPDATE + `payments` INSERT/UPDATE (see the ORDER-SET CONTRACT block).
- **Checkout CONSUMES the confirm contract + OWNS the `converted_to_order_id` write.** Gate order-create on `inquiries.status = 'confirmed'` (the T02-pinned member; `confirmInquiry` is the only writer of it). On successful order creation, checkout writes `inquiries.converted_to_order_id = <new order id>` (FK `fk_inquiries_order` → `betk.orders`) — this column is **NULL through all of Phase 06 by design**; Phase 07 is its sole writer. `inq_update` (store/admin) is the policy surface, so confirm/convert are seller/admin writes.
- **AC-BUY-6 — atomic order + payment record(s) (ADR-012-class decision OWED).** Creating an order plus its payment/order_items rows is a multi-table write with a genuine orphan invariant → decide atomicity up front (ADR-012 atomic `SECURITY INVOKER` rpc vs ADR-013 draft-first decomposition). Record the decision as a NEW ADR (next free slot) BEFORE building. Do NOT `SECURITY DEFINER` (advisor 0029); let the RLS phone gate bite naturally.
- **[FLAGGED] The pre-transaction identity opacity RESOLVES here (REG-44 + REG-45 = ONE POSTURE).** Fulfilment NECESSARILY exposes the buyer's name/address to the seller at order time → REG-44 (seller-can't-read-buyer-name) resolves in Phase 07 via the order/fulfilment surface, NOT by broadening the `buyer_profiles` SELECT policy pre-transaction. **A PRODUCT DECISION is owed** on whether contact exchange (REG-45's UI_SPEC-pinned WhatsApp/contact deep-link) unlocks at **CONFIRM** or at **ORDER** — record it as a decision (an ADR/scope note), NOT a defect. Whichever is chosen drives whether REG-45's WhatsApp surface can finally be served (it is RLS-unreachable pre-transaction).
- **⚠️ STANDING (REG-43): `inquiries.last_message_at` is UNMAINTAINED.** No trigger, no app write; it sits at its INSERT default (`= created_at`). If any Phase-07/12 surface needs an inquiry's latest activity, DERIVE it from `max(inquiry_messages.sent_at)` (`latestActivityAt`, `messagingRules.ts`) — NEVER read `last_message_at` as truth.

## Phase 06 / T01 — pinned CONFIRM→CHECKOUT CONTRACT + messaging schema facts (T02–T05 + Phase 07 cite THIS)
> Pinned 2026-07-22 (Phase 06 / T01, Opus). Every fact below is cited from `BETK_DATABASE_SCHEMA.sql` + LIVE staging (`pg_enum`/`information_schema`/`pg_policies`/`pg_trigger`/`pg_proc`), never assumed. Do not re-derive — cite this block.
- **CONFIRM member (the confirm→checkout contract).** `inquiry_status` enum = `{open, replied, confirmed, declined, expired}` (schema `BETK_DATABASE_SCHEMA.sql` L52; live `pg_enum` zero-drift). The member that marks a seller-confirmed inquiry = **`'confirmed'`**. `inquiries.status` is `NOT NULL DEFAULT 'open'`. **T02 `confirmInquiry` transitions `status → 'confirmed'`** (seller-only, via `inq_update` = `store_id=my_store_id() OR is_admin()`; buyers cannot UPDATE — integration-proven). **Phase 07 checkout gates order-create on `status='confirmed'`** (AC-BUY-6). This IS the checkout-enablement write.
- **`converted_to_order_id` = Phase-07-write, NEVER Phase 06.** `inquiries.converted_to_order_id` UUID nullable (schema L372; live: nullable YES, no default), FK `fk_inquiries_order` → `betk.orders(id)` (schema L431-433; live-confirmed). Phase 06 NEVER writes it; **Phase-07 checkout owns the write** (set when the confirmed inquiry becomes an order). It stays NULL through all of Phase 06.
- **`last_message_at` ownership = app-layer (no trigger), with a buyer-bump caveat (REG-43).** `inquiries.last_message_at` `TIMESTAMPTZ NOT NULL DEFAULT now()` (schema L375); **NO trigger maintains it** (live: the only inquiries/inquiry_messages triggers are internal FK `RI_ConstraintTrigger`s). Plan = app-layer bump in T02. **CAVEAT:** ERD §3 row 51 restricts inquiries UPDATE to store/admin (`inq_update`), so a BUYER-sent message cannot bump it via RLS — see **REG-43** for the T02 decision (seller-only bump + derive buyer inbox sort from `max(inquiry_messages.sent_at)` / DEFINER trigger / accept). `createInquiry` sets it at INSERT (buyer INSERT allowed).
- **UNREAD mechanism = `inquiry_messages.is_read`, RECEIVER-WRITABLE (REG-42 CLOSED by T02-FIX).** `inquiry_messages.is_read` `BOOLEAN NOT NULL DEFAULT false` (schema L385) is the read-state column. Under the AUTHORIZED ERD §3 row-52 amendment (2026-07-22) + migration `20260722124510`, the **RECEIVER** may flip `is_read` on the OTHER party's messages: **column safety = GRANT** (`authenticated` UPDATE re-scoped to the `is_read` column only — a `body` edit is denied by the grant, error 42501), **row safety = policy** (`inq_msg_read_receipt`, party AND `sender_id <> auth.uid()`; the sender policy `inq_msg_update` is untouched but now column-confined). `markInquiryRead(inquiryId)` (no service-role, idempotent, outsider→not_found) is the write; the queries surface `unreadCount` (inbox) + per-message `isRead`/thread `unreadCount` (thread). T03/T04 render the unread indicator + mark-read-on-view. See REG-42 (CLOSED) + ADR-015.
- **`avg_response_hours` = UNMAINTAINED (re-cited, REG-26-class).** `seller_profiles.avg_response_hours` `NUMERIC(5,2)` (schema L189) has **NO trigger/function** touching it (live `pg_proc` scan of betk*/public normal functions for `%avg_response_hours%` = empty; re-confirms T06-Phase-05). T02 DECISION 2: app-layer recompute on seller reply OR formal defer (mint REG-4x, render static/NULL) — cite formula or state it as engineering choice.
- **RLS surface minted (REG-41, migration `20260722115026`).** inquiries: `inq_buyer` (SELECT, buyer OR store OR admin — pre-existing, untouched), `inq_insert` (buyer INSERT), `inq_update` (store/admin UPDATE). inquiry_messages: `inq_msg_select` + `inq_msg_insert` (thread parties; INSERT pins sender), `inq_msg_update` (sender own). No DELETE either table. No phone gate on any inquiry/message path (`requireActiveUser` is T02's gate, not `requireVerifiedPhone`).

## Phase 07 / T01 — pinned ORDER-SET CONTRACT (T02–T06 + Phase 08 cite THIS)
> Pinned 2026-07-23 (Phase 07 / T01, Opus Max). Every fact cited from `BETK_DATABASE_SCHEMA.sql` + LIVE staging (`pg_enum`/`information_schema`/`pg_trigger`/`pg_policies`) — zero-drift verified. Do not re-derive — cite this block. Migration `20260723074953_order_rls_and_conversion_link` (ledger 28→29).
- **`order_status` enum + lifecycle.** `order_status = {pending, confirmed, preparing, dispatched, delivered, cancelled, returned}` (schema L56-59; live zero-drift). **NO `'paid'` member** (payment state lives on `payments.status`, NOT order status — Phase-05 T06 precedent). `orders.status NOT NULL DEFAULT 'pending'` (L416). Terminal-meaning: **cancelled** = order killed (with `orders.cancelled_by cancelled_by_type` + `cancellation_reason` L417-418), **dispatched** = handed to courier / out for delivery, **delivered** = received (`orders.delivered_at` L422). **PHASE 07 owns** (cite BETK_PHASES L46-48): `pending→confirmed` (AC-SEL-14 seller-confirm, FIRES the stock trigger), `confirmed→preparing` (seller order mgmt), `→cancelled` **only from `pending`** (R-O03), COD auto-confirm (R-O04 — COD skips the deposit gate). **PHASE 08 owns** (delivery leg, follows the shipment-create ownership; BETK_PHASES L52-54): `→dispatched`, `dispatched→delivered` (sets `delivered_at`, opens the review window), `→returned`. Status transitions are written to `order_status_history` on EVERY transition (AC "status history written").
- **`payments` — EXACT two-row split (C2 §3.8 split-origin; payee/verification authority = OD-8 + ADR-016, supersedes ADR-002; NOT invented).** **CUSTODIAL (OD-8): payee = BETK, not the seller — the buyer pays BETK, which settles to the seller net of a flat platform commission (snapshot + derived-balance rules below).** Columns (schema L550-563): `id, order_id, payment_type, amount NUMERIC(10,2) CHECK(amount>0), method, status DEFAULT 'pending', confirmed_by, confirmed_at, transfer_reference, notes, created_at` + **OD-8 additive `payments.proof_path` (buyer's transfer screenshot; owed by T02's migration)**; **`uq_payment_type_per_order UNIQUE(order_id, payment_type)`** — this constraint is what caps an order at ≤1 deposit + ≤1 balance row. Enums: `payment_type={deposit, balance}` (L62), `payment_method={instapay, vodafone_cash, orange_cash, cod}` (L63), `payment_status={pending, confirmed, failed, refunded}` (L64). **SHAPE:** exactly TWO rows per order — **row 1** `payment_type='deposit'`, `amount = 50% of total_amount`, `method ∈ {instapay,vodafone_cash,orange_cash}` **paid to BETK's rails (from `admin_settings`, NOT the store's handles)**; **row 2** `payment_type='balance'`, `amount = the remaining 50%`, `method='cod'` **(courier collects and remits to BETK)**. The **50/50 split IS pinned** (C2 §3.8 "50% deposit upfront + 50% COD" + ADR-016); the per-piastre ROUNDING of an odd `total_amount` is NOT spec-pinned → T02 engineering choice (`deposit=round(total/2,2)`, `balance=total−deposit`, so the two always sum to `total_amount`). Status-per-row: both created `'pending'`; deposit→`confirmed` at the **ADMIN** deposit-confirmation gate (OD-8 — verification moved seller→admin, checked against the buyer's uploaded `proof_path`; awaiting-review convention = `proof_path NOT NULL AND status='pending'`); COD balance→`confirmed` on delivery (admin-confirmed after courier remittance). **Commission snapshot (OD-8 §3):** at checkout T02 snapshots `orders.commission_rate` (from `admin_settings`) + `orders.commission_amount = round(commission_rate * subtotal, 2)` onto the order (OD-8 additive columns; commission is on `subtotal`, NOT `total_amount`). **Derived-balance rule (OD-8 §6 — NO wallet/ledger table):** the seller's BETK-owed balance is COMPUTED at read time from `orders`+`payments`, never persisted — **seller net = `subtotal − commission_amount`** per order; **approved** = past the return-hold window (`delivered_at + admin_settings.return_hold_hours`, both payment rows `confirmed`), **pending** = confirmed-but-still-in-hold. **NO DB CHECK enforces `deposit+balance = total_amount`** — app-layer invariant (T02). ⚠️ **`payments` has ONLY `payments_access` (SELECT); INSERT/UPDATE policies are ABSENT → REG-49, owed by T02.**
- **R-O02 BETK-ref.** `orders.betk_ref VARCHAR(25) NOT NULL`, UNIQUE `uq_orders_betk_ref` (schema L407/L423), **NO DB default → APP-GENERATED at checkout (T02)**. Format **`BETK-YYYYMMDD-XXXX`** (UI_SPEC L237, PRD FR-BUY-6, DESIGN_BRIEF L34 — an LTR island in the RTL UI). T02 generates + guarantees uniqueness (retry on the unique violation).
- **`delivery_method` — NOT a separate buyer enum (REG-14 sibling).** `orders.delivery_method` is typed **`delivery_preference`** (schema L412) — the SAME enum as the store-side `StoreDeliveryOptions.modes`. Members `{delivery, pickup, remote}` (L54). **Do NOT create/conflate a distinct buyer-side `delivery_method` type** — checkout writes one of the store's own advertised `delivery_preference` modes (REG-14: `StoreDeliveryOptions.modes` = identical `{delivery,pickup,remote}`, live-verified match).
- **Money columns = NUMERIC(10,2), NO-FLOAT.** `orders.{delivery_fee,subtotal,total_amount}` (L413-415), `order_items.{unit_price,subtotal}` (L442-443), `payments.amount` (L554) — all `NUMERIC(10,2)`. NEVER coerce to JS `number`/`::float` for arithmetic; compute in integer piastres or NUMERIC. Live CHECKs: `chk_order_total (total_amount = subtotal + delivery_fee)` (L424), `chk_order_item_subtotal (subtotal = quantity * unit_price)` (L444), `payments.amount > 0` (L554).
- **THE TRIGGER TRAP — `decrement_stock_on_confirm`.** Live def (schema L505-509): `AFTER UPDATE OF status ON betk.orders FOR EACH ROW WHEN (OLD.status IS DISTINCT FROM NEW.status AND NEW.status = 'confirmed')`. It fires ONLY on a transition INTO `'confirmed'` via **UPDATE** — never on INSERT. ⇒ **The COD auto-confirm path (R-O04) MUST be INSERT `status='pending'` THEN UPDATE `status='confirmed'`** (two-step) so the trigger fires and decrements stock. A direct `INSERT … status='confirmed'` would NOT fire the AFTER-UPDATE trigger → stock never decrements (silent oversell). This INSERT-pending-then-UPDATE shape is the T02 contract for BOTH the COD-at-create and the deposit-confirm-later paths.
- **THE TENSION — `converted_to_order_id` writer = PINNED option (a), LANDED in T01.** Checkout is buyer-driven (buyer INSERTs the order) but `inquiries` UPDATE RLS = store/admin only (`inq_update`) → the buyer cannot write `inquiries.converted_to_order_id`. **RESOLUTION (landed):** hardened SECURITY DEFINER `AFTER INSERT ON betk.orders WHEN (NEW.inquiry_id IS NOT NULL)` trigger `trg_set_inquiry_converted_order` → `betk.set_inquiry_converted_order()` copies `NEW.id` onto the source inquiry, once (idempotent via `converted_to_order_id IS NULL` guard — first order wins). Hardened identically to `decrement_stock_on_confirm`: `SET search_path=betk,public` + `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` (definer *trigger*, never API-exposed → NO 0028/0029 advisor; distinct from ADR-012's rejected definer *RPC*). **T02 does NOT write `converted_to_order_id` from the app — the trigger owns it.** Only this derived linkage write is definer; the order INSERT itself stays fully RLS-gated (`orders_insert` + `orders_phone_gate`).
- **RLS surface minted (REG-09 + REG-48, migration `20260723074953`).** `orders`: `orders_insert` (permissive INSERT WITH CHECK `buyer_id=auth.uid()`) — combines with the pre-existing RESTRICTIVE `orders_phone_gate` (verified-phone bites HERE, OD-4). `order_items`/`order_status_history`/`order_messages`: parent-scoped `*_access` (SELECT = buyer own OR store OR admin via the parent order) + `*_insert` (order_items INSERT = buyer-of-parent; status_history INSERT = any party; order_messages INSERT pins `sender_id=auth.uid()` AND party). `order_status_history` UPDATE/DELETE stay blocked by the live append-only RULES `no_update_order_history`/`no_delete_order_history` (DO INSTEAD NOTHING — fire regardless of RLS; a party's UPDATE/DELETE is a silent no-op, NOT an error), so no UPDATE/DELETE policy exists. `order_messages` read-state NOT pinned (row 53 was NOT REG-42-amended; `is_read` unused this phase). `shipments`/`shipment_tracking_events`: **READ-only** `*_access` landed now (parties via parent order / parent shipment→order) — WRITE (store/courier INSERT/UPDATE) DEFERRED to Phase 08.
- **⚠️ REG-49 (owed by T02): `orders` UPDATE + `payments` INSERT/UPDATE policies are ABSENT.** T01 landed only the INSERT/READ foundation. T02's write layer needs: `orders` UPDATE (seller confirm/preparing + buyer cancel R-O03 + the COD two-step) and `payments` INSERT (checkout two-row create) + `payments` UPDATE (deposit/COD confirmation). Mint per ERD §3 rows 54/57-58 when T02 builds the checkout RPC + seller order-mgmt actions.
- **SHIPMENTS decision recorded for the Phase-08 checklist.** T01 landed the buyer/seller READ policies (`shipments_access`, `shipment_tracking_events_access`); Phase 08 owns the store/courier WRITE policies (INSERT/UPDATE) alongside shipment CREATE (BETK_PHASES L52-54). The `/orders/[id]` tracking section (FR-BUY-9) renders empty until Phase 08 populates.

## ERD §3 specced-but-absent-policy tables — owning-phase map (PINNED, 13 tables)
> Pinned 2026-07-19 (Phase 03 T07 gate) from the REG-25-FIX live `pg_policies` vs `BETK_ERD.md §3` audit. These tables are RLS-**enabled** but ship with **ZERO policies today** (the Phase-01 T08-F1 "policies arrive in the owning phase" class → default-deny for all until then). This is **not a defect** — each row's policies are owned by the phase that first needs the table's writes. Future phase-entry checklists MUST inherit their row: adding the specced policy is a first-class task of that phase, not a surprise. No policy here is Phase 03's to add (Phase 03 is read-only public + the two already-live self-scope write policies).

| # | Table(s) | Owning phase / decision | Note |
|---|---|---|---|
| 1–3 | `order_items`, `order_status_history`, `order_messages` | **Phase 07 — checkout/orders** (order set) | ✅ **CLOSED by Phase 07 / T01 (2026-07-23, REG-48)** — parent-scoped READ + INSERT (order_items INSERT=buyer-of-parent; status_history INSERT=any-party, UPDATE/DELETE blocked by append-only RULES; order_messages INSERT pins sender). Migration `20260723074953`. |
| 4–5 | `shipments`, `shipment_tracking_events` | **Phase 07 — orders/delivery** | ✅ **READ CLOSED by Phase 07 / T01 (2026-07-23, REG-48)** — buyer/seller/admin SELECT via parent order (FR-BUY-9 tracking). ⚠️ **Store/courier WRITE (INSERT/UPDATE) DEFERRED to Phase 08** (shipment CREATE is Phase 08 per BETK_PHASES L52-54) — Phase-08 checklist. |
| 6–7 | `dispute_evidence`, `dispute_messages` | **Disputes phase** | `disputes` itself already has its SELECT policy; the evidence/message children are owed by the disputes build. |
| 8 | `inquiry_messages` | **Messaging phase** | ✅ **CLOSED by Phase 06 / T01 (2026-07-22, REG-41)** — `inq_msg_select`/`inq_msg_insert` (thread parties) + `inq_msg_update` (sender), no DELETE; migration `20260722115026`. (`inquiries` also gained `inq_insert`/`inq_update` this migration.) ⚠️ **REG-43 STANDING:** `inquiries.last_message_at` is UNMAINTAINED (no trigger, no app write) — any Phase-07/12 "latest activity" read MUST derive from `max(inquiry_messages.sent_at)`, never `last_message_at`. |
| 9 | `restock_alerts` | **Notifications phase** | Backs R-N06 sold_out restock CTA (the CTA renders in Phase 03; the subscribe/write path + policy are notifications' to build). |
| 10 | `seller_strikes` | **Admin phase** | Admin-write / seller-read own. |
| 11 | `flagged_content` | **Admin phase** | Admin moderation surface. |
| 12 | `whatsapp_templates` | **Admin phase** | Merged tab under Admin → Settings → Notifications (OD-5); not a standalone route. |
| 13 | `sessions` | **Intentionally UNUSED (ADR-010)** | No policy owed — Supabase Auth owns session state; `betk.sessions` is a legacy/unused artifact per ADR-010, not a live surface. |

## Schema deltas to apply in Phase 01 migration (vs C3 baseline)
- migration 003: `CREATE TYPE auth_provider AS ENUM ('phone','google');`
- migration 004 (users): `phone_number` nullable; add `auth_provider NOT NULL DEFAULT 'phone'`, `deleted_at TIMESTAMPTZ NULL`, `anonymized_at TIMESTAMPTZ NULL`.
- Transaction gate: Server Action + RLS WITH CHECK requiring `phone_number IS NOT NULL` on orders/seller_profiles/payouts inserts.
- R-A05 login block also checks `deleted_at IS NULL`.

## Precedents
- Recurring architectural/procedural patterns (RLS classes, migration procedure, compose-only discipline, etc.) are catalogued in `docs/PRECEDENTS.md` — consult it before re-deriving a pattern that's already been established.

## Recent work (last 3 tasks — full detail in the journal)
- **Phase 07 / T01 — order-set DB & RLS foundation + contract pinning (2026-07-23, Opus Max).** READ-FIRST audit → ONE additive migration `20260723074953_order_rls_and_conversion_link` (ledger 28→29): REG-09 `orders_insert` + REG-48 order-children READ/INSERT (order_items/status_history/messages) + shipments/tracking READ + the `converted_to_order_id` DEFINER trigger (THE TENSION, option a — search_path-pinned + EXECUTE-revoked, advisor-clean). Advisor rls-no-policy **13→8 INFO**, all WARN byte-identical. ORDER-SET CONTRACT block pinned (enum lifecycle, 50/50 payments split, BETK-ref, delivery_method=delivery_preference, money=numeric, the trigger trap, tension, shipments decision). REG-09 + REG-48 CLOSED, **REG-49 opened** (orders UPDATE + payments INSERT/UPDATE owed by T02). CI green: typecheck 0 · guards 4/4 (i18n 736/736) · unit 120/120 · integration **25 files 188+2 skipped** (new `order.rls.test.ts` 15/15+1 opt-in, zero residue) · build 44 routes. RLS-smoke A4b FINDING→PASS (+F2/F3 retired). Schema backfilled. **HOLD — T02 not started.**
- **Phase 06 / T05 — EXIT VERIFICATION + consolidated PR PREP (2026-07-22, Opus 4.8).** DoD ledger all-PASS; full cross-task E2E lifecycle proven on staging (zero residue); DB live-state clean (ledger 28/28, advisor baseline 13, 0 new); full CI green (guards 4/4 i18n 736/736, unit 120/120, integration 173+1 skipped, build 44 routes both locales). REG-41/42/46 CLOSED, REG-43 CLOSED-BY-DECISION, REG-47 minted (record-only), REG-44/45 re-framed as one posture resolving at Phase 07. Phase-07 entry checklist written. **✅ MERGED — PR #47 → `main` @ `881832f`.**
- **Phase 06 / T04 — `/seller/inbox` + `/seller/inbox/[id]` (2026-07-22, Sonnet).** Seller inbox list + thread detail composed from T01's RLS surface + T02's write layer; REG-44 (generic buyer label) + REG-45 (no WhatsApp deep-link) symmetry applied; `avg_response_hours` rendered page-level; confirm/decline wired behind `ConfirmDialog`. Zero `components/ui`/`components/shared` edits. Full CI green + 9/9 runtime smoke (cross-seller 404, unread round-trip both directions, confirm/decline cross-surface proofs).

## Update template (append per session)
```
Date | Model | Phase | Task | Files changed | Issues | Next
```
