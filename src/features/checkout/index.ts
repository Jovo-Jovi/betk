/**
 * Feature: checkout
 * FR IDs:  FR-BUY-6 (checkout), FR-BUY-7 (payment confirmation)
 * UI Spec: §4.6–4.7 Checkout & payment
 * Tables:  betk.orders, betk.order_items, betk.payments, betk.addresses, betk.listings
 * Gate:    Verified phone required (OD-4) — enforced in Server Actions + RLS WITH CHECK.
 */

export {};
