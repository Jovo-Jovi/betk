# src/hooks/

Global React hooks shared across multiple feature areas (e.g. `useSupabaseUser`, `useRTL`, `useToast`). Feature-specific hooks live under `features/<area>/hooks/` instead.

Feature folders map to UI Spec areas — see `src/features/README.md`.
