---
alwaysApply: true
---
# BETK — core (always on)
BETK = Arabic-first, RTL marketplace (Egypt). Next.js 15 + Supabase + Tailwind/shadcn + TS strict + Zod. Scope is FROZEN (docs/01-product/BETK_MVP_SCOPE.md §6, OD-1..OD-6) — never invent pages/tables/features; if something seems missing, STOP and flag.
- Ground truth per page: docs/00-design/BETK_UI_SPEC.md. Data contract: docs/03-database/BETK_ERD.md. Start each session from docs/10-ai-development/BETK_MASTER_EXECUTION_PROMPT.md + SESSION_CONTEXT.md; update SESSION_CONTEXT + docs/12-changelog/DEVELOPMENT_JOURNAL.md after each task.
- The role auto-attaches from the files you touch (database / actions+api / ui / tests rules below). For reviews, request the security or UI review rule.
- Always: RSC by default, mutations = Server Actions, Zod-validate every action/route before any DB call, RLS is the authz boundary (handle {data,error}; denial → not-found), RTL + logical Tailwind utilities, shadcn extend-not-override. The design system (components/ui + components/shared) is owned by Claude Design — compose + wire data, never restyle.
- MODEL (CF-9, 2026-09-23; the human still picks the tier): **Grok 4.7 (high)** is the top-tier executing model and satisfies every former Opus row. Tiers are **Max / High / Medium / Low**. Max = exit gates, RLS, read-first audits. High = migrations, write layers, security review, salvage, T00 packs. Medium = compose-only UI on a settled query layer. Low = mechanical housekeeping. Do not report a model mismatch because the window is Grok 4.7. This sentence supersedes the 2026-07-22 Opus/Sonnet names and the Opus/Sonnet names in ADR-007 for execution.
